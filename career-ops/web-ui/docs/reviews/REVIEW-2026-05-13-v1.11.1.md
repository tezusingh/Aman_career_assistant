# Code Review — 2026-05-13 (v1.11.1 — deep career-ops.org docs integration)

> Patch release. v1.11.0 added a summary "About career-ops" block; v1.11.1 enriches the existing §5 Portals / §7 Scan / §14 Apply sections of every help bundle with the **full CLI flows** from career-ops.org/docs. One UI addition: `#/reports` gains a score-thresholds card.

## Scope

- **8 help bundles × 3 new subsections each** = 24 localized CLI-flow blocks injected. All structural code blocks (commands) stay verbatim; only the connecting prose translates per locale.
- **`#/reports` score-thresholds card** — collapsible `<details>` at the top of the list view with the canonical action table, sources to `career-ops.org/docs/.../scan-job-portals`.
- **`qa/claude-cowork-browser-test-prompt.md`** — added Scenario 17 (career-ops.org/docs coverage, 5 sub-assertions) + Scenario 18 (help bundle parity regression).

Baseline before slice: 348/349 unit + 28/28 Playwright.
After slice: **348/349 unit + 28/28 Playwright**. Coverage **94.59 % line / 84.18 % branch**.

## What changed

### `docs/help/{en, ru, es, pt-BR, ko-KR, ja, zh-CN, zh-TW}.md`

Each bundle gained three new subsections — inserted right before `## 6.`, `## 8.`, and `## 15.` respectively so the existing 16-H2 contract stays intact.

**§5 → CLI flow** (career-ops.org/docs/.../scan-job-portals):

- `cp templates/portals.example.yml portals.yml` setup command.
- Canonical schema for the three `portals.yml` sections matching the SPA-side schema 1:1 (title_filter / tracked_companies / search_queries).

**§7 → CLI scan flow** (career-ops.org/docs/.../scan-job-portals):

- Option A — direct script: `npm run scan` (+ `--dry-run`, `--company`).
- Option B — AI-powered browser scan: `/career-ops scan`.
- Action thresholds table (≥4.5 / 4.0–4.4 / 3.5–3.9 / <3.5).

**§14 → 3 new subsections**:

- **Full CLI apply flow** (career-ops.org/docs/.../apply-for-a-job) — 8-step numbered walkthrough from `/career-ops apply <company>` through Playwright opening the browser, draft answers, flagged items, user clicks Submit, `Submitted.` confirmation, tracker flip `Evaluated → Applied`.
- **Batch evaluate** (career-ops.org/docs/.../batch-evaluate-offers) — full runner: `batch/batch-input.tsv` columns, dry-run, parallel modes (`--parallel 2|3`), `--min-score`, `--retry-failed`, `node merge-tracker.mjs`.
- **Playwright setup** (career-ops.org/docs/.../set-up-playwright) — `npm install` + `npx playwright install chromium` + `claude mcp add playwright npx @playwright/mcp@latest` + alternative `.claude/settings.local.json` registration.

H2 parity preserved at 16/locale (`tests/help-ui.test.mjs::section-parity` stays green).

### `public/js/views/reports.js` + `public/js/lib/i18n.js`

New collapsible card at the top of `#/reports` list view. `<details open>` with summary "🎯 Score → next step" and an inline table of the 4 action thresholds + source link to `career-ops.org/docs/.../scan-job-portals`.

7 new i18n keys (across 8 locales): `rep.thresholdsTitle`, `rep.score`, `rep.thrAction`, `rep.thr45`, `rep.thr40`, `rep.thr35`, `rep.thrLow`, `rep.thresholdsSource`.

### `qa/claude-cowork-browser-test-prompt.md`

Two new scenarios added at the bottom:

- **Scenario 17 — career-ops.org/docs coverage in Help** (5 sub-assertions): 17.1 About-career-ops block in 8 locales · 17.2 CLI-flow subsections in §5/§7/§14 · 17.3 README block in 8 locales · 17.4 `#/apply` Playwright link · 17.5 `#/reports` score-thresholds card.
- **Scenario 18 — Help bundle parity** — regression for `help-ui::section-parity` (all 8 bundles return 16 H2 sections).

## Verification

```
npm test                # 348/349 (1 pre-existing parent drift)
npm run test:coverage   # 94.59 % line / 84.18 % branch
npm run test:e2e        # 20/20
npm run test:e2e:full   # 23/23
npm run test:e2e:browser # 28/28 Playwright

grep -c "^## " docs/help/*.md   # → 16 per file, all 8 files
grep -l "career-ops.org/docs/introduction/guides/scan-job-portals" docs/help/*.md  # → all 8
```

## Out of scope (deferred to future)

| Item | Why |
|---|---|
| Batch evaluate SPA page | Canonical docs describe a CLI-only loop; SPA equivalent = new view + 3 endpoints + fixtures + tests. Multi-day phase. Documented in help §14 instead. |
| F-018 full adapter-registry consolidation | Still queued; label-only slice closed in v1.10.3. |
| Full multer pipeline (PR-4) | v1.10.2 closed the data-corruption hole via 415 envelope; full parser is its own phase. |
