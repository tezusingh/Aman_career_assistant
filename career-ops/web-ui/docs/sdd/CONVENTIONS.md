# CONVENTIONS — career-ops-ui

> Project-specific coding conventions. Extends `~/.claude/rules/coding-style.md`. When user-global rules and these conflict, the more restrictive wins.

## Module system

- `"type": "module"` everywhere. ESM only.
- Server files: `.mjs`. Built-ins use the `node:` prefix (`node:fs`, `node:path`, `node:url`, `node:child_process`).
- SPA files: `.js`. Loaded as classic scripts via `<script src="…">` in `public/index.html`. They expose globals (`window.Router`, `window.API`, `window.UI`, `window.I18n`) — do not introduce ES module imports on the client until a build step is adopted (a ROADMAP-level decision).

## File size

| Size | Action |
|---|---|
| < 400 lines | Target. |
| 400 – 800 lines | Add `// TODO: split by concern` at top; split at next opportunity. |
| > 800 lines | Split before merging the PR. |

File-size status of the largest files (as of **v1.206.0**):

- `server/index.mjs` (**273 LOC** as of v1.206.0; was reduced to ~174 by **P-2 phase 2** in v1.9.0 and grows as `register*Routes(app)` calls are added — still well within target). Pure orchestrator — middleware + `register*Routes(app)` calls + SPA catch-all. New routes go into `server/lib/routes/<topic>.mjs` exporting `register<Topic>Routes(app)`. **Currently 36 route modules** (v1.200.0–v1.205.0 added `liveness` (GET /api/liveness — zero-token ATS "still live?" check), `discover-ats` (POST /api/portals/discover + /track — company-name → Greenhouse/Ashby/Lever board resolver via safeGet, explicit control-char-guarded portals.yml write), `cv-sync` (GET /api/cv-sync-check — read-only "Setup doctor" relay of cv-sync-check.mjs), `assessments` (GET/POST /api/assessments — Skills log; POST is a control-char-guarded array-args write to assessment-log.mjs); as of v1.133.0 — adds `funded`: `GET /api/company-funded` read-only relay of the parent's `company-funded.mjs` funded-company discovery, `--json --dry-run`, no writes; the weekly interview digest `GET /api/interview/weekly-digest` relay of `weekly-digest.mjs` was added to the existing `interview` module, not a new one): `activity`, `funded`, `auto-pipeline`, `batch`, `career-plan` (v1.95.0 — `GET`/`PUT /api/career-plan` writes `config/career-plan.md`; `POST /api/career-plan/generate` AI development plan), `cli-detect` (v1.103.0 — `GET /api/cli-detect` read-only PATH scan for installed agent CLIs; never executes a found binary), `config`, `content`, `cv-studio` (v1.92.0 — CV humanize/voice-match; v1.101.0 — `POST /api/cv-studio/tailor` tailor CV + cover letter through a generic recruiter checklist gate; no writes), `docs-assistant` (v1.102.0 — `POST /api/docs-assistant/ask` answers grounded ONLY in `docs/help/<lang>.md`; no user data, no writes), `export` (v1.100.0 — `POST /api/export/docx` dependency-free `.docx`; stateless, bounded, no writes), `health`, `help`, `interview` (v1.90.0 — mock interview turn/save/sessions), `jds`, `llm`, `logos` (v1.104.0 — `GET /api/logo?domain=` company-favicon proxy via SSRF-safe `safeGet` binary mode; LRU-cached, no disk writes), `market` (v1.94.0 — `POST /api/stats/market` AI salary/market report from CV+profile target roles + region + currency; no writes), `memory` (v1.93.0 — about-me note GET/PUT/suggest; writes `config/memory.md`), `networking` (v1.91.0 — networking plan/save/plans; writes `networking/`), `openrouter`, `orientation` (v1.96.0 — `POST /api/orientation/generate` AI career-orientation profile from CV+profile+two-pager+memory; reflection-not-test framing, no writes), `pipeline`, `portals` (v1.99.0 — `POST /api/portals/health` SSRF-safe probes tracked `careers_url`s; read-only), `reports`, `runners`, `scan`, `stats` (v1.86.0 — target-role stats snapshot store), `tracker`, `two-pager` (v1.89.0 — candidate two-pager GET/PUT/draft; writes `config/two-pager.yml`; v1.100.0 — live YAML auto-fill), `usage` (v1.105.0 — `GET /api/usage` per-provider token + estimated-USD rollup of `data/llm-usage.jsonl`; read-only). Shared live-LLM cascade lives in `server/lib/llm-dispatch.mjs` (`runActiveProvider` / `providerAvailable`), used by new live-LLM routes.
- `public/js/views/scan.js` (**648 LOC** as of v1.156.0 — down from ~1254 after two extraction passes; **pass 1** (v1.132.0): the results-rendering subsystem (`renderResults`, `buildChipRow`, `getRows`, the row/facet builders, the seniority/country option painters, and the `FALLBACK_SOURCES` registry mirror) moved verbatim into `public/js/lib/scan-results.js` — a `window.ScanResults.create(ctx)` factory that closes over a context object the view supplies (filter elements, the active-facet Sets, the paginator, two-pager data, a `lastResults` getter); the view now calls `SR.render()`/`SR.getRows()`). **Now within the 800-LOC limit.** Extraction pass 2 (v1.156.0) cut the scan-execution engine and the filter state machine into `views/scan/runner.js` (`createScanRunner(ctx)`) + `views/scan/filters.js` (`createScanFilters(refs, deps)`). Touch with caution; this is the most user-facing view. Source-static tests read the two files via `tests/helpers/scan-src.mjs::loadScanSrc()`; the extraction is verified in a real browser by `tests/playwright-scan-filters.mjs` (seeds `data/last-scan.json`, drives every filter, asserts exact row counts).
- `public/js/views/config.js` (**854 LOC** as of v1.206.0; was ~1010 LOC at v1.131.1, cut to ~783 by the v1.155.0 split into `views/config/*.js`, then grew back with the OpenRouter provider panel, the API-keys summary chip, the AI-CLI-tools tab, and the Appearance/logos card). **Now exceeds the 800-LOC hard target again — a further split is owed** (`// TODO: split by concern`); tracked as known debt, not addressed in a docs-only release.
- `public/css/app.css` — **split in v1.131.2** (was ~1990 LOC). Now three ordered stylesheets, each within the hard limit: `app.css` (~740 — a11y, tokens/theme, sidebar, main, buttons, content-shell), `components.css` (~729 — cards, grids, paginator, badges, tables, forms, log, markdown, language switcher, chip filter, connection banner), `overlays.css` (~742 — toast, notifications drawer, modal, misc/responsive, the `[dir="rtl"]` mirror, docs-fab, usage-hud). They load as three `<link>`s **in that exact order** (cascade is byte-for-byte identical to the pre-split file). Tests assert on CSS via `tests/helpers/css.mjs::loadAppCss()` (reads the concatenation); the split is locked by `tests/css-modularization.test.mjs` (existence · ≤ 800 LOC each · index.html link order).
- `scan.js` (648 LOC after pass 2, v1.156.0) clears the 800-LOC hard target; `config.js` (854 LOC as of v1.206.0) has grown back over it since the v1.155.0 split and is owed a further pass. Both were no-bundler refactors (extracted panels become separate `<script>`s in `index.html` exposing `window.*` helpers, mirroring the `app.css`→3-sheet and `scan.js`→`scan-results.js` cuts).

## Naming

- Files: `kebab-case.mjs` / `kebab-case.js` / `kebab-case.test.mjs`.
- Functions: `camelCase`.
- Constants from configuration: `SCREAMING_SNAKE` (e.g. `KNOWN_KEYS`, `SECRET_KEYS`, `MODE_ALLOWLIST`).
- Tests: `tests/<feature>.test.mjs` matching the module under test where possible.

## Express routes

- Method + path:
  - `GET /api/<resource>` — list / read.
  - `POST /api/<resource>` — create / append (idempotent dedup is fine).
  - `PUT /api/<resource>` — replace.
  - `DELETE /api/<resource>/:id` — delete.
  - `GET /api/stream/<verb>` — SSE long-running. Headers: `text/event-stream`, `no-cache`, `keep-alive`, `X-Accel-Buffering: no`. Events: `start`, `log`, `done`, `error`.
- Status codes: `200` ok, `400` validation, `404` not found, `413` body too large, `500` unexpected, `502` upstream LLM failure.
- Response body: always JSON, even for errors: `{ error: '...', details?: ... }`.
- Validation: gate inputs at the route handler. Don't trust `req.body` shapes — destructure with defaults.
- File-system writes: always `mkdirSync(dir, { recursive: true })` first. Never assume the parent directory exists.

## Sanitizers — single-source rules

These functions live in `server/lib/security.mjs` (after v1.21.0 H-4 consolidation — pre-v1.21 they were scattered across `server/index.mjs` and individual route files). DO NOT duplicate. Route every relevant ingress through them:

| Concern | Function | Module |
|---|---|---|
| Job URL → SSRF guard | `isValidJobUrl(url)` | `server/lib/security.mjs` |
| Outbound HTTP with DNS-pinned redirect-revalidation | `safeGet(url, opts)` | `server/lib/safe-fetch.mjs` (v1.21 B-1) |
| CV / markdown → XSS strip (entity-aware) | `stripDangerousMarkdown(md)` | `server/lib/security.mjs` (v1.22 M-4) |
| JD text → length / char strip | `sanitizeJobDescription(jd)` | `server/lib/security.mjs` |
| User-supplied slug → filesystem-safe | `sanitizePathName(s)` | `server/lib/security.mjs` (v1.21 H-4) |
| File-lock concurrency control | `withFileLock(path, fn)` | `server/lib/file-lock.mjs` (v1.21 H-6) |
| LLM rate limiting | `llmRateLimit` middleware | `server/lib/rate-limit.mjs` (v1.21 H-5) |
| Env-config writes | `validateConfig(body)` then `updateEnvFile(...)` | `server/lib/env-config.mjs` |
| Activity log redaction | handled by `activityMiddleware` | `server/lib/activity.mjs` |
| Scan-write (external feed metadata → `scan-history.tsv`) | `normalizeScanScalar` / `normalizeScanUrl` / `sanitizeTsvField` | `server/lib/scan-sanitize.mjs` (v1.75.0; v1.75.1 widened the separator set to `\r \n \t \v \f U+2028 U+2029`) |

Route reviewers (`web-ui-route-reviewer` agent) flag every miss.

## Client-side patterns

- **Event handlers via `addEventListener`.** Never `onclick=`. CSP enforces this.
- **Markdown rendering** for CV / reports / interview-prep goes through the project markdown renderer (sanitizes `<script>`, `javascript:` URLs, `onerror=` attributes). Never bypass.
- **API calls** through `window.API.{get,post,put,delete}`. Never raw `fetch` from a view.
- **SSE consumers** via `API.stream(path, onEvent)`. Auto-closes the `EventSource` on `done` (single-phase) or on the final `done` with `data.final !== false` (multi-phase, see v1.29.2 invariant M-13 in `qa/REGRESSION-v1.29.2.md`).
- **Navigation** via `Router.go('/path')`. Never set `window.location.hash` directly outside `router.js`. The router strips `?query` from the route-name lookup (v1.28.1 fix); views parse query params from `window.location.hash.split('?')[1]` themselves via `URLSearchParams`.
- **Paginator** via `UI.paginate({ pageSize, onChange })`. Used by `#/tracker` / `#/reports` / `#/activity` / `#/scan` (v1.30.0). Filter inputs MUST call `pager.reset()` so a deep-page user lands on page 1 when their search narrows the result set.
- **Source-list lookups** (`#/scan` filter dropdown, regional scanner dispatch) read from `server/lib/sources/registry.mjs` via `GET /api/scan/sources`. Since v1.69.0 (P-14) this registry auto-discovers adapters: a `<slug>.mjs` with an `export const meta` block dropped into `server/lib/sources/` shows up in the dropdown with no registry edit. Never hardcode a source list in a view or the dispatcher.
  - **Two registries, don't conflate them (v1.75.0).** `server/lib/sources/registry.mjs` (auto-discovered `meta`, drives the *dropdown* + RU dispatch) is distinct from `server/lib/portals/registry.mjs` — the hand-maintained `ALL_ADAPTERS` array the EN scanner walks to actually *fetch* a board (`adapter.matches(company)` → `buildEndpoint` → fetch). A genuinely new EN board needs **both**: the auto-discovered `meta` file under `sources/`, AND an adapter under `portals/adapters/<slug>.mjs` imported into `ALL_ADAPTERS`. So "drop a file, no registry edit" is true only for dropdown visibility, not for fetching.
  - **Aggregators select by `provider:`, not `careers_url` (v1.75.0).** The seven v1.75.0 aggregators (RemoteOK / Remotive / Working Nomads / IBM / Arbeitsagentur / Glints / Jobstreet · SEEK) match on an explicit `provider: <slug>` field on a `tracked_companies` entry. The four config-driven ones read a per-entry `<provider>:` block (e.g. `glints: { searchKeywords, countryCode }`) which `en-scanner.mjs` threads to every fetcher as `opts.company`. URL-detected ATS fetchers ignore the extra opt. `buildEndpoint` must always return a string.

## i18n

- Static strings: `<element data-i18n="key.path">English fallback</element>`.
- Dynamic strings: `I18n.t('key.path', 'English fallback')`.
- New key → add it to **all 17 per-locale files** (`en, es, pt-BR, ko, ja, ru, zh-CN, zh-TW, fr, pl, uk, da, ar, de, it, tr, hi` — note `ko` in the dict but `ko-KR.md` for help / README files). The English fallback must always be present in code; locale files override it. **Arabic (`ar`) is RTL** — `i18n.js` flips `<html dir>` and `app.css` has a scoped `[dir="rtl"]` block.
- **Per-locale layout (I18N-SPLIT, v1.60.0).** Translations live in one file per locale under `public/js/lib/locales/` — `i18n-dict.<lang>.js`, each assigning `window.__I18N_DICT_<LANG> = { 'key.path': 'string' }` — plus a shared `i18n-dict.aliases.js` (`window.__I18N_ALIASES = { 'key': 'canonical.key' }`). A translator edits a single language in isolation (the i18next / OpenWA layout). `public/js/lib/i18n-dict.js` is now a small **assembler** that merges those tables back into the key-major `window.__I18N_DICT` that `i18n.js`'s `t()` consumes — `t()` and every call-site are unchanged. `<script>` order in `public/index.html`: the 17 locale files → `i18n-dict.aliases.js` → `i18n-dict.js` (assembler) → `i18n.js`. No build step, no runtime fetch. `tests/i18n-locale-files.test.mjs` locks the load order, per-locale key parity, alias integrity, and a lossless-migration snapshot (`tests/fixtures/i18n-dict.snapshot.json`). Node tests/tooling load the dict through `tests/helpers/i18n-vm.mjs` (`loadAssembledDict`, `loadI18n`, `legacyDictText`, `allLocaleSource`).
- `@alias` keys (sidebar label = page title = dashboard tile) share ONE translated string with their canonical key; alias targets must exist and must not themselves be aliases (`tools/i18n-audit.mjs` + `tests/i18n-alias.test.mjs` enforce it). Keys that merely collapse in English but diverge in another locale (e.g. `nav.config` vs `config.title`) are NOT aliased.
- Help-bundle markdown lives in `docs/help/<locale>.md` (**17 files**: `en, es, pt-BR, ko-KR, ja, ru, zh-CN, zh-TW, fr, pl, uk, da, ar, de, it, tr, hi` — pl/uk/ar added in v1.71.1; da in v1.77.0; de/it/tr in v1.85.0; hi in v1.122.0). CI invariant: **31 H2 sections** (`tests/canonical-docs-coverage.test.mjs` + `tests/help-ui.test.mjs` `SECTION_COUNT`) and **113 H3** (`tests/help-ru-config-section.test.mjs` + `tests/locales-de-it-tr.test.mjs`). §19 "Localizing the app" added in v1.60.0; §20 "Statistics by target roles" (+3 H3) added in v1.86.0; §21 "Your two-pager" (+3 H3) added in v1.89.0; §22 "Mock interview" (+3 H3) added in v1.90.0; §23 "Networking & deep company research" (+3 H3) added in v1.91.0; §24 "CV Studio" (+3 H3) added in v1.92.0; §25 "Memory" (+3 H3) added in v1.93.0; §26 "Statistics" (rework — market report / pipeline / trend, +3 H3) added in v1.94.0; §27 "Career plan" (+3 H3) added in v1.95.0; §28 "Career orientation" (+3 H3) added in v1.96.0; §29 "The CareerOps Manifesto" (+2 H3) added in v1.120.0 (parent v1.20.0 parity); §30 "Hermes & Telegram" (+3 H3) added in v1.147.0; §31 "Running the whole stack in the cloud" (+4 H3) added in v1.154.0.
- `tests/i18n-coverage.test.mjs` enforces locale parity. If you add a key and forget a locale, that test fails.
- CHANGELOG parity: all 17 `CHANGELOG*.md` files (EN + 16 locales) must reference the same `## [vX.Y.Z]` top — `scripts/check-changelog-parity.mjs` is the gate (part of `npm run test:ci`).

## Error handling

- Server: catch known failure modes (file not found, parse error, upstream timeout) and return a 4xx/5xx with a JSON body. Let unexpected errors bubble — Express's default 500 is acceptable.
- Client: `API` wrapper transforms network errors into a banner. Per-view try/catch toasts a friendly message and offers retry.

## Logging

- Server: `console.log` for startup banners and one-shot lifecycle events. `console.warn` for recoverable degradations. No `console.error` for handled errors — those belong in the activity log.
- Never log full request bodies. Never log values for keys in `SECRET_KEYS`. The activity-log middleware redacts on the way in; respect that.
- Client: `console.error(err)` for unhandled render errors so DevTools shows the stack — paired with the toast.

## Testing

- `npm test` runs `node --test tests/*.test.mjs tests/acceptance/*.test.mjs` (the acceptance dir was added in v1.26.0 — see [`docs/architecture/TESTING.md`](../architecture/TESTING.md) for the 4-tier pyramid).
- In-process via `createApp()`. Bind to ephemeral port (`server.listen(0)`).
- Fixtures: tests bootstrap any parent layout they need under `mkdtemp`. No reliance on `../cv.md` etc. — `tests/test-isolation-reviewer` enforces this on every new test diff.
- Coverage floor: 80 % line / 75 % branch on non-trivial logic. Current baseline ~93 % line / ~83 % branch — keep at or above. Exempt: pure data mappers, getters, generated code.
- TDD when adding behavior. Pure refactors with full coverage may skip TDD.
- Real network is **forbidden** in tests (CI-isolation contract). Source adapters take a `fetchImpl` opt that defaults to `globalThis.fetch`; tests inject a mock that returns canned responses. Safe-fetch supports the same via `_setTransport()`.
- Long-running tests (E2E): keep them under `tests/e2e*.mjs` / `tests/playwright-*.mjs`, not in the default `npm test` matcher. `npm run test:e2e:browser` drives them.
- CI gates (run via `npm run test:ci`): unit + acceptance + `scripts/check-no-also-leftovers.mjs` (no `.also(` patterns leaking into views) + `scripts/check-changelog-parity.mjs` (all 16 non-EN locales at the same version).
- Current count as of **v1.213.0**: **2724** (v1.213.0 — parity delta: **Added** the **MyCareersFuture** (Singapore) scan source (`+30` incl. adapter/host-guard/pagination tests; registry **82** = 77 EN + 5 RU, `ALL_ADAPTERS` **77**); **Fixed** — Greenhouse boards are now fetched with `content=true` and decoded to a plain-text `description` so the content_filter (which reads `j.description ?? j.snippet`) matches them (`+3`), and Ashby appends `Remote` to the location + lets `workplaceType` win over a stale `isRemote` so remote roles survive a city filter (`+4`). Deferred: a DNS-rebinding guard (validate a host's resolved address before connecting) — needs a web-ui-specific design (web-ui uses per-call fetchImpl injection, not the parent's global patched fetch). Previously **v1.212.1**: **2687** (v1.212.1 — **Fix**: the cvstart.org landing under-counted scan sources (showed **80**, dropped **Job Bank (Canada)**) while the app/dropdown/help all listed **81** — `jobbankca` top-level-imported `js-yaml`, which the Pages build (site/-only `npm ci`) can’t resolve, so the registry enumeration silently dropped it. Fixed by lazy-loading js-yaml (`await import` inside `resolveProfileKeywords`) so every source is import-safe for the dep-free enumeration, plus a `sync-assets.mjs` hard-fail when the registry count ≠ the adapter file count, and a static gate in `tests/site-sources.test.mjs` forbidding any source top-level-importing a third-party package (`+2`). Previously **v1.212.0**: **2685** (v1.212.0 — career-ops 1.28.0 parity: **Added** the **Job Bank (Canada)** federal ATOM scan source (`+24` incl. adapter/security tests; registry **81** = 76 EN + 5 RU, `ALL_ADAPTERS` 76 — net unchanged because the bot-blocked **EchoJobs** source was **Removed**, `−10`); **Fixed** — Consider now performs an anonymous CSRF handshake before its `POST /jobs` so its boards return results again (`+1`), and multi-location Lever roles merge `location`+`allLocations` so no eligible city is dropped (`+3`); gentler 250 ms inter-page pacing on eightfold/oraclecloud/tencent. Deferred: the anchored-keyword `word:` prefix matcher (opt-in scan-quality, cleanly separable). Previously **v1.211.0**: **2667** (v1.211.0 — post-1.27.0-tag parity delta: **Added** the **Yourator** Taiwan tech source (`+19`, registry **81** = 76 EN + 5 RU, `ALL_ADAPTERS` 76); **Fixed** — the shared `html-entities.mjs` gained the Latin-1 letter entities so accented named entities (`&eacute;`→é) decode case-correctly everywhere (`+3`, also closes the `&constructor;` prototype-lookup), and `trust-validator.mjs::asciiFoldForHostname` NFD-folds accented company names so "Işık"/isik.com.tr no longer flags (`+1`). Previously **v1.210.1**: **2644** — v1.210.1 — Fixed: **Habr Career title/company HTML-entity decode** — the 6th and last affected source; the SSR cards arrived escaped so an undecoded "&" failed the title filter and company names reached the tracker/reports mangled; +1 case in `tests/sources-title-entity-decode.test.mjs`. Also pinned the help §17 "As of vX" registry-count anchor to the current version. Previously **v1.210.0**: **2643** — v1.210.0 — career-ops 1.27.0 parity: the **Senjob** scan source (first African board, Senegal) + HTML-entity title decoding across five providers (beesite / csod / hackernews / phenom / tkms); registry **80** = 75 EN + 5 RU, `ALL_ADAPTERS` 75; previously recorded **v1.197.0**: **2527** — v1.157.0 — **Fix: live evals run on ANY configured provider, not just Anthropic/Gemini** — a user with only `OPENROUTER_API_KEY` was forced to manual mode because a keyless `LLM_PROVIDER=claude` pin (from `init`) dead-ended. Now a forced provider whose key isn't set falls back to the auto order among configured providers (`env-config.mjs::selectActiveProvider` + both dispatch gates `routes/llm.mjs::_provGate` + `llm-dispatch.mjs::gate`); the SPA gates live-eval via the new `public/js/lib/provider-status.js` (`window.ProviderStatus` → `/api/status/providers`, all 7) instead of a stale `ANTHROPIC/GEMINI_API_KEY` health probe; hints reworded ×17 + 3 new `dash.system.*` keys (snapshot 1214→1217); `tests/live-provider-gating.test.mjs` **+4** + a `selectActiveProvider` fallback case; previously **v1.156.0**: **2396** (v1.156.0 — **Refactor: split `scan.js` under the 800-line limit (P-16)** — `public/js/views/scan.js` 906 → **648** by extracting `scan/runner.js` (`createScanRunner(ctx)` — scan-execution engine) and `scan/filters.js` (`createScanFilters(refs, deps)` — filter state machine); the `loadScanSrc()` test helper now concatenates scan.js + the two new files + lib/scan-results.js so 4 source-reading tests auto-repoint; **both oversized views now under 800 (P-15/P-16 done)**. Also fixes CodeQL `js/useless-assignment-to-local` (#428) in `config/tab-controller.js` (`let n = i;` → `let n;`). Pure refactor, no new test cases; previously **v1.155.0**: **2396** (v1.155.0 — **Refactor: split `config.js` under the 800-line limit (P-15)** — `public/js/views/config.js` 1030 → **783** by extracting `config/field-specs.js` (model lists + FIELDS descriptor data) and `config/tab-controller.js` (the ARIA tab-bar factory); 6 source-reading tests repointed to the new files; pure refactor, no new test cases (`scan.js` 906 intentionally left — already partially split via `lib/scan-results.js`, remaining core too coupled for a clean mechanical extraction); previously **v1.154.0**: **2396** (v1.154.0 — **New guide "Running the whole stack in the cloud"** — docs-only: in-app Help §31 ×17 (career-ops + career-ops-ui + engine [Claude subscription / Hermes / API keys] on a cloud server), a README section ×17, and a wiki page; the four help-bundle gate tests move to the **31 H2 / 113 H3** contract; **no new test cases** (assertions updated, not added), count unchanged; previously **v1.153.0**: **2396** (v1.153.0 — **Jobvite scanner migrated to the public XML feed (parent-sync)** — the parent retired Jobvite's JSON API (returns zero jobs); web-ui's `sources/jobvite.mjs` used that dead endpoint, so it now reads `https://app.jobvite.com/CompanyJobs/Xml.aspx?c={companyEId}` (XML parse, `companyEId` resolved from `company_eid:` config → `c=` param → board discovery), pinning **two** hosts via `assertJobviteUrl` (https-only, no redirect followed); `http-json.mjs::fetchText` gained read-only `.location`/`.retryAfter` on non-ok; `deploy-pages.yml` concurrency → `cancel-in-progress:false` so a superseded Pages build no longer reds the status; `tests/sources-jobvite.test.mjs` rewritten **+4**; previously **v1.152.0**: **2392** (v1.152.0 — **Hermes provider completion + docs actualization** — a code review of v1.151.0 fixed two real gaps: the `#/config` `LLM_PROVIDER` dropdown gained `hermes` (8th option, was unforceable) guarded by a new dropdown-vs-`LLM_PROVIDERS` parity test, and `hasHermesKey` relaxes `isUsableKey`'s 20-char floor to 8 via a `minLen` param (short self-hosted `API_SERVER_KEY`); plus `hermesChatUrl` bare-host completion, the `routes/llm.mjs` manual-fallback copy naming Hermes, and a `health.mjs` `HERMES_API_KEY` row; the six-provider chain/force-list strings normalized to the full seven across README ×17 / help ×17 / `config.llmProviderHint` ×17 / `docs/sdd`, snapshot 1214; **+2**; previously **v1.151.0**: **2390** (v1.151.0 — **Hermes wired as an LLM provider (Phase 5, Shape A)** — the scoping spike confirmed `hermes gateway` is OpenAI-compatible (`POST /v1/chat/completions`), so `runHermes` (shared `runOpenAICompatible` client) joined both dispatch cascades + the auto provider-order tail + `LLM_PROVIDER=hermes` pin + `/api/status/providers` + `llm-pricing.mjs`; `HERMES_API_KEY`/`HERMES_BASE_URL`/`HERMES_MODEL` in `#/config` (6 i18n keys ×17, snapshot 1208→1214); configured-endpoint fetch (not the SSRF job-URL guard); `tests/hermes-provider.test.mjs` **+5** + the inverted v1.146.0 canary + updated provider-surface tests; docs (HERMES.md/help §30 ×17/README teaser ×14/skill/roadmap) moved planned→wired; previously **v1.150.0**: **2385** (v1.150.0 — **Consistent empty states (Phase 4 polish)** — `#/activity`, `#/cv-studio`, `#/stats`, `#/usage` dropped a redundant inline `style: { padding: '40px', textAlign: 'center', color: 'var(--foggy)' }` on their empty panels; those 3 properties are already owned by the shared `.empty` class (tokenized `--space-7`=48px, centered, muted, dashed border), so the 4 now render pixel-identical to the ~25 other `.empty` panels instead of drifting on a magic 40px; legit per-view overrides (`#/dashboard` width, `#/pipeline` border) untouched; client CSS-usage cleanup only (no `.empty` rule / route / i18n change); `tests/empty-state-consistency.test.mjs` **+2**; previously **v1.149.0**: **2383** (v1.149.0 — **Portals → Settings (Phase 4)** — the `#/portals` sidebar nav item moved from the *Sourcing* group into the *Setup* (settings) group next to *App settings* in `public/index.html` — it's a settings surface since v1.144.0 (enable/disable tracked companies + ATS health), not a sourcing action; nav markup only (route/view/`nav.portals` label unchanged); `tests/portals-nav-placement.test.mjs` **+2**; previously **v1.148.0**: **2381** (v1.148.0 — **Cleaner scan filters (Phase 4)** — the `#/scan` result-filter panel moved from a ragged flex-wrap of rigid 160–240px boxes to a responsive `grid` (`repeat(auto-fill, minmax(180px, 1fr))`), with the Apply/Reset actions on a separated, right-aligned full-width row; CSS + a small `scan.js` DOM cleanup only — every `#scan-filter-*` id + `SR.render()` wiring preserved, no new i18n keys; `tests/scan-filters-grid.test.mjs` **+3**; previously **v1.147.0**: **2378** (v1.147.0 — **Hermes & Telegram — in-app help §30 (Phase 5b, part 2)** — a new help-guide section (what Hermes is + the two integration shapes, running on a cloud server, Telegram via Hermes + "what NOT to expose") fanned out to all 17 locales, reachable from `#/help` and auto-grounding the `docs-assistant`/`DocsFab`; help-bundle gate lifted **29→30 H2 / 105→108 H3** (`canonical-docs-coverage`/`help-ui`/`help-ru-config-section`); a cvstart.org footer deep-link to the guide; `tests/help-hermes-section.test.mjs` **+2** (every locale carries §30 with its language-independent anchors). Still docs-only — the Hermes LLM-provider stays planned/not-wired. Closes the Phase 5b docs+skill deliverable; previously **v1.146.0**: **2376** (v1.146.0 — **Hermes agent + Telegram (Phase 5b, part 1)** — a docs + skill deliverable: `docs/integrations/HERMES.md` deep-dive (two integration shapes, cloud-server deploy, Telegram-via-Hermes, threat model), a `## Hermes agent + Telegram` README teaser mirrored across the fully-translated locale READMEs, a `hermes-bridge` skill, and an OVERVIEW **Integrations** link; the Hermes LLM-provider path stays **planned / not-yet-wired** (blocked on the Phase 5 API-contract spike); `tests/hermes-docs.test.mjs` **+4** (asserts the honesty markers + that `llm-dispatch.mjs` has no Hermes/Nous branch); previously **v1.145.0**: **2372** (v1.145.0 — **Insightful stats (cont.): a rebuildable chart** — `#/stats` Target-role-trend tab (`renderTrend`) gains a **Build a chart** widget: metric (vacancies/median/avg salary) × dimension (by country/by role) selects that re-render, salary metrics honoring the currency + year/month controls, from the existing RoleStats.aggregate; 8 new i18n keys × 17, snapshot 1200→1208; `tests/stats-custom-chart.test.mjs` **+2**; previously **v1.144.0**: **2370** (v1.144.0 — **Settings & filters (Phase 4, part 1): enable/disable tracked portals** — new `POST /api/portals/toggle` surgically flips a company `enabled` flag in portals.yml (comment/order-preserving text edit + parse-validate, keyed by careers_url), Enable/Disable button per company on `#/portals`; scanner unchanged (en-scanner already filters `enabled !== false`); 5 new i18n keys × 17, snapshot 1195→1200; `tests/portals-routes.test.mjs` **+3** (setEnabledInRaw + toggle round-trip + 404); previously **v1.143.0**: **2365** (v1.143.0 — **Understandable (cont.): `?` hints on 9 core workflow views** (scan/evaluate/cv-studio/tracker/config/deep/batch/auto/apply via `HelpHint.title`), 9 new i18n keys × 17, snapshot 1186→1195; `tests/help-hint.test.mjs` **+1** (9-view wiring guard); config+batch each wired on both their main + error-state `<h1>`; previously **v1.142.0**: **2364** (v1.142.0 — **Correctness: no more "Unknown" career archetype**: `#/orientation` AI prompt now MUST rank the top 3 from exactly the 8 named vectors and NEVER answer "Unknown"/"N/A"/invent a label (server-only `buildOrientationPrompt` change, no i18n); `tests/orientation-routes.test.mjs` **+1**); previously **v1.141.0**: **2363** (v1.141.0 — **Insightful stats (cont.): funded-company enrichment**: `#/funded` flat table → **enriched card grid** — company **logo** (`CompanyLogo.badge`/`avatar`), **round/amount/discovery-score** chips, **suggested-action** (previously discarded), + a **funding-amount bar chart** (new `parseAmount`); 3 new i18n keys × 17 (`funded.byAmount`/`score`/`action`), snapshot 1183→1186; `tests/funded-view.test.mjs` **+2** (parseAmount + card wiring) and the v1.133.1 guard updated table→cards; previously **v1.140.0**: **2361** (v1.140.0 — **Insightful stats: richer salary figures**: `RoleStats.salaryStats` now returns `avgUsd` (mean) alongside min/median/max, `#/stats` "My pipeline" salary section gains a **per-year ⇄ per-month** toggle + a **min·avg·median·max** table per country (`salaryTable`), 8 new i18n keys × 17 (`stats.period`/`perYear`/`perMonth`/`col*`), snapshot 1175→1183; `role-stats.test.mjs` **+1** (average right-skew); previously **v1.139.0**: **2360** (v1.139.0 — **Understandable: `?` help hints**: a reusable CSP-safe `?` popover (`public/js/lib/help-hint.js` → `window.HelpHint`, rendered via `UI.md()`, `role="tooltip"` + `aria-expanded`, Escape/outside-click close) wired to the **5 `#/stats` tabs** (active-tab caption) and **8 AI/analytics view titles** (`HelpHint.title`: career-plan / orientation / two-pager / networking / mock-interview / memory / funded / interview-digest); **14** new i18n keys × 17 locales (`help.hint.*` + `stats.hint.*`), assembled-dict snapshot 1161→1175; `tests/help-hint.test.mjs` **+4**); previously **v1.138.0**: **2356** (v1.138.0 — **generation-in-UI-language**: the output-language directive is threaded through all **8** AI-generation endpoints (career-plan / orientation / market / mock-interview / networking / docs-assistant / **memory-suggest** / **two-pager**) and the client sends the active `lang` on all 8 generate POSTs (+2 canaries in `memory-routes` / `two-pager-routes`); **review hardening** — a source-static CSS colour-role guard `tests/css-role-tokens.test.mjs` (+3: text-role tokens never a `background`, surface-role tokens never a text `color`, aliases resolve), a `UI.md()` XSS-loader self-probe, and a `#/career-plan` `scrollIntoView` isConnected guard; **+5**); previously **v1.137.0**: **2351** (v1.137.0 — user-reported **readability & rendering** pass (no parent-sync): dark-mode white-on-white/black-on-black fixed at the root — 15 undeclared CSS tokens (`--fg`/`--panel`/`--panel-2`/`--line`/`--surface-elev1`/`--ok`/`--go`/`--err`/`--error`/`--danger`/`--warn`/`--muted`/`--ink`/`--card`/`--border`) aliased to theme-aware tokens (0 WCAG-AA failures across 29 views), config active-tab → tinted-badge pattern; `#/stats` chart labels ellipsize + hover-title; `#/career-plan` auto-renders the plan (no raw Markdown); +2 `tests/dark-theme-tokens.test.mjs`; previously **v1.136.0**: **2343** (v1.136.0 — parent career-ops v1.26.x parity, quality wave: new source **eightfold** → registry **79 sources = 74 EN + 5 RU**, `ALL_ADAPTERS` 74 (`tests/sources-eightfold.test.mjs`); shared Unicode-key helper `text-key.mjs::normalizeTextKey` (#2569/#2587/#2667) rewiring `detect-reposts` company key + `role-matcher` title fold (`tests/text-key.test.mjs` + non-Latin cases in `detect-reposts.test.mjs`); `fetchJsonWithRetry` refused-redirect non-retryable (#2657); `title_filter.positive` ` + ` AND-groups (#2552, `compilePositiveKeyword`); oraclecloud numbered apexes (#2683); workable retry/headers/serialization hardening (#2675); personio XML→HTML fallback; states FALLBACK aliases (#2615); the 5 gate lists bumped for eightfold; +37; previously **v1.135.0**: **2306** (v1.135.0 — parent career-ops v1.26.0 parity: **5 new zero-auth scan sources** join/getro/consider/joinup/remotli → registry **78 sources = 73 EN + 5 RU**, `ALL_ADAPTERS` 73 (new suites `tests/sources-{join,joinup,getro,consider,remotli}.test.mjs`); provider fixes — a16z Speedrun transient-retry via a new shared `fetchJsonWithRetry` (+ page budget 3→6 / cap 120→1000), arbeitsagentur → v6 Jobsuche API, thehub → v2 jobsandfeatured API, hackernews → `author_whoishiring` tag lookup; the 5 gate lists (adapter-registry ids, scan-sources-endpoint EN set, `FALLBACK_SOURCES`, `SOURCE_URLS`, `http-json` retry tests) bumped; the Unicode role-dedup/company-key #2569 was NOT ported (web-ui's repost grouping already keys on plain lowercase, non-Latin-safe); +119; previously **v1.134.1**: **2187** (v1.134.1 — validation-hardening from a full-project audit: `successfactors` mid-scan-failure now keeps partials (dead-board port regression — was discarding scraped jobs + could quarantine a live tenant on a page-N 404; +1 regression test); `#/scan` filter chips made keyboard-operable (WCAG 2.1.1 — role/tabindex/aria-pressed/Enter-Space); 3 hardcoded English strings localized (`scan.trustTip` + `scan.col.reloc` new ×17, `track.col.score` reuse) with a source-static guard (+2); previously **v1.134.0**: **2184** (v1.134.0 — parent career-ops v1.25.0 parity: new scan source **getManfred** (`manfred`) → registry **73 sources = 68 EN + 5 RU**, `ALL_ADAPTERS` 68 (new suite `tests/sources-manfred.test.mjs`); provider fixes — a16z Speedrun 50-job truncation (`PER_PAGE` 100→50), the dead-board-throw contract on cryptocurrencyjobs/phenom/radancy/successfactors (a total outage now throws, not `[]`), and workable switched to the public widget API for large accounts; gate lists (adapter-registry, scan-sources-endpoint, `FALLBACK_SOURCES`, `SOURCE_URLS`) bumped for manfred; +40; previously **v1.133.1**: **2144** (v1.133.1 — bugfix for `#/funded`: the view read the parent discovery output under `res.candidates` but the parent emits `companies`, and the results table passed cells to `UI.el('tr', …)` as varargs instead of an array so only the first column rendered; `tests/parity-routes-v1133.test.mjs` fake now emits the real `companies` shape + source-static canaries (+1); previously **v1.133.0**: **2143** (v1.133.0 — two read-only parent-parity relays: funded-company discovery (`GET /api/company-funded` → `company-funded.mjs`, `#/funded`) + weekly interview digest (`GET /api/interview/weekly-digest` → `weekly-digest.mjs`, `#/interview-digest`); +5 in `tests/parity-routes-v1133.test.mjs` (success passthrough, `--from`/`--to` threading, the read-only `--dry-run` guarantee, the script-error fail-soft path) + 26 new i18n keys ×17; previously **v1.132.0**: **2138** (v1.132.0 — `#/scan` results-rendering subsystem extracted from `public/js/views/scan.js` (~1254→~906 LOC) into `public/js/lib/scan-results.js` (`window.ScanResults.create(ctx)`); **no net `node --test` delta** — source-static scan tests were repointed to `tests/helpers/scan-src.mjs::loadScanSrc()` (reads both files) and the new in-browser gate `tests/playwright-scan-filters.mjs` runs under `npm run test:e2e:browser`, outside the `node --test` count; a CodeQL housekeeping pass also removed dead `readFileSync`/`resolve`/`APP_CSS` imports from four CSS source-guard tests (closing 8 `js/unused-local-variable` alerts, test-only); previously **v1.131.2**: **2138** (v1.131.2 — `app.css` split into 3 ordered stylesheets (app/components/overlays), byte-identical cascade; CSS-asserting tests read the concatenation via `tests/helpers/css.mjs::loadAppCss()`; +3 `tests/css-modularization.test.mjs`; previously **v1.131.1**: **2135** (v1.131.1 — adapter host-pinning hardening (code-review follow-ups on the v1.130.0 sources): the `a16z-speedrun-talent` adapter re-validates the endpoint override (HTTPS + exact host via a new exported `SPEEDRUN_TALENT_HOST_RE`) and falls back to `FEED_URL` — parity with `cryptocurrencyjobs`; `cryptocurrencyjobs` `cleanUrl` tightened from `endsWith` to the exact-match `CRYPTOCURRENCYJOBS_HOST_RE` so the parser is never looser than the SSRF guard; +2 (`tests/sources-{a16z-speedrun-talent,cryptocurrencyjobs}.test.mjs`); previously **v1.131.0**: **2133** (v1.131.0 — **#/tracker CRM stage-tab board** (parent web/ `/pipeline` port): the funnel-chip bar + status `<select>` become a stage-tab strip over the full canonical funnel (All + one tab per canonical status, zero-count stages included); new read-only `GET /api/tracker/stages` returns the canonical labels + alias-fold map from `server/lib/states.mjs` (client never hardcodes the whitelist; legacy no-param `/api/tracker` still `{rows}` only) + new pure lib `public/js/lib/tracker-stages.js` (`foldStatus`/`stageCounts`), +10 (`tests/tracker-stages.test.mjs` +6, `tests/tracker-stages-endpoint.test.mjs` +4); no new i18n keys; previously **v1.130.0**: **2123** (v1.130.0 — parent career-ops v1.24.0 parity: **2 new sources** a16z-speedrun-talent + cryptocurrencyjobs (registry **72** = 67 EN + 5 RU, `ALL_ADAPTERS` 67) with a CI-isolated suite each (`tests/sources-{a16z-speedrun-talent,cryptocurrencyjobs}.test.mjs`, +30), plus the echojobs #2258 hybrid mirror (`tests/sources-echojobs.test.mjs` +7) and the radancy legacy-TalentBrew + JSON-results-fragment mirror (`tests/sources-radancy.test.mjs` +13); previously **v1.129.1**: **2073** (v1.129.1 — AI-review follow-ups on the web-ports: job-facets seniority precedence (explicit modifier beats a management word), states.mjs fallback no longer pinned + warns on malformed present file, score-tone null/blank → muted (not red), domainFromName skips non-ASCII slugs, +a states.test isolation-sanity guard (+4); previously **v1.129.0**: **2069** (v1.129.0 — wired the v1.128.0 `job-facets.js` lib into `#/scan`: a Seniority filter facet + a Seniority badge column + a zero-token Age/freshness column (`daysSince`), 12 new i18n keys ×17, +3 in `tests/scan-seniority-facet-v1129.test.mjs`; previously **v1.128.0**: **2066** (v1.128.0 — four solutions ported from the parent's own Next.js web app: `server/lib/states.mjs` reads `templates/states.yml` live for the tracker status vocabulary (`tests/states.test.mjs` +4), `public/js/lib/score-tone.js` 4-tier score tone (+4), `public/js/lib/job-facets.js` seniority/source/days facets (+8), and `company-logo.js` `domainFromName()` name→domain resolver for ATS-hosted logos (+5); no new sources, no i18n/help changes; previously **v1.127.0**: **2045** (v1.127.0 — parent career-ops v1.23.0 parity: **3 new sources** flowxtra/vdab/icims (registry **70** = 65 EN + 5 RU, `ALL_ADAPTERS` 65) with a CI-isolated suite each (`tests/sources-{flowxtra,vdab,icims}.test.mjs`, +60), Greenhouse office-city enrichment (`tests/sources-greenhouse-offices.test.mjs`, +6), the agenticjobs HTML→REST rewrite, role-matcher parity (+4 in detect-reposts), and Cursor re-added to cli-detect (10 tools) + the roster ×17; previously **v1.126.1**: **1969** (v1.126.1 — docs-drift patch: two CLI-roster spots the v1.126.0 sweep missed — the `#/config` API-keys tab intro `config.providerModelNote` (i18n ×17, gained Antigravity + Grok Build) and a stale `Cursor / Gemini CLI` help-table row ×17; i18n snapshot regenerated, no test delta; previously **v1.126.0**: **1969** (v1.126.0 — docs & CLI-roster resync with career-ops.org/docs: `cli-detect.mjs` gained Grok Build + Kimi (7→9 detected tools), the `cli-detect-routes` length assertions and `canonical-docs-coverage` CANON list widened accordingly, help ×17 + README ×17 CLI lists resynced — no net test-count change; previously **v1.125.4**: **1969** (v1.125.4 is a maintenance sync — dependabot site bumps + parent parity sweep, no test delta; v1.125.3 added **+12** locale-scaffold cases — the sweep now iterates the canonical 17-locale `I18N_LANGS` list, da/hi joined the explicit directive/role regression block, and a structural parity gate fails any `SCAFFOLD_STRINGS` key falling back to English — fixing the user-reported bug where Danish/Hindi LLM prompts answered in English); previously **v1.125.2**: **1957** (v1.125.2 merged the first external-contributor PRs — #145 headless deep-prompt fix (+1 in `tests/critical-fixes.test.mjs`) and #144 Gemini default-model bump — and added the `tests/gemini-default-model.test.mjs` drift gate (+5) pinning the default across server/dropdown/OpenRouter/hints ×17/help ×17); previously **v1.125.1**: **1950** (v1.125.1 ported the parent #2099 SuccessFactors brand-path fix + test block); previously **v1.125.0**: **1949** (v1.125.0 added tests/site-sources.test.mjs — the cvstart.org Job-sources section drift gate); previously **v1.124.0**: **1945** `node --test` cases (v1.124.0 — parent v1.22.0 parity — added 5 source suites `tests/sources-{wttj,agenticjobs,jobvite,gem,alibaba}.test.mjs` (registry 67 = 62 EN + 5 RU, `ALL_ADAPTERS` 62) + the Arbeitsagentur VOLLSTAENDIG verification and SmartRecruiters URL-fix cases; v1.123.0 — parent v1.21.0 parity — added `tests/sources-oraclecloud.test.mjs` (14 cases; registry 62 = 57 EN + 5 RU, `ALL_ADAPTERS` 57) and +2 role-matcher #1922 assertions; v1.121.0 added `tests/site-pages.test.mjs` — 6 cases pinning the cvstart.org /methodology/ /license/ /changelog/ pages ×16, the sync-assets CHANGELOG×16 + LICENSE sync, header/footer wiring, and the methodology links in help ×16 + README; v1.120.0 — parent v1.20.0 parity — added `tests/manifesto-link.test.mjs` (5 cases: SPA footer anchor contract, `footer.manifesto` ×16 dict parity, §29 in every help bundle, README section, cvstart.org footer link ×16) and bumped the help gates to **29 H2 / 105 H3**; no new routes or adapters). Previous baseline as of **v1.119.1**: **1845** `node --test` cases (unit + functional + acceptance) + Playwright/E2E surfaces (smoke + full-cycle + forms + locale-sweep ×16 + theme-toggle) + the shell-surface tier (`tests/sh-files.test.mjs` — `bin/*.sh` + `.githooks` + `install-hooks` wiring, WS9). v1.75.x added the aggregator-source suites + `tests/scan-sanitize.test.mjs` + `tests/http-json.test.mjs`. **v1.76.0** (parent career-ops v1.13.0 parity) added `tests/sources-ats-providers.test.mjs`, `tests/title-filter.test.mjs`, `tests/arbeitsagentur-remote.test.mjs`, `tests/trust-validator.test.mjs`, and rewrote `tests/scan-result-cap.test.mjs` as a "no cap" guard. **v1.77.0** added **Danish (da)** as the 13th locale (locale-sweep ×13; lang-switcher/e2e locale counts 12→13). **v1.78.0** added the Scan-page **country filter** (`public/js/lib/countries.js` + `tests/countries.test.mjs` — free-text location → ISO-country/flag detection). **v1.78.1** added `tests/ux-v1781.test.mjs` (scan auto-refresh, global-search Enter→#/scan prefill incl. same-route re-render guard, clickable logo→home with a localized `nav.logoHome` aria-label). **v1.79.0** (parent career-ops v1.14.0 parity) added `tests/weworkremotely-source.test.mjs` (board-wide RSS source, 26th adapter) and the title-filter trim-before-length guard (#1261). **v1.80.0** (ideas from job-crawler) added `tests/teamtailor-source.test.mjs` (27th adapter), `tests/scan-quarantine.test.mjs` (dead-source quarantine), `tests/scan-prefs.test.mjs` (saved searches + favorites localStorage, validation + corrupt-cache reset), plus the per-source cap + "Posted within" age filter. **v1.81.0** (parent career-ops parity) added **13 new job-board scan sources** with one CI-isolated suite each (`tests/sources-{arbeitnow,himalayas,jobicy,landingjobs,4dayweek,themuse,thehub,jobspresso,hackernews,justjoin,nofluffjobs,pinpoint,rippling}.test.mjs`) — the registry now ships **40 adapters** (35 EN + 5 RU); `ALL_ADAPTERS` count + sorted-id and `/api/scan/sources` EN-set assertions bumped to 35. **v1.82.0** (parent career-ops v1.15.0 parity) added `tests/sources-nodesk.test.mjs` (board-wide NoDesk RSS feed) — the registry now ships **41 adapters** (36 EN + 5 RU); the same three assertions bumped to 36. **v1.83.0** (parent career-ops v1.15.0 parity) added the **repost / ghost-posting detector** — `tests/detect-reposts.test.mjs` covering `server/lib/role-matcher.mjs` (fuzzy role-title match) + `server/lib/detect-reposts.mjs` (scan-history clustering); read-only `GET /api/scan/reposts`, **no new source** (registry stays **41 adapters**). **v1.84.0** (parent #1201/#1017) added the **re-apply cooldown** (`server/lib/cooldown.mjs` + `tests/cooldown.test.mjs`) and **compensation in pipeline.md** (`url | <comp>` column via `addPipelineUrl`/`parsePipeline`, + tests in `tests/parsers.test.mjs`) — scan-logic only, registry still **41 adapters**. **v1.85.0** added **German/Italian/Turkish** (`de`/`it`/`tr`) as the 14th–16th locales — full UI dict + `docs/help` + README + CHANGELOG ×16, prompt scaffolding, `tests/locales-de-it-tr.test.mjs`; locale-sweep ×13→×16, lang-switcher/e2e locale counts 13→16. **v1.86.0** added the **`#/stats` Target-role market statistics** page — `public/js/lib/role-stats.js` (client aggregator: salary→USD, fuzzy role match, per-role×country) + `server/lib/routes/stats.mjs` (16th route module; snapshot store `POST /api/stats/snapshot` → `data/role-stats.jsonl`, `GET /api/stats/trend`) + `tests/role-stats.test.mjs` + `tests/stats-routes.test.mjs`; registry still **41 adapters**. **v1.87.0** (parent career-ops v1.16.0 parity) added **4 new zero-auth providers** — `getonbrd` (board-wide JSON:API), `amazon` (amazon.jobs), `avature` + `successfactors` (per-tenant HTML ATS) — each a `sources/<slug>.mjs` (auto-discovered `meta`) + `portals/adapters/<slug>.mjs` in `ALL_ADAPTERS` + `tests/sources-<slug>.test.mjs`; the registry now ships **45 adapters** (40 EN + 5 RU), `ALL_ADAPTERS` count + sorted-id + `/api/scan/sources` EN-set assertions bumped 36→40. **v1.88.0** (issue #29 polish) closed the last Scan i18n gaps — 4 new keys (`scan.pillNew`/`scan.pillMatching`/`scan.newOffers`/`scan.relocBadge`) ×16 + `tests/scan-i18n-gaps.test.mjs` — and disabled the `X-Powered-By` header (`app.disable('x-powered-by')` + assertion in `tests/security-headers.test.mjs`). **v1.89.0** (Epic 14 — candidate market fit) added the **two-pager**: `server/lib/routes/two-pager.mjs` (17th route module — `GET`/`PUT /api/two-pager` + `POST /api/two-pager/draft`; writes the user-layer `config/two-pager.yml`), `public/js/views/two-pager.js` (guided builder + AI-fill), `public/js/lib/fit-score.js` (client `window.FitScore` — null-safe fit heuristic feeding a `◎` badge on `#/scan`), inlined into `bundleProjectContext`; suites `tests/two-pager-routes.test.mjs` + `tests/fit-score.test.mjs`, 27 new i18n keys ×16, Help §21 (+3 H3). **v1.90.0** (Epic 15 — Mock Interview 2.0) added the **mock interview**: `server/lib/routes/interview.mjs` (18th route module — turn-by-turn `POST /api/mock-interview/turn` + `save` + `GET/DELETE sessions`; writes user-layer `interview-prep/mock-*.md`), `public/js/views/mock-interview.js` (chat UI), the shared `server/lib/llm-dispatch.mjs` (`runActiveProvider`/`providerAvailable` — the extracted provider cascade), `PATHS.storyBank`, and a `bundleProjectContext({ extraFiles })` option to inline the story bank; suite `tests/interview-routes.test.mjs`, 30 new i18n keys ×16, Help §22 (+3 H3). **v1.91.0** (Epic 16 — Networking & deep company research) added `server/lib/routes/networking.mjs` (19th route module — `POST /api/networking/plan` builds a who-to-contact + intro-path + outreach-drafts + dossier plan via the shared cascade; `save`/`GET`/`DELETE plans` write the user-layer `networking/net-*.md`), `public/js/views/networking.js`, `PATHS.networkingDir`; suite `tests/networking-routes.test.mjs`, 24 new i18n keys ×16, Help §23 (+3 H3). **v1.92.0** (Epic 21 — CV Studio) added `server/lib/routes/cv-studio.mjs` (20th route module — `POST /api/cv-studio/humanize`: voice-match rewrite grounded in `voice-dna.md` + `writing-samples/`, no fabrication, no file writes), plus two pure client libs `public/js/lib/cv-diagnostics.js` (`window.CvDiagnostics.analyze` — deterministic résumé score) and `public/js/lib/cv-privacy.js` (`window.CvPrivacy.mask` — in-browser PII redaction), the `public/js/views/cv-studio.js` view, and `PATHS.voiceDna` + `PATHS.writingSamplesDir`; suites `tests/cv-diagnostics.test.mjs` + `tests/cv-studio-routes.test.mjs`, 29 new i18n keys ×16, Help §24 (+3 H3). **v1.93.0** (Epic 24 — memory layer) added `server/lib/routes/memory.mjs` (21st route module — `GET`/`PUT /api/memory` writes the user-layer `config/memory.md`; `POST /api/memory/suggest` mines the tracker into a behavioural-steering draft prompt) + `public/js/views/memory.js` + `PATHS.memory`, and inlined `config/memory.md` into `bundleProjectContext` so the note reaches **every** AI request across all providers; suite `tests/memory-routes.test.mjs`, 11 new i18n keys ×16, Help §25 (+3 H3). **v1.94.0** (Epic 25 — statistics rework) reworked `#/stats` into a three-tab **Statistics** section (AI market report + own-pipeline analytics + target-role trend), added `server/lib/routes/market.mjs` (22nd route module — `POST /api/stats/market` builds a salary/market-report prompt from CV+profile target roles, region + currency; shared provider cascade, manual fallback, no writes) + the shared `public/js/lib/report-export.js` (Markdown blob download + PDF via the inline-PDF runner + copy); suite `tests/market-routes.test.mjs`, 36 new i18n keys ×16 (+ `nav.stats`/`stats.title` reworded to "Statistics"), Help §26 (+3 H3). **v1.95.0** (Epic 26 — career plan) added `server/lib/routes/career-plan.mjs` (23rd route module — `GET`/`PUT /api/career-plan` writes the user-layer `config/career-plan.md`; `POST /api/career-plan/generate` builds a horizon-aware development-plan prompt from CV+profile+two-pager+memory via the shared cascade, manual fallback) + `public/js/views/career-plan.js` + `PATHS.careerPlan` + a **Growth** nav group; reuses `report-export.js` for MD/PDF; suite `tests/career-plan-routes.test.mjs`, 20 new i18n keys ×16, Help §27 (+3 H3). **v1.96.0** (Epic 27 — career orientation) added `server/lib/routes/orientation.mjs` (24th route module — `POST /api/orientation/generate` builds a career-orientation profile prompt (best-fit archetype vectors + roles + strengths + working-style + development recs) from CV+profile+two-pager+memory via the shared cascade; reflection-not-psychometric-test framing, manual fallback, **no writes**) + `public/js/views/orientation.js` under the **Growth** nav group; reuses `report-export.js` for MD/PDF; suite `tests/orientation-routes.test.mjs`, 7 new i18n keys ×16, Help §28 (+3 H3). **v1.97.0** (parent career-ops parity #1498 + audit sweep) added the **Dassault Systèmes** scan source (`server/lib/sources/dassault.mjs` + `server/lib/portals/adapters/dassault.mjs` — zero-token Exalead card-search XML, single-company/provider-selected, host-pinned to `www.3ds.com`); the registry now ships **46 adapters** (41 EN + 5 RU), `ALL_ADAPTERS` count + sorted-id + `/api/scan/sources` EN-set assertions bumped 40→41; suite `tests/sources-dassault.test.mjs` (10 cases). Also ported parent robustness fixes (avature two-variant markup #1541, getonbrd `published_at>0` guard, successfactors last-page cap #1528) and fixed real audit defects: `safe-fetch` over-cap hang, SSE `stream.*` activity-logging, the `#/stats` async tab race, two empty-bodied delete confirms, and untranslated uk/ru/it dictionary values. **v1.97.1** (review-driven follow-up) hardened `public/js/lib/fit-score.js` — `salaryFloor()` skips sub-annual rates (day/hour/week/month) and country matching is whole-word (`\b…\b`), +3 assertions in `tests/fit-score.test.mjs` (count unchanged) — and corrected documentation parity (every localized README at **16 locales**; in-app help §17 adapter count → **46 — 41 EN + 5 RU** ×16). **v1.98.0** (parent web-v0.2.0 parity) added the **in-app bug reporter** (`public/js/lib/logbuf.js` error ring buffer + `public/js/lib/bug-report.js` privacy-floored snapshot → pre-filled GitHub issue; `tests/bug-report.test.mjs`, 11 i18n keys ×16, no new route). **v1.99.0** added the **Portals health page** (`server/lib/routes/portals.mjs` — 25th route module, `POST /api/portals/health` SSRF-safe probes tracked `careers_url`s + `public/js/views/portals.js`; `tests/portals-routes.test.mjs` + updated `tests/router.test.mjs`, 14 i18n keys ×16) and hardened the bug reporter (network-fetch capture + bare-key scrub). **v1.100.0** added **two-pager export + live auto-fill** — the dependency-free `.docx` writer `server/lib/docx.mjs` + `server/lib/routes/export.mjs` (26th route module — `POST /api/export/docx`, stateless/bounded/no-writes), `saveDocx` + a DOCX button in the shared `report-export.js` (so market/career-plan/orientation reports gain DOCX too), and live YAML auto-fill in `POST /api/two-pager/draft {run:true}` (`parseYamlFields` → `normalizeTwoPager`); suite `tests/export-routes.test.mjs`, 4 new i18n keys ×16, Help §21 extended in place (H3 count unchanged). **v1.101.0** (CV Doctor — from a career-coaching brief, generalised) extended CV Studio with `POST /api/cv-studio/tailor` (no new route module — `buildTailorPrompt` + a generic recruiter checklist gate distilled into `TAILOR_INSTRUCTIONS`, zero hardcoded companies/roles; grounded in `bundleProjectContext`, manual fallback, no writes) + a "Tailor to a job" card in `public/js/views/cv-studio.js` reusing the `report-export.js` MD/PDF/DOCX bar; +3 assertions in `tests/cv-studio-routes.test.mjs`, 10 new i18n keys ×16, generic reference `docs/prompts/resume-cover.md`, Help §24 extended in place. **v1.102.0** (the "AI-assistant portfolio" analogy — a grounded help chat) added `server/lib/routes/docs-assistant.mjs` (27th route module — `POST /api/docs-assistant/ask`: dependency-free keyword retrieval over the app's own `docs/help/<lang>.md` `##` sections → top-N inlined → answer-from-excerpts-only via the shared cascade, manual fallback; reads **no** user data, no writes) + `public/js/views/docs-assistant.js` (a chat under the Help nav) + suite `tests/docs-assistant-routes.test.mjs` (6 cases — section split/rank, grounded prompt, manual seed, empty→400), 14 new i18n keys ×16 (`docs.*` + `nav.docsAssistant`). **v1.103.0** (parent-web parity — "works with Claude Code, Codex, …" settings panel) added `server/lib/routes/cli-detect.mjs` (28th route module — `GET /api/cli-detect`: pure PATH scan reporting which of 7 agent CLIs are installed + their paths; **never executes** a found binary — no spawn, no `--version` — fixed allowlist, no writes/LLM/network) + an "AI CLI tools" tab in `public/js/views/config.js` + suite `tests/cli-detect-routes.test.mjs` (3 — PATH resolve with a stub exec, detect shape, endpoint), 8 new i18n keys ×16 (`cli.*` + `config.tabCli`). **v1.104.0** (parent-web parity — company logos) added `server/lib/routes/logos.mjs` (29th route module — `GET /api/logo?domain=`: proxies a company's favicon from its OWN domain via a new `binary` mode on the SSRF-safe `safeGet` (returns the raw Buffer + content-type; DNS-pinning/redirect-validation/maxBytes unchanged), image-magic sniffed, size-capped + time-bounded, in-memory LRU cached, **no disk writes**, no third-party logo API) + client `public/js/lib/company-logo.js` (`window.CompanyLogo` — off by default via a localStorage flag; derives the company domain from a scan row's URL but skips shared ATS hosts → deterministic letter-avatar; CSP-safe `img.onerror` fallback) wired into the `#/scan` company cell, plus an **Appearance** card + toggle in `public/js/views/config.js`; suite `tests/logo-routes.test.mjs` (5 — domain guard, image sniff/reject, negative cache, binary safeGet), 5 new i18n keys ×16 (`appear.*`). **v1.105.0** (parent-web parity — usage/cost) added the **LLM usage & cost** page: `server/lib/llm-usage.mjs` (`recordUsage` appends `{ts,provider,in,out}` to `data/llm-usage.jsonl` — best-effort, normalized across Anthropic/OpenAI/Gemini usage shapes; `readUsage`/`aggregate` roll up per 24h/7d/30d/all window × provider) + `server/lib/llm-pricing.mjs` (editable per-provider $/1M price table → estimated USD) + `server/lib/routes/usage.mjs` (30th route module — `GET /api/usage`, read-only) + `public/js/views/usage.js` (`#/usage`, window tabs). Recording is hooked at the dispatch chokepoints — `runActiveProvider` (3 branches) + `routes/llm.mjs` (evaluate + deep, 5 sites). Suite `tests/usage-routes.test.mjs` (5 — normalize/price/record-round-trip/aggregate/endpoint; the round-trip truncates the log first, honoring PATHS-resolves-once), 17 new i18n keys ×16 (`usage.*` + `nav.usage`). **v1.106.0** (CodeQL triage — security hardening) fixed three real (if low-severity) findings and dismissed the documented FS-access false positives: `public/js/router.js` now **escapes the error message** before it reaches `innerHTML` (xss-through-exception — a server error can echo user input); `server/lib/routes/content.mjs` (`setArray`/`setDotted`) and `server/lib/routes/config.mjs` (the env-apply loop) reject `__proto__`/`constructor`/`prototype` keys (**prototype-pollution guards** — belt-and-braces, the paths come from fixed specs). Suite `tests/security-hardening-v1106.test.mjs` (3 source-pattern guards). The ~74 `js/missing-rate-limiting` / `js/http-to-file-access` / `js/file-access-to-http` / `js/file-system-race` alerts on the scanner/route FS code are dismissed as false positives (custom limiters not credited; the scanner legitimately reads/writes `data/*`). **v1.107.0** hardened `server/lib/security.mjs::stripDangerousMarkdown` (the documented at-rest XSS defense-in-depth): the tag strip now runs to a **fixed point** (repeat-until-stable, bounded 8 passes) so a removal that reforms a payload (`<scr<script></script>ipt>`) is caught, the end-tag patterns use `[^>]*>` so `</script foo>` variants are removed (CodeQL bad-tag-filter), and an **unclosed** executable opener is stripped too; +3 regression cases in `tests/cv-xss-bypasses.test.mjs`. **v1.108.0** (CodeQL triage round 2) hardened three more: `server/lib/prompts.mjs` resolves the locale role-line by OWN key + `typeof === function` (unvalidated-dynamic-method-call — a tampered `lang` cannot dispatch to a prototype method); `server/lib/routes/runners.mjs` caps the PDF slug to 200 chars BEFORE the dash-trim regex (polynomial-redos on an uncapped all-dash slug); `server/lib/cv-import.mjs` coerces an array `filename` (repeated header) to a string (type-confusion). +3 tests in `tests/security-hardening-v1108.test.mjs`. The remaining ~40 alerts are note-level lint (`unused-local-variable` etc., mostly in tests) + guarded/sanitized false positives (`remote-property-injection` behind the v1.106.0 `UNSAFE_KEY` guard, `path-injection` on the allowlist-sanitized help-locale resolver), dismissed with rationale. **v1.109.0** (parent-web layout parity) added an **Exclude** keyword field to `#/scan` (comma-separated; hides rows matching any term) and made the search box comma-OR ("roles to find"), both round-tripped through saved searches; and a compact **pipeline overview** strip on `#/pipeline` (inbox count + tracker `tracked`/`Applied`/`Responded`/`Interview`/`Offer` counts, each linking to `#/tracker`). Client-only (no new route); suite `tests/scan-pipeline-ui-v1109.test.mjs` (2), 4 new i18n keys ×16 (`scan.filterExclude`/`scan.lblExclude` + `pipe.ovInbox`/`pipe.ovTracked`). **v1.110.0** was docs-only (QA prompts + help paragraphs ported to all 16 languages; no code). **v1.111.0** (CodeQL backlog closeout) added `tests/security-hardening-v1111.test.mjs` (7) + an updated `v1108` guard: `server/lib/security.mjs` escapes any truncated dangerous-tag opener that survives the strip loop, `server/lib/cv-import.mjs` reads the verified-Buffer size via `Number()`, and `server/lib/prompts.mjs` role-lines are template strings interpolated with `String.replace` (no dynamic dispatch) — closing the last 6 open CodeQL alerts at source (5 fixed, 1 categorical FP dismissed). **v1.117.0** (parent parity pack) added the follow-up cadence board, the Rejection-patterns tab, Add-to-CV, the knock-out pre-scan, the reconcile runner, and **4 providers** (beesite/HigherEdJobs/JibeApply/softgarden — registry **50 adapters**, 45 EN + 5 RU); suites `tests/sources-parity-v1117.test.mjs`, `tests/parity-routes-v1117.test.mjs`, `tests/parity-hardening-v1171.test.mjs`. **v1.118.0** (parent career-ops v1.18.0 parity) added **9 providers** (csod/Phenom/Radancy/Deutsche Bahn/EchoJobs/TKMS/Heckler & Koch/Rheinmetall/LaraJobs — registry **59 adapters**, 54 EN + 5 RU; `ALL_ADAPTERS` + `/api/scan/sources` assertions bumped 45→54) + **Lever EU** (`jobs.eu.lever.co`), the canonical **`Hired`** tracker status (whitelist + badge + job-landed banner + funnel/conversions), and the `#/stats` **Lifetime** tab relaying the parent's `stats.mjs`/`salary-gap.mjs` (`GET /api/stats/lifetime` + `GET /api/stats/salary-gap`, read-only fail-soft); suites `tests/sources-parity-v1118{a,b,c}.test.mjs` + `tests/parity-routes-v1118.test.mjs`, 28 i18n keys ×16, Help §26 +1 H3 (**103**). **v1.119.0** (parent career-ops v1.19.0 parity) added **2 providers** (Meituan `zhaopin.meituan.com` / Tencent `careers.tencent.com` — zero-auth Chinese tech-board JSON APIs, keyword-driven pagination config via the company entry — registry **61 adapters**, 56 EN + 5 RU; `ALL_ADAPTERS` + `/api/scan/sources` assertions bumped 54→56), suites `tests/sources-meituan.test.mjs` + `tests/sources-tencent.test.mjs`, ported the parent #1813 Workday CXS browser-headers fix (+1 case in `tests/workday-fallback.test.mjs`, shared `BROWSER_LIKE_USER_AGENT` in `http-json.mjs`, Glints parity headers), and refreshed the cvstart.org landing (live client-side GitHub star counter, build-time contributors block ×16 locales, weekly scheduled Pages rebuild). **v1.119.1** synced the `#/scan` Source-dropdown offline fallback (`FALLBACK_SOURCES`) to all 61 providers and added the drift guard `tests/scan-fallback-sources.test.mjs` (exact value+label parity with the source registry). Run `npm run test:coverage` for the V8 report; see `docs/architecture/TESTING.md` for the 4-tier pyramid.
- `tests/canonical-docs-coverage.test.mjs` enforces H2 across the localized help bundles; `tests/help-ru-config-section.test.mjs` additionally locks **H3 parity** (113 per bundle) — an en-only H3 addition can no longer silently diverge the localized bundles.

## LLM provider selection (v1.39.0, WS8.2)

`LLM_PROVIDER` ∈ `auto|claude|gemini` (env-config `KNOWN_KEYS`).
`providerOrder(env)` in `server/lib/env-config.mjs` is the single
contract: `auto`→`[anthropic,gemini]` (legacy), `claude`→`[anthropic]`,
`gemini`→`[gemini]`. All 6 provider-gate sites in
`server/lib/routes/llm.mjs` (evaluate/deep/mode × Anthropic/Gemini)
consult it via the local `_provGate()` — never re-derive provider
preference elsewhere. Provider keys = exactly what Fighter90/career-ops
implements: `GEMINI_API_KEY` (parent gemini-eval), `ANTHROPIC_API_KEY`
(web-ui SDK + Claude Code), `OPENAI_API_KEY` (Codex/OpenCode CLI side).
Do NOT add speculative keys for providers the parent doesn't wire.

## CLI dispatcher (v1.38.0, WS8.1)

`bin/career-ops-ui.sh` is the unified entrypoint (`package.json`
`bin.career-ops-ui`). Verbs: `setup` (→ `bin/setup.sh`), `run`
(→ `bin/start.sh`), `doctor` (→ `scripts/doctor.mjs`), `init`
(→ `scripts/init.mjs`), `help`.

- `doctor` spins `createApp()` in-process on an ephemeral port and
  renders `/api/health` to the terminal — **single source of truth**;
  it never reimplements checks. Exit 0 iff every REQUIRED check is
  green (so `setup`/CI can gate on it).
- New verbs go in the `case` in `bin/career-ops-ui.sh` + a thin
  `scripts/<verb>.mjs`. Keep verbs standalone-usable AND chainable.

## Pre-commit AI review (v1.37.0, WS7)

`git commit` runs `.githooks/pre-commit` → `scripts/ai-precommit-review.mjs`.
`npm install` wires `core.hooksPath=.githooks` via the `prepare` script.

- **Deterministic floor — fail-HARD, always:** no `.env`/secret-bearing
  file staged; no high-confidence secret pattern in the staged diff
  (`.env.example` placeholders are exempt); no `.also(` leftover in
  staged `public/js/views/*` (mirrors the CI gate); every staged
  `.mjs`/`.js` passes `node --check`.
- **AI layer — advisory, fail-SOFT:** runs `claude -p` over the staged
  diff when the CLI is on PATH and `AI_REVIEW !== 'off'`. Missing CLI /
  offline / timeout → prints a notice, never blocks.
- `AI_REVIEW=off git commit …` skips ONLY the AI layer; the floor
  always runs. Never `--no-verify` (CLAUDE.md hard rule #7) — fix the
  cause. CI still runs the full `npm run test:ci` gate regardless.

## Accessibility (WS2 UX-audit, v1.41–v1.52)

A senior UX audit (`.planning/.../UX-AUDIT.md`, 40 findings) shipped one
fix per release v1.41→v1.52. The patterns it established are now
conventions — match them in new views:

- **SPA route focus.** `router.js` `focusNewView()` moves focus to the
  new view's `h1`/`.page-title` on every hashchange (WCAG 2.4.3). New
  views just need a single `.page-title` `<h1>`.
- **Destructive actions** go through the focus-trapped `UI.confirm(title,
  msg, {danger, confirmLabel, cancelLabel})` (`api.js`) — **never**
  native `confirm()`. It returns `Promise<boolean>`; Esc/backdrop/×/
  Cancel all resolve `false` via the `_onClose` hook; focus defaults to
  Cancel.
- **Tabs** use the full WAI-ARIA pattern: `role=tablist` container with
  `aria-label`, each tab `role=tab`+`aria-selected`+`aria-controls`+
  roving `tabindex`, panel `role=tabpanel`+`aria-labelledby`, ←/→/Home/
  End keyboard nav (see `config.js`).
- **SSE / streaming** output: `role=log aria-live=polite` for the stream,
  plus a visually-hidden `role=status aria-live=assertive` for terminal
  events; a Stop control that closes the `EventSource`; `aria-busy` on
  the trigger; a persistent `role=alert` error banner with Retry (see
  `scan.js`).
- **Every form control** has a programmatic name: explicit
  `label[htmlFor]`↔`control[id]`, or `aria-labelledby` to a visible
  heading. Tables: `<th scope="col">`, sortable headers as buttons with
  `aria-sort`. Mouse-only handlers get `role`+`tabindex`+keydown.
- **Long async relabels** (button text changing under the user) are
  announced via a polite `role=status` region.
- Every user-facing string is i18n-keyed across all 17 locales (the
  `i18n-coverage` gate enforces it); icons on peer CTAs are consistent.

## Commits

Conventional commits with types: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `perf`, `ci`. Optional scope: `feat(scan):`, `fix(api):`. Breaking: `feat!:`. Examples:

```
feat(pipeline): add server-side preview for ATS pages
fix(cv): strip <script> from cv.md ingress (FIX-C10 follow-up)
test(runner): add subprocess kill watchdog
docs(sdd): add convergence note to SDD-GUIDE
```

Subject ≤72 chars, imperative mood, no trailing period.

## Branch and PR

- Branch names: `feat/<slug>`, `fix/<slug>`, `chore/<slug>`. Never `feature/<…>` (the type prefix differs from commit prefixes by convention).
- One PR = one logical change. If you find an unrelated bug while in flight, open a separate branch.
- PR description ends with the GSD phase ID it implements (e.g. `Phase: P-3`).

## CSS

- Token-based — colors, spacing, radii defined as CSS custom properties at the top of `app.css`. Use the tokens; don't hardcode hex.
- Mobile-first media queries. The sidebar drawer logic lives at `<900 px`.
- No CSS-in-JS, no preprocessor.

## Versioning

- `package.json::version` is the source of truth.
- Bump in the PR that ships the user-visible change. Never bump in a separate commit "for release prep" — release-please handles tags.
- Footer reads `/api/health.version` — keep that read path unchanged.
