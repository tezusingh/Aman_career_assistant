# ROADMAP — career-ops-ui

> Public roadmap. Mirrors the active milestone in `.planning/ROADMAP.md` (which is GSD-managed and gitignored). Update this file when a milestone closes or scope shifts.

## Current milestone — `v2.0.x` (planning)

| # | Phase | Status | Goal |
|---|---|---|---|
| P-11 | TS/JSDoc adoption evaluation | pending | Re-evaluate the TS migration heuristic. Combined LOC trajectory + bug-class analysis from v1.7–1.9. |
| P-12 | Auth gate for LAN exposure | pending | When `HOST=0.0.0.0`, require a token-bearer header gated by `WEB_UI_AUTH_TOKEN`. Today the user is on their honor. |
| P-13 | Persistent SSE log archive | pending | Today scan logs vanish on page reload. Persist last N runs under `data/scan-logs/`. Provide a `/#/activity` filter to browse them. |
| P-14 | Public Plug-in scanners | **done (v1.69.0)** | `server/lib/sources/registry.mjs` now DYNAMICALLY auto-discovers every `server/lib/sources/*.mjs` via `readdirSync` + dynamic `import()`, collecting each adapter's `export const meta` block at boot. Users drop in an adapter file with a `meta` export — no core-file edit. Foundation laid in v1.29.0; dynamic discovery completed in v1.69.0 (PR #28). |
| P-15 | UI views split | pending | `public/js/views/scan.js` is now ~670 LOC (was 461 at last measurement) — past 400-LOC soft target. Extract Active Companies card to `views/scan/active-companies.js`; consider splitting source-filter logic. `public/css/app.css` at ~958 LOC is past the 800-LOC hard target — split candidates: paginator → `views/paginator.css`, sidebar → `views/sidebar.css`. |
| P-16 | G-005 report-block alignment | deferred | A-G → canonical A-F (`apply-for-a-job §step-8` still names "Section G"). Requires coordinated parent commit on `Fighter90/career-ops :: modes/oferta.md`. Renderer is schema-tolerant — legacy A-G files still display correctly, so this is vocabulary alignment, not a behavioural fix. |

## Completed milestones

- **`v1.60.0` — I18N-SPLIT: per-locale dictionary (May 2026).** The 8-language translation megafile (`public/js/lib/i18n-dict.js`, 849 LOC, `key → { en, es, … }`) was split into **one file per locale** under `public/js/lib/locales/` (`i18n-dict.<lang>.js`, each `window.__I18N_DICT_<LANG> = { key: string }`) plus a shared `i18n-dict.aliases.js` — the i18next / OpenWA layout, so a translator edits a single language in isolation. `i18n-dict.js` became a small **assembler** that merges them back into the identical `window.__I18N_DICT`; `t()`, every view, every call-site unchanged. All loaded via `<script src>` — no build step, no runtime fetch (both hard rules preserved). Lossless (assembled dict ≡ pre-split snapshot, 678 keys). Tooling + ~25 tests + the `ci.yml` inline check made split-aware via `tests/helpers/i18n-vm.mjs`; new `tests/i18n-locale-files.test.mjs` + `tests/playwright-locale-sweep.mjs` (every page × 8 locales in real Chromium). 994→**1000** unit · 62→**70** Playwright · coverage 95.68%. No user-facing behaviour change.
- **`v1.58.54 → v1.59.13` — UX-A polish + mobile + a11y + pre-fr i18n hygiene (May 2026)** — one-fix-per-release, all CI-green + AI-review LGTM: UX-A1–A15 (dashboard primary tile, sticky API-keys chip, reduced-motion, mobile ≤420px), the 6-cycle Help-TOC scroll-spy close (IntersectionObserver → scroll listener, behaviourally locked), Express path-traversal guard hoisted above route registration, `Cache-Control: no-store` on `GET /api/cv`, JSON-404 via `app.all`, e2e hash-route `goRoute` fix, and the I18N-CL1–CL4 dictionary cleanup (personal-data purge, `@alias` mechanism for true duplicates, `tools/i18n-audit.mjs`).
- **`v1.56.1 → v1.58.53` — FIX-PROMPT-FINAL + provider + external-QA + post-MASTER security cycle (May 2026)** — one-fix-per-release, every release CI-green + pre-commit AI-review LGTM, each regression-locked:
  - **v1.56.1–v1.56.4** brand focus-ring (a11y), per-route `document.title` (UX-N1), provider key-detection trust fix (`isUsableKey`), platform-aware ⌘K/Ctrl K hint (UX-N2).
  - **v1.57.0** **OpenRouter** as the 5th headless live-eval provider (one key → 300+ models, live `#/config` model dropdown via the CSP-safe `GET /api/openrouter/models` proxy) + the `/#/config` "validation failed" fix (trim keys, relax Anthropic gate). **v1.57.1** site-wide per-field/what-where-why API errors + PORT/HOST defaults + readable toast dwell. **v1.57.2** the real root cause — strip the SPA-injected `lang` before `validateConfig`.
  - **v1.58.0** external 32-item QA-report sweep (BUG-001 followup ISO date, BUG-003 markdown-bold in help blockquotes, BUG-004 `#/outreach` alias, BUG-005 pipeline-dup toast, BUG-006 humanized URL msg, BUG-007/008 doctor toast/title, BUG-010 reports subtitle, BUG-002/UX-032 fixture-profile heuristic, I18N-012/013 RU) + **`cleanLlmMarkdown`** (clean, formatted `#/deep` & Saved research — strips echoed `<tool_call>`/`<tool_response>`/`<thinking>` at every provider boundary + on saved-file serve) + AI-review i18n (client net-error localized; server diagnostics English-by-policy). **v1.58.1** CI-isolation fix (PATHS-once-per-process → static guard). **v1.58.2** **I18N-011 closed** — help-TOC headings localized to the sidebar `nav.*` term in all 7 non-EN locales. **v1.58.3** R-2/FIX-C1 — `cleanLlmMarkdown` now strips ORPHAN/unbalanced scaffold tags (`</tool_response>` with no opener) + Anthropic tool XML + fenced `tool_*` from `#/deep`/Saved research; FIX-C2 triaged not-reproducible (i18n.js already correct) — both regression-locked. **v1.58.4** NEW-1 (post-MASTER §5 invariant gap) — `Content-Security-Policy` is now **unconditional** on every response; previously gated behind `isPubliclyExposed()`, so `/` and `/api/health` over loopback returned no CSP and `UI.md()`'s escape-first contract was the sole XSS defence. Directive set unchanged (Google Fonts allowlist preserved for Inter; `script-src 'self'` only, no `'unsafe-inline'`/`'unsafe-eval'`). New Playwright route-walk in `tests/playwright-smoke.mjs` asserts 0 CSP violations across en/ru/ja/zh-TW × 7 routes. **v1.58.5** NEW-3 — `#/followup` Run-live double-POST triaged **not-reproducible** (single `onClick` per button, no `<form>` parent, `UI.withSpinner` already disables the button while in-flight); locked with a Playwright regression guard that runs the exact regression recipe (company/role/notes filled, date blank, single click) and asserts exactly one `POST /api/mode/followup` over a 3 s window. Cross-test localStorage pollution from the language-switcher test was handled via `page.addInitScript` seeding `career-ops-ui:lang=en`, plus a locale-stable `▶` selector. **v1.58.6** BUG-008-tb — second entry-point (top-bar `Doctor` button) now passes the BUG-008 invariant: modal title `UI.modal(I18n.t('top.doctor', 'Doctor'), …)` matches the visible button's `data-i18n="top.doctor"` declaration across all 8 locales (was hardcoded `'doctor'` regardless of UI language). Static contract guard added in `tests/qa-report-fixes.test.mjs`.
  - **v1.58.7** NEW-2 — `isValidJobUrl` now rejects paired template placeholders (`${…}`, `{{…}}`) so the rejection message ("contain no script or template characters") matches reality; ASP/EJS `<%…%>` was already covered by the `[<>"'`\\\s]` gate. New `TEMPLATE_PATTERNS`/`hasTemplatePlaceholder()` helper closes the regex↔message gap without over-blocking single-brace ATS paths. **v1.58.8** (user-requested feat) — `#/health` now surfaces `OPENAI_API_KEY`, `QWEN_API_KEY`, and `OPENROUTER_API_KEY` rows analogous to the existing `GEMINI_API_KEY`/`ANTHROPIC_API_KEY` rows, so the page reports the real state of every headless live-eval provider that `/api/status/providers` can route to (same `isUsableKey` gate). The Health view iterates over `body.checks`, so no per-locale string changes were needed. **v1.58.9** M-1 (WCAG 2.4.7 Level AA) — restored a visible `:focus-visible` ring on every form field. The base `.input/.textarea/.select { outline: none }` rule (and `.searchbar input { outline: none }`) was at higher specificity than the global `*:focus-visible` and silently nuked the keyboard-focus ring on 88 focusable elements per page; new explicit `.input:focus-visible/.textarea:focus-visible/.select:focus-visible` and `.searchbar input:focus-visible` rules paint a 2px-solid `var(--rausch)` outline plus a translucent box-shadow on keyboard focus only (mouse focus uses plain `:focus`, stays clean). **v1.58.10** M-2 — `UI.modal()` now auto-dismisses any in-flight progress toast as its first executable statement so result modals never compete with the "Running …" toast on narrow screens. Localized cv.js sync-check button label + progress toast + modal title via two new i18n keys (`cv.syncCheck`, `cv.syncCheckRunning`) across all 8 locales, also satisfying the BUG-008 invariant on that entry-point. **v1.58.11** M-4 — saved-research card title↔date spacing is now structural CSS (`.saved-card { display: inline-flex; gap: var(--space-2, 8px) }`), with a semantic `<time class="saved-card__date" datetime="…">`; pre-fix `style="margin-left: 8px"` collapsed on some entries (`software-engineer-generaltoday`). **v1.58.12** M-7 — `UI.providerCostHint()` now tracks every headless provider correctly: OpenRouter (added as 5th in v1.57.0) was falling through to a generic 0.03 fallback **and** rendering the lowercase literal `openrouter` as the name. Now EST has `openrouter: null` (router picks the model — cost varies), the render path branches on `null` to emit a localized `cost varies (router picks)` instead of a fabricated number, and NAME maps `openrouter: 'OpenRouter'`. New i18n key `cost.varies` in all 8 locales. **v1.58.13** M-8 — `#/apply` checklist is now interactive: parsed items render as real `<input type="checkbox">` rows (each in `<label>` for full-row click target, WCAG 2.5.5), state persisted per URL in `localStorage['applyChecklist:'+slug]`, with **Copy unchecked** (markdown bullets of still-open items) and **Reset** buttons. 5 new i18n keys + defensive `<pre>` fallback if the parser finds no items. **v1.58.14** M-9 — connection-banner Refresh now gives feedback: synchronous "Refreshing…" toast, button disabled to swallow double-clicks, `sessionStorage['refreshedToast']` bridge so the next page boot can emit a localized success toast (the in-flight toast is destroyed by `location.reload()`). 2 new i18n keys. **v1.58.15** I-1 — top-bar search `aria-label` + visually-hidden `<label>` now localized via a new generic `data-i18n-aria-label` hook in `applyI18n()`. Two new i18n keys (`top.search.aria`, `top.search.label`) × 8 locales; hook reusable for any future control. **v1.58.16** (user-reported) — brand-button hover-flicker: `.btn-primary` / `.btn-danger` default background is a gradient and pre-fix `:hover` swapped to a solid colour — CSS can't interpolate gradient↔solid, so the 180 ms transition snapped and produced a visible white/pink flash. Fix keeps the gradient on hover and dims via `filter: brightness(0.92)` (interpolates cleanly in every browser); `.btn` transition list extended with `filter var(--transition)` so the dim animates. **v1.58.17** I-2 — saved-research date labels now use `Intl.RelativeTimeFormat(I18n.getLang(), { numeric: 'auto' })` ("today"/"yesterday"/"N days ago" localized per locale); 7-day+ dates fall back to `Intl.DateTimeFormat(locale, { dateStyle: 'medium' })`.
  - Net: **928** `node --test` + **62** Playwright + 20/23 E2E; 8-locale CHANGELOG/README/help parity; `qa/v158-regression/{FIX-PROMPT,REGRESSION-FINAL}` + exhaustive matrices appended to the 3 perennial prompts. Sole open item: **G-005** (cross-repo, blocked on parent).
- **`v1.55.1 → v1.56.0` — consolidated UX fix-prompt (May 2026)** — the 2026-05-14→18 audit cycle's 12 UX findings + 2 v1.55.0 a11y findings, shipped one-fix-per-release, every release CI-green + pre-commit AI-review LGTM, each regression-locked by a `tests/*.test.mjs`:
  - **v1.55.1** `#/auto` stepper pre-render (F-V55-E/UX-1) · **v1.55.2** `#/cv` editor descriptive `aria-label` (F-V55-H/UX-5) · **v1.55.3** 4-provider OR onboarding banner + `GET /api/status/providers` (UX-2) · **v1.55.4** `#/auto` ETA + `#/scan` Stop prominence (UX-6) · **v1.55.5** `#/dashboard` hero-CTA + focal hint (UX-3) · **v1.55.6** `#/scan` Advanced-filters disclosure (UX-4) · **v1.55.7** `#/pipeline` >1000-row vanilla virtualization (UX-7) · **v1.55.8** `#/tracker` server-side pagination + funnel chips (UX-8) · **v1.56.0** LOW bundle: cv breadcrumb, ⚡ cost hint, help TOC autoscroll, dashboard first-paint a11y (UX-9/10/11/12).
  - **Pipeline AI-review** rebuilt: the PR-only `ai-review.yml` (zero historical runs — repo ships direct to main) now runs on **push→main** posting an advisory commit-comment review (fail-soft; needs the `ANTHROPIC_API_KEY` repo secret).
  - **`docs/` + `qa/` actualized**: API.md (+`/api/status/providers`, tracker pagination), TESTING.md totals (757→813/110), CLAUDE.md/PROJECT-CONTEXT.md, perennial `qa/{REGRESSION-FINAL,UX-AUDIT-PROMPT}.md` + new `qa/FUNCTIONALITY-CHECK.md`, stale run-dirs archived. Sole open item: **G-005** (cross-repo, blocked on the parent `modes/oferta.md` A-G→A-F commit).
- **`v1.31.0 → v1.54.0` — P-31 program (May 2026)** — a single fix-per-release loop, every release CI-green, each through the pre-commit AI-review to LGTM:
  - **WS0** (v1.31) parent-sync — batch `--model`/`--start-from` parity. **WS1** (v1.32) `#/config` field-by-field profile/config forms (merge-not-replace invariant). **WS4–WS8** (v1.33–v1.40) parent-parity audit, one-click `#/auto`, settings-decomposition, pre-commit AI-review hook, the `career-ops-ui` CLI (setup/init/doctor/run/**open** + LLM provider selector).
  - **WS2 senior UX/a11y audit** (v1.41–v1.52) — a 40-finding audit shipped one fix per release, every screen Playwright-verified: SPA route-focus (WCAG 2.4.3), focus-trapped `UI.confirm` for destructive writes, WAI-ARIA tabs, scan SSE live-regions + Stop, bound form labels, sortable-table `aria-sort`, help search/back-to-top, feedback/i18n sweep, LOW polish.
  - **WS9** (v1.53) test pyramid — shell-surface coverage (`bin/*.sh` + `.githooks`), `docs/architecture/TESTING.md`. **WS10** (v1.54) canonical re-validation — help-bundle H2/H3 parity (17/70 all 8) locked by a gate, `docs/sdd/` refreshed. **WS11** — `qa/REGRESSION-v1.54.md` (single authoritative end-to-end spec) + `qa/G-005-closure-kit.md`.
  - Net: **717** `node --test` cases + 4 E2E surfaces; 8-locale README/CHANGELOG/help parity; single open item `G-005` (cross-repo, deferred since v1.27).
- **`v1.30.0` (May 2026)** — `#/scan` results paginator replaces the v1.12 "first 200 of N" truncation. `UI.paginate({ pageSize: 200 })` consistent with `#/tracker` / `#/reports` / `#/activity`. Sort full filtered set before page-slicing so boost-to-top is stable across pages. Filter inputs reset to page 0. Stale `scan.shownTop` key removed (8 locales). 9 new tests. 567 unit + 32 Playwright.
- **`v1.29.x` (May 2026)** — Regional scanner expanded from 2 to **5 RU adapters**: Trudvsem (open-data API), GetMatch + GeekJob (HTML) added alongside hh.ru + Habr Career. New `server/lib/sources/registry.mjs` as single source of truth. Dynamic source-filter dropdown via `GET /api/scan/sources`. Help-bundle §17 "How to add a portal" (developer flow) + §5 "Configuring RU portals" (user flow) × 8 locales. **v1.58.35** (user-reported) — drawer no longer auto-opens (explicit `[hidden]` CSS override against `.notif-drawer { display: flex }` cascade); help §18 catalogues notification categories in all 8 locales. **v1.29.2 hot-fix:** multi-phase SSE contract — `done` payload carries `final: true|false`; client `EventSource` closes only on `final !== false`. Pre-v1.29.2 the RU phase was silently dropped from 🌐 Scan. 558 unit + 32 Playwright.
- **`v1.28.x` (May 2026)** — Two GitHub-issue closures: #1 AI-assistant list aligned to upstream (Claude Code · Codex · OpenCode · Qwen CLI), #2 `#/batch` exposes `--max-retries N` (1-10). **v1.28.1 hot-fix:** router strips `?query` before route-name lookup (closes 404 on `#/pipeline → ▶` and `#/config?tab=modes`); `HH_USER_AGENT` row pruned from health-check. 520 unit + 32 Playwright.
- **`v1.27.0` (May 2026)** — Sidebar `#/dashboard` dedupe (brand block now `<div>`, not duplicate `<a>`).
- **`v1.26.x` (May 2026)** — v1.26.0 test pyramid (TESTING.md + `tests/acceptance/` + 2 CI gates). **v1.26.1 hot-fix:** restored WCAG-2.5.5 `.btn` 44 px floor + `flex-shrink: 0`.
- **`v1.25.0` (May 2026)** — G-014 auto-pipeline manual-mode short-circuit (returns in ≤ 2 s without LLM key). G-012 CHANGELOG locale parity gate.
- **`v1.24.x` (May 2026)** — Help-bundle content depth refresh × 8 locales (each canonical career-ops.org URL ≥ 2× per file). **v1.24.1 hot-fix:** G-015 `#/config` crash (`c(...).also is not a function`) closed with CI gate.
- **`v1.23.0` (May 2026)** — `i18n.js` split into `i18n-dict.js` (data, 578 LOC) + `i18n.js` (logic, 86 LOC). Connection-banner cadence fix.
- **`v1.22.0` (May 2026)** — M/L/N backlog clearout: entity-aware `stripDangerousMarkdown` (M-4), score-pill redundant glyphs (M-3), Safari private-mode robustness (M-5), `Element.prototype.also` retired (N-2).
- **`v1.21.0` (May 2026)** — Security + concurrency pass: B-1 DNS-rebind-safe `safe-fetch.mjs`, H-4 consolidated `sanitizePathName`, H-5 `llmRateLimit` middleware, H-6 `withFileLock` for tracker/pipeline writes.
- **`v1.20.x` (May 2026)** — WCAG 2.5.5 / 2.5.8 / 1.3.1 / 3.3.2 per-component a11y. `/api/scan-ru/config` alias retired.
- **`v1.19.0` (May 2026)** — WCAG 1.4.3 contrast pass. `HH_USER_AGENT` removed from `#/config` UI surface.
- **`v1.18.0` (May 2026)** — F-018 / G-004: scan-en + scan-ru SSE aliases retired (consolidated `/api/stream/scan?source=…`). WCAG 2.2 AA baseline.
- **`v1.15.0` – `v1.17.0` (May 2026)** — G-006/7/8/9/11 scope: Legitimacy column, auto-pipeline endpoint, modes editor (`#/config → Modes`), canonical profile schema, sidebar dedup + `#/batch` SPA. `#/batch-prompt` legacy alias with deprecation banner (v1.17).
- **`v1.9.x` (May 2026)** — P-2 phase 2 finishes the server split: `index.mjs` 762 → 130 LOC (-83%). Twelve route modules under `lib/routes/`. P-7 Anthropic parity for `/api/evaluate` + new `/api/evaluate/test-anthropic` smoke endpoint. P-8 help-center i18n parity test now covers all 8 locales with content-floor guard. P-9 Playwright browser smoke wired into CI matrix. P-10 multi-CLI shims (`AGENTS.md` for Codex, `GEMINI.md` for Gemini CLI). **v1.9.1** added a production-readiness pass with 4 targeted bug fixes (BF-1 tracker pipe escape + GFM-compliant parser, BF-2 config write try/catch, BF-3/BF-4 prompt-size soft cap on Anthropic SDK calls) and Playwright smoke expanded 5 → 12 tests. 284 unit + 12 Playwright. See `CHANGELOG.md`.
- **`v1.8.x` (May 2026)** — Hardening + SDD bootstrap. Three high-severity fixes (A1 Anthropic file context, A2 SIGKILL escalation, A3 SSE max-runtime), four medium (B1 redirect validation, B2 key check unification, B3 AbortSignal in scanners, B4 log guard). P-2 phase 1: server split 1230 → 762 LOC. P-1: full SDD foundation (`docs/`, `.claude/`, `CLAUDE.md`, `.aiignore`). Parent career-ops v1.7.0 audit. Playwright browser smoke (5 tests). 283 unit tests. See `CHANGELOG.md`.
- **`v1.7.x` (May 2026)** — Help center, mobile sidebar drawer, real version surface, Active Companies card, Anthropic live execution, runner unit tests, E2E comprehensive harness, GitHub Packages publish, CI workflows. See `CHANGELOG.md`.
- **`v1.6.x` (Apr–May 2026)** — QA fix sprint: stored XSS in CV markdown preview (FIX-C10), CSP headers (FIX-L2), URL validation in pipeline (FIX-M3/M6), 31-defect bug fix queue, dotenv loader, security headers, JD sanitization, JD delete, health doctor unify, output PDFs.

## Backlog (not yet scheduled)

- **TS migration evaluation.** Revisit if combined LOC > 10k or if implicit-any bug count > N/quarter. For now, JSDoc + `// @ts-check` on hot paths is enough.
- **Authentication for LAN exposure.** When `HOST=0.0.0.0`, add token-bearer auth gated by an env var. Today the user is on their honor.
- **Bundler experiment.** Test esbuild-based optional bundling for `public/js/` for production deployments while keeping the dev story zero-tooling.
- **Plug-in scanners.** Allow third-party portal source modules (`server/lib/sources/*.mjs`) to be discovered automatically rather than hard-coded.
- **Persistent SSE log archive.** Today scan logs vanish on page reload. Persist last N runs under `data/scan-logs/`.

## How phases get added

1. Run `/gsd-explore` (or `/superpowers:brainstorming`) for ideation.
2. Run `/gsd-plan-phase` — produces `.planning/phases/P-NN-<slug>/PLAN.md`.
3. After the user approves the plan, the phase is added here as **pending**.
4. On execution start, status becomes **active**.
5. On completion (verify + review pass), status becomes **completed** and the phase moves into the milestone history.

See `docs/sdd/SDD-GUIDE.md` for the full pipeline.
