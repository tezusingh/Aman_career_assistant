# career-ops-ui — Quick Agent Context

> **Purpose.** Single-file orientation for AI agents and IDE assistants
> joining a session. The canonical project rules live in
> [`../CLAUDE.md`](../CLAUDE.md). This file is the compressed version
> a fresh agent should read first.
>
> **Audience.** Claude Code subagents, Cursor / Codex / Aider sessions,
> any IDE assistant that doesn't auto-load CLAUDE.md.
>
> **Repo state.** v1.213.0 (2026-08-22 — **Feature+Fix — post-1.28.0-tag parent delta.** **Added** the **MyCareersFuture** (Singapore national job bank) scan source — public search API, host-pinned (`www.mycareersfuture.gov.sg` job-detail URLs, `redirect:error`), config-driven keywords with a profile-target fallback (lazy `await import(js-yaml)`, so import-safe for the Pages enumeration); source+adapter+30-test suite wired across all five gate surfaces; registry **82** (77 EN + 5 RU, `ALL_ADAPTERS` 77). **Fixed** — Greenhouse now fetches with `content=true` and decodes each posting body to a plain-text `description` (double-decode → strip script/style → strip tags → cap 4000), activating en-scanner's content_filter (`j.description ?? j.snippet`) which was inert for Greenhouse; Ashby appends `Remote` to the location string when remote and makes `workplaceType` win over `isRemote` (an `isRemote:true`+`Hybrid` office role is no longer mislabeled), so remote roles survive a location_filter. **Deferred to a dedicated release:** the DNS-rebinding guard (parent `_ip-guard.mjs`+`_dns-cache.mjs`+`_http.mjs` AsyncLocalStorage scoping) — validates a resolved address against loopback/RFC1918/link-local/cloud-metadata before connecting; web-ui uses per-call `fetchImpl` injection + host-pinning + `safeGet`, so it needs a web-ui-specific design, not a direct port. Also not ported: verify-portals entity-decode (CLI-only). Tests **2724**. Previously v1.212.1 (2026-08-21 — **Fix — landing source-count drift.** cvstart.org showed **80** job sources and dropped the **Job Bank (Canada)** chip after v1.212.0, while `/api/scan/sources`, the `#/scan` dropdown, and help §17 all listed **81**. Root cause (CI-log-confirmed): `jobbankca.mjs` top-level-imported `js-yaml`; the deploy-pages build only `npm ci`s in `site/`, so `server/lib/sources/jobbankca.mjs` couldn’t resolve js-yaml from the (absent) repo-root node_modules, its dynamic `import()` in the registry threw, and the enumeration silently dropped it → `facts.sources` = 80 (while `facts.adapters`, a file count, still said 81 — self-inconsistent). Three fixes: (1) `jobbankca` lazy-loads js-yaml (`await import(js-yaml)` inside async `resolveProfileKeywords`, awaited at the one call site) so the module is import-safe with zero third-party deps; (2) `site/scripts/sync-assets.mjs` now hard-fails the build when `scanSources.length !== adapterFiles.length` (turns a silent drop into a loud build error, in the exact Pages environment); (3) `tests/site-sources.test.mjs` gains a static gate asserting no source top-level-imports a non-`node:`/non-relative package. Proven by hiding node_modules/js-yaml and re-enumerating → 81 with jobbankca. Source count unchanged everywhere (81/76) — help/OVERVIEW/API untouched. Tests **2687**. Previously v1.212.0 (2026-08-21 — **Feature+Fix — career-ops 1.28.0 parity.** **Added** the **Job Bank (Canada)** scan source (federal national job board, public ATOM feed, host-pinned SSRF guard, config-driven keywords with profile-target fallback) — source + adapter + 24-test suite, wired across all five gate surfaces (registry `ALL_ADAPTERS`, adapter-registry test, scan-sources-endpoint test, SPA `FALLBACK_SOURCES`, site `Sources.astro`). **Removed** the **EchoJobs** source — its public feed is now behind bot protection and returns nothing; 3 files deleted and un-wired from every one of those surfaces. Registry stays **81** (76 EN + 5 RU, `ALL_ADAPTERS` 76) because −echojobs +jobbankca nets zero, so help §17 / OVERVIEW / API adapter counts are **untouched**. **Fixed** — Consider boards return results again (anonymous CSRF handshake: a GET seeding a session cookie + `x-csrf-token` before the `POST /jobs`); multi-location Lever roles no longer hide half their locations (merge `cats.location` + `cats.allLocations`, deduped, ` · `-joined); gentler 250 ms inter-page pacing on eightfold/oraclecloud/tencent. Deferred to a future release: the anchored-keyword `word:` prefix matcher (parent `title-keywords.mjs` — opt-in scan-quality, cleanly separable). Tests **2685**. Previously v1.211.0 (2026-08-19 — **Feature+Fix — the post-1.27.0-tag parent delta I under-scoped in v1.210.0 (release-please lag: I diffed to the `career-ops-v1.27.0` TAG, not the fork's main HEAD).** Three ports: (1) **Yourator** (yourator.co, Taiwan tech/digital board) as a new scan source — `sources/yourator.mjs` + `adapters/yourator.mjs` + `tests/sources-yourator.test.mjs` (19; subagent port). Public JSON API (`/api/v4/jobs?page=N`, no auth), walks every page until `payload.hasMore` false; the EMITTED url is the employer's own ATS link (`thirdPartyUrl`, utm-stripped, **display-only — accepted from ANY https origin, NEVER fetched**), the host-pin (`YOURATOR_HOST_RE` + `assertYouratorUrl`, HTTPS-only, `redirect:'error'`) is on the API urls actually requested. Wired all 5 surfaces → registry **81** = 76 EN + 5 RU, `ALL_ADAPTERS` 76. (2) **html-entities.mjs Latin-1 letters** — the shared decoder's `NAMED_ENTITIES` gained the full à-ÿ/À-Ý set + punctuation (deg/hellip/laquo/ndash/…), and the named lookup became exact-case-first then a `CASE_INSENSITIVE_NAMES` whitelist via `Object.hasOwn`, so `&Eacute;`→É (not é) AND `&constructor;` resolves to itself (a latent prototype-pollution bug in the old plain-object `NAMED_ENTITIES[body.toLowerCase()]` lookup). This lifts EVERY shared-decoder call site (senjob/habr/beesite/csod/hackernews/phenom/tkms/jobvite/oraclecloud/gem/dassault/…) — a French board's `D&eacute;veloppeur` now decodes; +3 in `html-entities.test.mjs` (7→10); the senjob `visibleText` "named-accent-left-literal" assertion updated (now decodes). (3) **trust-validator.mjs::asciiFoldForHostname** — NFD-normalize + strip combining marks + a `NON_DECOMPOSING_LATIN` table (ø/æ/ß/ı/…) folds accented company names to their ASCII base BEFORE the `[^a-z0-9]` strip, so "Işık" (Turkish dotless ı) matches isik.com.tr and "Société Générale" matches societegenerale.com instead of being flagged for being on its own domain; +1 in `trust-validator.test.mjs`. NOT ported (already covered): the parent's senjob **C0-control fix** (web-ui's senjob used the shared C0-safe decoder from day one) and the jobvite shared-decoder swap (web-ui's jobvite already imports it). Suite **2667**. Docs ×17 (CHANGELOG/README/help §17 80→81 · 75→76 + anchor → v1.211.0), API/OVERVIEW/CONVENTIONS counts, site `Sources.astro` `SOURCE_URLS`, wiki. Also synced the **server's** parent to 1.27.0 (`git` fast-forward on `/opt/career-ops/src`, user data preserved). Deploy: resumecraft rsync the new source+adapter + registry + html-entities + trust-validator + scan-results.js, restart.) Before it v1.210.1 (2026-08-19 — **Patch — Habr Career title/company HTML-entity decode: the 6th and last source in the v1.210.0 entity-decode theme.** User-reported with live evidence: a 1.210.0 scan at 09:54Z returned `Changellenge &gt;&gt;`, `Demand Forecasting &amp; Inventory Optimization`, `ООО &quot;М-ТЕХ&quot;` — `server/lib/sources/habr.mjs` extracted title + company via regex with NO decoding at all (no import of the shared decoder). Exact symptom v1.210.0 closed on the other five: an undecoded `&` silently fails a user's own `&` `title_filter` (the job never surfaces), and company names reach the tracker/reports mangled. Fix: habr.mjs now imports the shared `decodeEntities` and applies it to `title` (in `parseHabrCards`) and `company`; +1 case in `tests/sources-title-entity-decode.test.mjs` (6 total, one HTML fixture covering `&amp;` in the title + `&quot;` in the company). Also pinned the help §17 "As of vX … registry ships N adapters" version anchor to the current version ×16 — it read `v1.119.0` in 14 locales / `v1.124.0` in zh-CN·fr while the count had moved to 80 (ja carries no anchor). Suite **2644**. Deploy: resumecraft rsync `habr.mjs` + restart. **NOTE for v1.211.0:** a `git pull` on the parent after 1.27.0 surfaced a post-tag delta I under-scoped in v1.210.0 (I diffed to the `career-ops-v1.27.0` TAG, not main HEAD — the release-please-lag trap): **yourator** (new provider, `providers/yourator.mjs` added), a **senjob C0-control fix** (`aa21e92` — the parent's private senjob decoder emitted C0 controls; web-ui's uses the shared C0-safe decoder, verify parity), and `_html-entities`/`_trust-validator`/`jobvite` changes — queued for a v1.211.0 parity release.) Before it v1.210.0 (2026-08-19 — **Feature — career-ops 1.27.0 parity: Senjob (the scanner's first African source, Senegal) + HTML-entity title decoding across five providers.** Synced the parent to 1.27.0 and scoped the 44-commit delta: the only web-ui-relevant changes were one new source + a scan-quality fix. Ported: (1) **Senjob** (senjob.com) as a new scan source — `server/lib/sources/senjob.mjs` + `portals/adapters/senjob.mjs` + `tests/sources-senjob.test.mjs` (13 cases). It's an HTML scraper (rare here) anchored on the `/jobseekers/{slug}_e_{id}.html` URL shape + the hidden ISO-date `<span style="display:none">` — `assertParsedSomething` THROWS on a listing page that still has posting links but parses to zero (a broken board must not look like a country with no jobs), host-pinned to senjob.com via `SENJOB_HOST_RE`+`assertSenjobUrl` (HTTPS-only, `redirect:'error'`, browser UA), C0-safe decode via the shared `html-entities.mjs`, sticky-row dedup. Wired across all FIVE surfaces (registry import+ALL_ADAPTERS, `adapter-registry` + `scan-sources-endpoint` sorted gate lists regenerated from the live registry, `FALLBACK_SOURCES` in scan-results.js, `SOURCE_URLS` in site Sources.astro) → registry **80** = 75 EN + 5 RU, `ALL_ADAPTERS` 75. (2) **Title HTML-entity decode** (parity with #2487/#2921) on beesite / csod / hackernews / phenom / tkms — an undecoded "R&amp;D" failed a user's own "r&d" `title_filter` positive and the posting was silently dropped (a "sales & marketing" negative never fired either); titles — and phenom locations — now `decodeEntities` before matching; hackernews's 7-form local ENTITY_MAP swapped for the shared decoder (so numeric/other entities decode too); new `tests/sources-title-entity-decode.test.mjs` (5). NOT ported (no web-ui surface): detect-reposts `validateFlags` (CLI-only — web-ui uses `detectReposts` as a library, not a CLI) and discover-ats `isDefinitiveAbsence`+httpStatus (#2883 — a CLI "definitively-absent vs inconclusive" refinement; web-ui's board discovery reports only positive matches). Queued for the next sync (post-1.27.0 on the fork): the **yourator** board. Suite **2643**. Docs ×17 (CHANGELOG/README banner+badges/help §17 count 79→80 · 74→75), API/OVERVIEW adapter count, CONVENTIONS baseline, site Sources.astro, wiki. Deploy: resumecraft rsync of the 8 server files (senjob source+adapter + 5 patched sources + registry); server picks up the new source on restart.) Before it v1.209.0 (2026-08-17 — **Docs — the in-app help now documents the Outcome button, and "Ask the docs" points to it.** QA finding: the v1.207.0 Outcome button (POST /api/outcome, #/tracker preview-then-confirm modal) shipped AFTER the v1.206 help pass, so §11 Tracker had 6 H3s and none covered recording an outcome — and the floating "Ask the docs" assistant (grounded ONLY on docs/help/<lang>.md) confidently answered "edit the Status directly" instead. Fix: added a **"Record an outcome"** H3 at the end of §11 Tracker in all 17 help bundles (walks the Outcome button: pick outcome type → Preview → Record; logs to the outcome journal, archives the submitted CV+cover letter, syncs canonical Status). The docs-assistant does whole-`##`-section keyword retrieval with a 14 KB budget (`splitSections`/`topSections` in `routes/docs-assistant.mjs`), so placing the H3 inside §11 means a "how do I record an outcome" question now grounds on §11 WITH the Outcome content (verified live: §11 is the top-ranked section and the grounding contains "### Record an outcome" + "click the **Outcome** button"). Gate bumped 118 → **119 H3** in the TWO count assertions (`help-ru-config-section.test.mjs` over 12 locales + `locales-de-it-tr.test.mjs` over de/it/tr; H2 stays 31). Docs-only, no code/behaviour change. Suite **2625**. Deploy: resumecraft.ru rsync `docs/help/*.md` (help served live — **no restart**) + cvstart.org Pages.) Before it v1.208.2 (2026-08-16 — **Patch — mobile top-bar action buttons no longer overlap the search box (v1.208.1 follow-up).** v1.208.1 fixed the VERTICAL spill (wrapped row over the page heading) but a HORIZONTAL overlap remained: the base `.topbar` is `justify-content: space-between`, so on a narrow-but-not-narrowest phone where the single row is ALMOST full — reproduced ONLY in long-label locales (RU «Открыть Scan»/«Диагностика» at ~565–640px; English labels are too short to trigger it) — flexbox keeps everything on one line and distributes the NEGATIVE free space as OVERLAP, dropping 🔔/🌙 on top of the `#global-search` pill (user-reported, screenshot). Diagnosed with a Playwright width×locale sweep (probe found `global-search`∩`notif-bell`/`theme-toggle`/`btn-doctor` at 580/590/640px in RU, clean in EN). Fix (CSS-only, ≤900px): drop the whole `.topbar-actions` onto its own full-width second row via `flex-basis: 100%` (+ `.searchbar input { min-width: 0 }` so the input can shrink) — locale-independent, removes the fragile band, and the search box stays fully readable on row one at every mobile width. Chosen over `justify-content: flex-start`, which also killed the overlap but squeezed the search box to a useless «Пои» stub (screenshotted both, A won). The two prior guards (horizontal page overflow v1.208.0, vertical topbar spill v1.208.1) both ran in English and missed it — added a dedicated guard that reproduces the exact trigger (RU locale × the 565–640px band) and asserts no two topbar controls share pixels (proven red→green: `not ok` without the fix listing the exact overlaps, `ok` with it). Suite **2621** + Playwright smoke **22**. Static-asset deploy → resumecraft.ru rsync `public/css/app.css`, no restart.) Before it v1.208.1 (2026-08-16 — **Patch — mobile top-bar no longer overlaps the page (v1.208.0 follow-up).** v1.208.0 gave `.topbar` `flex-wrap: wrap` on mobile so its action buttons (Diagnostics/Open Scan/bell/theme) drop to a 2nd row, but the BASE `.topbar { height: var(--topbar-h) }` rule (app.css:449) sits AFTER the mobile `@media` block (line 332) in source order and — same specificity — WON, so the bar kept its fixed 80px height while `flex-wrap` pushed the 2nd row out of the box, spilling it over the page header (`scrollHeight 109 > clientHeight 80`). User-reported ("съехала верстка", screenshot). Fix: base `.topbar` `height: var(--topbar-h)` → **`min-height: var(--topbar-h)`** so the bar GROWS to fit its wrapped rows (verified: box 110/166px = content, header pushed below, 0 overlap at 360/390px; desktop 1-row unchanged). The v1.208.0 Playwright guard only checked HORIZONTAL overflow (rect.right), so it missed this vertical spill — extended it to also assert `.topbar` `scrollHeight <= clientHeight`. CSS-only + test. Suite **2621**. Static-asset deploy → resumecraft.ru rsync `public/css/app.css`, no restart.) Before it v1.208.0 (2026-08-16 — **Responsive app layout — the SPA no longer overflows sideways on a phone (CSS-only + one help.js class swap; no server change).** Root cause was the classic flex/grid **`min-width: auto`** trap plus a few unwrapped wide elements, all surfaced by a QA browser run. Fixes: (1) `.topbar` `flex-wrap: wrap` + `.searchbar { min-width: 0 }` + `.topbar-actions` wrap at the ≤900px breakpoint (actions drop to a 2nd row); (2) `.table-wrap { min-width: 0; max-width: 100% }` (a wide table scrolls INSIDE the wrap); (3) `.grid-2 > *, .grid-3 > *, .qa-grid > *, .card-row > *, .help-grid > * { min-width: 0 }` + `.grid-2/.grid-3` mobile collapse → `minmax(0, 1fr)` (the grid min-content trap); (4) `.md { overflow-wrap: break-word }` + `.md table { display: block; overflow-x: auto }` (help long tokens wrap, markdown tables scroll); (5) `.help-grid` grid-template-columns moved from an INLINE style (`240px 1fr`) in `help.js` to CSS so the `@media (max-width:900px)` can stack it to `1fr` (an inline grid-template-columns beats the breakpoint); (6) `.flex, .flex-between { flex-wrap: wrap }` on mobile (config's `role=tablist` + CV's action toolbars); (7) `.page-title/.page-subtitle { overflow-wrap: anywhere }` (a long project PATH on `#/config` was a phantom overflow — internal `scrollWidth` with no rect past the edge). Verified **0 horizontal overflow at 375/414/500px across 16 routes**; a Playwright guard in `tests/playwright-smoke.mjs` asserts it at 375px on 8 routes (found the config phantom the dev-server test missed — the minimal-fixture harness shows a long mktemp path). CSS still ≤800 LOC per file (761/754/748). Suite **2621**. Static-asset deploy → resumecraft.ru rsync of `public/css/*` + `public/js/views/help.js`, NO restart (served live). NEW-2 from the QA report is now RESOLVED.) Before it v1.207.2 (2026-08-16 — **Patch — `cleanLlmMarkdown` unwraps a whole-document ```markdown fence (AI plans rendered as a code dump).** A QA browser run (E2E-BROWSER-v1.207.1) found `career-plan/generate` + `orientation/generate` occasionally returned their ENTIRE brief wrapped in a ```markdown … ``` fence (model whim), so `UI.md` rendered a monospace code block instead of a formatted plan. Fix in the shared declutter step `server/lib/llm-output.mjs::cleanLlmMarkdown`: after the scaffold-strip + `.trim()`, unwrap `^```(markdown|md)[ \t]*\n([\s\S]*?)\n```[ \t]*$` — ONLY explicit `markdown`/`md` language AND the closing fence at EOF (a real ```python / ```js / bare-``` code answer is LEFT intact; the non-greedy middle still reaches the FINAL fence so inner code blocks survive; idempotent — the unwrapped result no longer starts with a fence). Deliberately NOT added to `UI.md` (the XSS boundary — kept a separate responsibility per the `cleanLlmMarkdown≠sanitizer` doctrine). `tests/llm-output.test.mjs` +3 (11 total). Suite **2621**. Server-code change → resumecraft.ru rsynced + restarted. NEW-2 (LOW — app `.topbar-actions` + help content horizontal overflow below ~500px) triaged OUT OF SCOPE: the SPA is a desktop local-tab tool, the overflow is multi-source (not just the topbar), and no spec promises a mobile app layout — reported, not fixed.) Before it v1.207.1 (2026-08-16 — **Patch — mobile landing overflow fix + flaky-test hardening (no server/SPA app change).** `site/` Astro landing (cvstart.org): on narrow phones the hero clipped off the right edge because the install-command `<pre>` (a flex item) and the two hero grid columns (`max-w-xl`, `hero-seq-3`) carried the default `min-width:auto` and refused to shrink to the viewport. Fix: `min-w-0` on both grid items + `min-w-0 flex-1` on the `<pre>` (so the long `curl` scrolls INSIDE its own terminal box instead of forcing the page wide). Verified **0 horizontal page overflow at 360/390/414px** via a Playwright measurement (`documentElement.scrollWidth == clientWidth`); the only intentionally-wide elements left are the `<pre>` code and the `.compare-scroll` table, both in `overflow-x:auto` wrappers. `tests/playwright-smoke.mjs`: two `assert.deepEqual(consoleErrors, [])` assertions (dashboard render + auto-pipeline) flaked on a TRANSIENT resource 404 (passed on the v1.207.0 PR, failed on the identical post-merge commit → not a regression); added a `BENIGN_CONSOLE` regex (`favicon|net::ERR|Failed to load resource`) + `realConsoleErrors()` helper and applied it to both, mirroring the existing filter in `playwright-forms.mjs` — real JS/script errors still surface. Smoke 20/20 locally. Suite **2618**.) Before it v1.207.0 (2026-08-15 — **Outcome port — record an application's outcome from #/tracker**: new `server/lib/routes/outcome.mjs` (registerOutcomeRoutes, after assessments in index.mjs). `POST /api/outcome {selector,type,stage?,feedback?,note?,role?,dryRun?}` relays the parent `outcome.mjs`: `dryRun:true` → `--dry-run --json` read-only PREVIEW `{available,dryRun,num,company,role,outcomeType,canonicalState,outcomeDir}` (matches the row, writes NOTHING); real → `--json` WRITE (archives the submitted CV/cover artifacts + syncs the tracker via set-status.mjs). WRITE-SAFETY: `type` whitelisted to the 14 OUTCOME_MAP keys (bad type = 400, not a spawn); every text field control-char-rejected (charCodeAt `<0x20||0x7f`) + length-bounded BEFORE the ARRAY-ARGS spawn (runNodeScript, no shell); a HANDLED CLI failure prints `{error,code}` to stdout under --json → 400, unexpected/timeout → 422; script-absent → `{available:false}` on preview / 422 on write. Client: every #/tracker row gets an **Outcome** button → a preview-then-record modal (`openOutcomeModal` in tracker.js: 6 curated outcome options, optional note, Preview → resulting-state readout → Record; the Record button is hidden via inline `style.display='none'`, NOT the `[hidden]` attribute, to dodge the `.btn{display:inline-flex}` author-display trap; UI.el + addEventListener + textContent, CSP-safe; `UI.closeModal()` + `Router.render()` reload on success). 13 `track.outcome.*` i18n keys ×17 + snapshot regen (1376 keys). `tests/outcome-route.test.mjs` (+8) + `fake-outcome.mjs` fixture. Suite **2618**.) Before it v1.206.0 (2026-08-15 — **Docs release — the in-app help guide ×17 now documents the 5 newest features**: Setup doctor, Discover ATS boards, "still live?" check, "reuse a past CV?" hint, Skills log. +5 H3 subsections in each of the 17 `docs/help/<lang>.md` → H2 31 / H3 118, parity-gated by canonical-docs-coverage + help-ui + help-ru-config-section + locales-de-it-tr. Reference docs actualized: `docs/architecture/API.md` +5 route entries; `CLAUDE.md` and `docs/sdd/CONVENTIONS.md` 32→36 route modules; site help + `facts.json` are gitignored build artifacts regenerated by `site/scripts/sync-assets.mjs`, so they self-heal on the Pages build. CI AI-review model switched to `qwen/qwen3.8-27b` via the `OPENROUTER_REVIEW_MODEL` repo var + the `ai-review-call.sh` fallback default. No server/SPA code change. Suite **2610**.) Before it v1.205.0 (2026-08-15 — **Skills log (`#/assessments`)**: new `server/lib/routes/assessments.mjs` (registerAssessmentsRoutes, after followup in index.mjs). `GET /api/assessments` relays the parent `assessment-log.mjs` — whose **default (bare) output IS the JSON list** (no `--json` flag; `--summary` is the text mode): `{assessments:[{date,company,reportNum,platform,subject,threshold,score,staleNote}], aggregates, quality}` — fail-soft `{available:false, reason}`. `POST /api/assessments` is an EXPLICIT WRITE that shells `assessment-log.mjs add --company --platform --subject [--report --threshold --score --stale]` with fields as **array args** (spawn, no shell) so the parent owns the 8-col TSV (`data/assessments.tsv`; NOTE the CLI writes relative to its own `__dirname`, not CAREER_OPS_ROOT). WRITE-SAFETY (the TSV analog of the C7 YAML-injection): `hasControlChar()` charCodeAt-rejects any field with `code<0x20||0x7f` (TAB breaks a column, LF injects a row) → 400 BEFORE any shell-out (verified live: tab-in-company + newline-injection → 400, assessments.tsv untouched); threshold/score whitelisted 0–100; lengths bounded. `#/assessments` "Skills log" view (Analytics group, index.html) — CSP-safe form + newest-first list. i18n ×17. `tests/assessments-route.test.mjs` + `fake-assessment-log.mjs` fixture (+8). Suite **2610**. (Built by a background subagent that hit a working-tree collision with this session's own v1.204.0 branch-switches — recovered via stash/ff/pop, verified clean: both cvsync+assessment features coexist, no conflict markers, parity+snapshot green.) Before it v1.204.0 (2026-08-15 — **"Setup doctor" panel in #/config**: new read-only `GET /api/cv-sync-check` (routes/cv-sync.mjs, registerCvSyncRoutes, llmRateLimit) relays the parent's `cv-sync-check.mjs` — which prints HUMAN TEXT + an exit code and has **NO `--json`** (argv ignored; it checks its own `__dirname` parent). The route does LIGHT structured parsing of its stable lines (`=== career-ops sync check ===` banner; `ERROR: `/`WARN: ` prefixed lines under `ERRORS (N):`/`WARNINGS (N):` headers) into `{available, ok, errors[], warnings[]}` — the BANNER's presence (not the non-zero exit, which is a normal RESULT) decides success; errors/warnings capped at 100. Fail-soft `{available:false, reason:'script-not-found'}` in CI/standalone. `#/config` gains a lazy CSP-safe "Setup doctor" tab (5th tab, `tab=doctor` deep-link; `c()`+addEventListener+textContent; green all-passed line, red Blocking issues / amber Warnings lists, muted unavailable line). i18n ×17 (`cvsync.*`). NOTE: cv-sync-check was already reachable via `POST /api/run/sync-check` + a raw-`<pre>` modal on `#/cv`; this is a DISTINCT complementary structured surface (fresh namespace, existing one untouched). Verified against the REAL CLI (in-process route smoke → `{available:true, ok:true}`). `tests/cv-sync-check-route.test.mjs` (+8). Suite **2602**. Before it v1.203.0 (2026-08-15 — **"Reuse a past CV?" hint in CV Studio**: new read-only `GET /api/jds/:name/reuse` (in routes/jds.mjs, llmRateLimit) relays the parent's `jd-similarity.mjs` — which emits JSON `{decision:reuse|reuse-with-edits|regenerate, score, reason}` **by default** (no `--json` flag; passing one errors) — once per OTHER saved JD (newest-first, `MAX_REUSE_CANDIDATES=25`, `REUSE_RANK` picks the best verdict; args are array-only, path-sanitized). Fail-soft `{available:false}` (`script-not-found` / `no-prior-jds`) on CI / standalone installs. `#/cv-studio` shows a muted CSP-safe hint (`cvs.reuseHigh/reuseEdits/reuseRegen` ×17) naming the closest prior JD. Relay contract verified against the REAL CLI (Jaccard overlap: identical `reuse-with-edits`@0.54, dissimilar `regenerate`@0). `tests/jd-similarity-reuse-route.test.mjs` (+6). Suite **2594**. Before it v1.202.0 (2026-08-15 — **Discover a company's ATS board from #/portals**: new `server/lib/discover-ats.mjs` — given a bare company NAME, `deriveSlugs` yields ≤2 `SLUG_RE`(`^[a-z0-9-]+$`)-validated candidates, and for each of Greenhouse/Ashby/Lever a probe URL is built from a FIXED host (`buildProbeUrl` re-asserts `new URL().hostname === vendor.host`), fed to the scanner's own `resolveAdapter` (id re-checked so a slug can't reroute vendors) and fetched through a `makeSafeFetchImpl` wrapper over the DNS-pinned `safeGet` (GET-only, `MAX_PROBES=12` budget, cross-origin-redirect → not-resolved). A board resolves only when the adapter returns ≥1 job. Workday is deliberately excluded (POST-only CXS feed + unguessable site slug). New `routes/discover-ats.mjs`: `POST /api/portals/discover {company}` (read-only preview) + `POST /api/portals/track {name,careers_url,provider?}` (explicit WRITE — `careers_url` must be https on `KNOWN_CAREERS_HOSTS` AND `resolveAdapter`-claimed, then `withFileLock` → `insertIntoTrackedCompanies` surgical text splice preserving comments/order → `yaml.load` re-parse guard → atomic temp-then-rename, idempotent; mirrors `/api/portals/toggle`). SECURITY (added after CodeQL flagged a real write-injection): `/track` rejects any field with a control character — a newline could splice a valid-YAML extra key past the `yaml.load` re-parse guard (which only catches BROKEN yaml); `provider` is also `yamlScalar`-quoted. `#/portals` gains a CSP-safe "Discover ATS board" card (`c()` + addEventListener + textContent; results show vendor · careers_url · N roles + Add button). i18n ×17 (14 keys). `tests/discover-ats-resolver.test.mjs` + `tests/discover-ats-route.test.mjs` (+23). Suite **2588**. Before it v1.201.0 (2026-08-15 — **Localized/variant tracker headers no longer render blank**: `parseApplications` (server/lib/parsers.mjs) read only canonical English column headers, so a `data/applications.md` with Spanish (`empresa`/`puesto`/`estado`/`fecha`/`enlace`) or variant (`position`/`stage`/`link`) headers keyed rows under the wrong names → the SPA reading `.company`/`.status`/… showed blank columns. New `export const HEADER_ALIASES` (identity entries mirroring the parent's tracker-aliases.json + the localized/variant synonyms) folds at the ONE header→field boundary (`h.replace(/^#/,'num').toLowerCase().trim()` then `HEADER_ALIASES[norm] ?? norm`); unknown/already-canonical headers pass through unchanged, so an all-English tracker parses byte-identically (regression-tested). Conservative map — no risky mis-maps (no salary/recruiter guesses). `tests/tracker-header-aliases.test.mjs` (+6). Suite **2563**. Before it v1.200.0 (2026-08-15 — **"Still live?" ATS liveness check on #/tracker**: new `server/lib/liveness-core.mjs` (pure classifier) + `liveness-api.mjs` (zero-browser ATS checker) + read-only `GET /api/liveness?url=` — confirms whether a Greenhouse/Lever/Ashby/Workday/SmartRecruiters posting is still open via the ATS's own public JSON, no LLM, no Playwright. SSRF envelope: the `url` is `isValidJobUrl`-gated, then the API is reached ONLY via the DNS-pinned `safeGet` (server/lib/safe-fetch.mjs — returns `{status,text,finalUrl}`, verified against the checker's reads) with a FIXED host + `isSafeValue` charset-validated path segments; a cross-origin redirect landing → inconclusive (preserves the parent's `redirect:'error'` intent). Conservative: only 404/410 → expired (Lever's confidential-postings 404 is NON-authoritative → uncertain); 429/5xx/redirect/unparseable → uncertain (never a false expired). `#/tracker` shows a lazy CSP-safe "Still live?" button (`livenessAffordance`/`isAtsPostingUrl` in tracker.js — `c()` + addEventListener + textContent, badge-ok/bad/warn pills) only for rows whose URL is a recognizable ATS posting. i18n ×17 (`track.live*` ×6). SmartRecruiters added as a 5th provider (endpoint from the repo's own sources/smartrecruiters.mjs). `tests/liveness-core.test.mjs` (+9) + `tests/liveness-route.test.mjs` (+8). Suite **2557**. Before it v1.199.0 (2026-08-15 — **Wide tables scroll horizontally instead of clipping (scan/tracker/stats/…)**: `.table-wrap` in public/css/components.css used `overflow: hidden`, so a table wider than the viewport with `white-space: nowrap` columns was CUT OFF with no scrollbar — the reported scan-table «она вся не вмещается» bug. Switched to `overflow-x: auto` (mirrors the existing `.reports-scroll` container from v1.180.0); the rounded border is preserved; benefits all 8 `.table-wrap` tables (scan/tracker/stats/usage/dashboard/activity/mode-page). Also verified the scan advanced filters (text/exclude/remote/salary/source/country/scope/age/seniority) work end-to-end — Playwright scan-filters 6/6. `tests/table-wrap-scroll.test.mjs` (+2). Suite **2540**. Before it v1.198.0 (2026-08-15 — **HTTP retry: exponential backoff + jitter + honoured Retry-After (scanner, resilience)**: web-ui`s `fetchJsonWithRetry` (server/lib/http-json.mjs — the retry wrapper every JSON scan source uses) waited a FLAT `retryDelayMs=500` on a transient 429/5xx, re-hammering a rate-limited board at a fixed cadence. Now: new pure `computeRetryDelayMs({attempt,baseDelayMs,maxDelayMs,retryAfter}, rand?)` → exponential backoff `baseDelayMs*2**attempt` capped at `maxDelayMs` (default 8000) minus a `JITTER_MS=250` jitter, PLUS random jitter (gated on backoff>0 so a 0 base / test mode stays exactly 0 — preserves the existing retryDelayMs:0 instant-retry tests). A `Retry-After` (429/503) is honoured but CLAMPED to `maxDelayMs*4` so a hostile `Retry-After: 86400` cannot stall a sweep. `fetchJson` now attaches `.retryAfter` on a non-ok response (was fetchText-only); new exported `parseRetryAfterMs` (delta-seconds | HTTP-date | null). Permanent 4xx / refused-redirect classification unchanged (still fail-fast). `tests/http-json.test.mjs` (+9). +9 → **2536**. Also this session: variant C — `main_rule` (#16723025) repointed to `~DEFAULT_BRANCH` so main keeps force-push/deletion protection; the redundant "Copilot review for default branch" ruleset delete was blocked by the CC classifier (user finishes it). Before it v1.197.0 (2026-08-15 — **Getro auto-resolve collection_id (scanner, feature)**: a tracked Getro board no longer needs a hand-looked-up numeric `getro_collection` — give it an https `careers_url` and the id auto-resolves. New in `server/lib/sources/getro.mjs`: `httpsCareersUrl(entry)` (https-only, normalized board URL, else null), `extractCollectionId(html)` (matches `<script id="__NEXT_DATA__">` tolerant of attr order/nonce/quotes, JSON-parses → `props.pageProps.network.id`, all-digit + positive), async `resolveCollectionId(entry,{safeGetImpl,signal})` (explicit `resolveCollection` id wins with NO network → else `safeGet` the careers page: DNS-pinned, 6 MB cap, 15 s timeout, status 200 → extract; any failure → null fail-soft). `fetchGetro` is now `await resolveCollectionId(...)` and throws only when NEITHER a numeric id NOR a resolvable https careers_url is present; the resolved id is still gated by `assertGetroUrl` (host-pinned api.getro.com, so a non-digit id can never build a URL). Adapter `matches` now accepts `provider: getro` carrying an https `careers_url` even without an id (never careers_url alone without `provider: getro`); `buildEndpoint` returns the careers_url as the informational probe endpoint when there's no explicit id (must be truthy so the registry dispatches). `tests/sources-getro.test.mjs` (+8: httpsCareersUrl, extractCollectionId, resolveCollectionId explicit-wins/auto-resolve/fail-soft, fetchGetro auto-resolve E2E + explicit-skips-fetch; the old matches/buildEndpoint contract tests updated). +5 → **2527**. Docs-hygiene bundled into this release: the shipped v1.196.0 CHANGELOG/README example URL `example.com` → RFC-2606 `example.com` across all 17+17 files (a user flagged the scary-looking placeholder; functionally identical — both are rejected by hostname; the SSRF test fixtures under tests/ keep the internal `example.com` convention, invisible to users). Also in-flight PR #260 (ci): the GitHub AI-review check renamed `Claude review` → `OpenRouter review` (OpenRouter is the primary provider now). Copilot auto-review email spam traced to a repo Settings toggle (no public API; UI-only) — the "Copilot review for default branch" ruleset #20758387 carries NO copilot rule, only deletion+non_fast_forward that protect `main` (note: `main_rule` #16723025 targets an empty ref list → inert). Before it v1.196.0 (2026-08-14 — **Workday `api` host validation (security, CodeQL #443)**: `server/lib/portals/adapters/workday.mjs` used `company.api.includes('myworkdayjobs.com')` — an incomplete-URL-substring-sanitization (js/incomplete-url-substring-sanitization, HIGH). A crafted `portals.yml` `api:` like `https://example.com/?x=myworkdayjobs.com` or `https://myworkdayjobs.com.example.com/…` passed the check and `buildEndpoint` handed it back as the fetchable endpoint. New `isWorkdayApi(api)`: `new URL(api).hostname.toLowerCase()` === `myworkdayjobs.com` || `.endsWith('.myworkdayjobs.com')` (empty/unparseable → false); used by BOTH `matches()` and `buildEndpoint()`. Low real risk (api is user-owned config, and fetchWorkday would still be bounded) but a genuine hardening. `tests/workday-adapter-endpoint.test.mjs` (+1: real accepted, 4 substring/suffix/non-URL evils rejected → matches false + buildEndpoint null, bare apex accepted). +1 → **2522**. NOTE separate in-flight PR #258 (ci): the GitHub AI-review workflow now prefers a cheap OpenRouter model (`.github/scripts/ai-review-call.sh`, OPENROUTER_API_KEY secret set by the user) with Anthropic fallback. Before it v1.195.0 (2026-08-14 — **detect-reposts inverted-index (scanner, perf — IDENTICAL output)**: `detectRepostsInGroup` in `server/lib/detect-reposts.mjs` no longer does the O(N²) nested loop over `roleFuzzyMatch` for per-company title grouping. New exported `groupRowsByTitle`: pass 1 buckets rows by lowercased title (atomic — exact-title matches, O(N), no fuzzy calls); pass 2 tokenizes each DISTINCT title once + builds an inverted index over non-baseline `roleTokens`; pass 3 seeds buckets in first-appearance order and only fuzzy-matches candidates sharing a discriminating token, gated by overlap≥2 + jaccard≥0.6 (the exact necessary conditions of web-ui's `roleFuzzyMatch`, so the gate drops only pairs roleFuzzyMatch would reject — every surviving pair still decided by roleFuzzyMatch). Output is byte-identical to the old loop (same clusters, same order — seed order + row re-sort by original index), PROVEN by `tests/detect-reposts-grouping.test.mjs` (a differential test vs a reference naive nested loop across 15 curated + 200 seeded-random histories up to 40 rows). Imports `roleTokens`/`BASELINE_TOKENS` from role-matcher (web-ui is Unicode-AHEAD of parent there). +2 → **2521**. Before it v1.194.0 (2026-08-14 — **Workday single-segment careers-URL parse fix (scanner, #255)**: `server/lib/portals/adapters/workday.mjs` `buildEndpoint` mis-parsed single-segment site URLs — the old two-segment regex `…myworkdayjobs\.com(?:\/[^/]+)?(?:\/([^/?#]+))?` let the FIRST optional group swallow a lone segment (`/Search`), so `site` was undefined → defaulted to `External` → wrong CXS endpoint `/wday/cxs/<t>/External/jobs` (probe looks healthy, returns nothing). Fixed with a structural parse: `HOST_PATTERN` (case-insensitive; tenant/wdN lowercased for the case-sensitive CXS path) captures tenant/wdN/rawpath, then `pathPart.split('/').filter(Boolean).filter(!LOCALE)` and takes the FIRST segment as the site (`/Search`→Search, `/en-US/External`→External unchanged, deep link `/en-US/External/job/…`→External, `/`→External). FIRST not last (a pre-commit AI review caught that "last" would make a deep posting link resolve to the job slug). `tests/workday-adapter-endpoint.test.mjs` (+7: +deep-link +uppercase-host). NOTE the README/site test-count jumped 2510→**2519** (includes the +2 from the earlier test-only chore #254 that didn't bump the badge). Reported in #255; closed with an EN comment. Before it v1.193.0 (2026-08-14 — **"Silent after interview" tab on #/stats (stats)**: new `GET /api/stats/rejection-latency` zero-token read-only relay of rejection-latency.mjs (JSON stdout by default: `{metadata:{today,courtesyDays,interviewRows,companiesChecked,flagged,disclaimer}, flags:[{company,role,daysSinceLastInterview,lastInterviewDate,reason,...}], warnings}`; exits 0 in JSON mode; fail-soft `{available:false}`). New 8th `#/stats` tab `renderRejection`: metadata chips (courtesyDays/companiesChecked/flagged) + a flag list (company + role + `Nd silent` badge-warn + last-interview date + reason) + disclaimer + data-quality warnings, all String()-wrapped. help-hint test bumped 7→8 tabs (+`stats.hint.rejection`). +10 i18n keys ×17 (`stats.rej*`/`stats.tabRejection`/`stats.hint.rejection`). `tests/stats-rejection-latency-route.test.mjs` (+2). +2 → **2510**. Tab verified via headless screenshot on a synthetic fixture. Before it v1.192.0 (2026-08-14 — **Fact-check gate in #/cv-studio (cv-studio)**: new `POST /api/cv-studio/verify-facts` zero-token relay of verify-cv-facts.mjs. Writes the client's generated text to a throwaway `mkdtempSync` temp file (NEVER the parent), runs `verify-cv-facts.mjs <tmp> --source cv.md --source config/profile.yml --source config/two-pager.yml --json`, and — KEY — trusts the JSON verdict even though the script exits 1 on a `block` verdict (`return verdict==='block'?1:0`); fail-soft `{available:false}` only on timeout / unparseable / missing script; temp dir removed in a finally. New `#/cv-studio` "Fact-check your CV" card (#7): textarea + Verify → verdict badge (pass=badge-ok/warn=badge-warn/block=badge-bad) + chip buckets (invented metrics / unsupportedFacts as `kind: value` / forbidden / warnings), all String()-wrapped. +15 `cvs.vf*` i18n keys ×17. `tests/cv-studio-verify-facts-route.test.mjs` (+4: block-exits-1-still-available, pass, empty-400, fail-soft). +4 → **2508**. Card verified via headless screenshot on a synthetic cv.md + stub. Before it v1.191.0 (2026-08-14 — **"What to learn next" tab on #/stats (stats)**: new `GET /api/stats/upskill` zero-token read-only relay of upskill.mjs (JSON stdout by default — carries an `{ error }` field when too little data; fail-soft `{available:false}`). New 7th `#/stats` tab `renderUpskill`: metadata chips (reportsScored/lowFitReports/knownSkillCount) + a tiered gap table (Critical=badge-bad/High=badge-warn/Medium=badge-info; skill/reports/lowFit/weightedScore) + excludedAsKnown line, all String()-wrapped. help-hint test bumped 6→7 tabs (+`stats.hint.upskill`). +15 i18n keys ×17 (`stats.up*`/`stats.tabUpskill`/`stats.hint.upskill`). `tests/stats-upskill-route.test.mjs` (+3: gaps, error-passthrough, fail-soft). +3 → **2504**. Tab verified via headless screenshot on a synthetic fixture. Before it v1.190.0 (2026-08-14 — **Company history panel on #/tracker (tracker)**: new `GET /api/stats/company-history[?company=]` zero-token read-only relay of company-history.mjs (JSON stdout by default — `--company X` → single card, bare → full result; `company` is a runNodeScript array arg, length-capped, never shell-interpolated; fail-soft `{available:false}`). New `#/tracker` "Company history" card (`companyHistoryCard`/`companyHistoryEvidence` in tracker.js): a company picker from the tracked rows + Look up → responsiveness badge (silent-on-you/mixed/responded-before/no-history) + silent-application list + posting-churn badge (reposts-detected/none-detected/no-scan-data) + cluster list, all String()-wrapped. +18 i18n keys ×17 (`track.hist*`; reused `scan.col.company`, inlined `×`). `tests/stats-company-history-route.test.mjs` (+3: single-card, full-result, fail-soft). +3 → **2501**. Populated card verified via headless screenshot. **FINDING**: the parent analytics CLIs emit JSON by DEFAULT (`--summary` = text) — no parent `--json` PR needed; R6/R8 (upskill/rejection-latency) are the same clean relay. Before it v1.189.0 (2026-08-14 — **classify-tier i18n numeral fix (scanner)**: the roman-numeral level matchers in `classify-tier.mjs` used `\b[a-z]{2,}[\s-](iii|iv|v)\b` etc — the ASCII `[a-z]{2,}` prefix meant a level numeral after a NON-Latin role word ("Инженер III", "エンジニア I", "Ingénieur IV") never matched and the posting fell back to `mid`, so `skip_tiers:[senior]`/`[entry]` missed them; the prefix also misreported the LEFTMOST position. Fixed with a script-agnostic lookbehind `(?<=[\s-])(iii|iv|v)\b` (positions at the numeral itself); dropped the dead duplicate `\bsr\./i` (already covered by `\bsr\b/i`). Same bug exists in the PARENT `classify-tier.mjs` — folded into the parent --json PR. `tests/classify-tier.test.mjs` (+1: non-Latin numerals). +1 → **2498**. Before it v1.188.0 (2026-08-14 — **UI polish: lead action buttons no longer flush against the page subtitle (ui)**: the primary action / control row on `#/interview-digest`, `#/funded`, `#/portals`, `#/career-plan` and `#/orientation` changed `margin: '0 0 16px'` → `margin: '16px 0'`. `.page-subtitle` carries only a TOP margin (`margin: var(--space-2) 0 0`), so a lead row appended right after it had zero top gap and the button butted against the subtitle (user-reported on `#/interview-digest`). Excluded (verified not the same pattern): `#/stats` market controls (tab-bar separated), `#/docs-assistant` (chat-log lead, controls at bottom). Anchored regression guard `tests/lead-row-top-margin.test.mjs` (+5). +5 → **2497**. Before it v1.187.0 (2026-08-14 — **skip_tiers scan filter restored (scanner)**: new pure `server/lib/classify-tier.mjs` (`classifyTier` title→intern/entry/mid/senior via LEFTMOST-marker + associate/program guards; `buildTierFilter(skipTiers)`), wired into BOTH `en-scanner`/`ru-scanner` filter chains (reads top-level `portals.skip_tiers`, lowercased). Before this the scanners ran title/location/content/trust filters but NO tier filter, so a user`s `skip_tiers` was silently ignored. Unrecognised titles → `mid`. `tests/classify-tier.test.mjs` (+7). +7 → **2492**. Before it v1.186.0 (2026-08-14 — **Skill-gap panel in #/cv-studio (cv-studio)**: new `GET /api/jds/:name/skill-gap` zero-token relay of jd-skill-gap.mjs (`:name` sanitizePathName-cleaned + existsSync-confirmed under jds/ BEFORE it becomes a runNodeScript arg-array element — no shell interpolation, no traversal; fail-soft `{available:false}`). New CV-Studio card: a saved-JD picker + Analyze → three colour-coded buckets (named / implied / gap) from `{existing, supportedByResume, gap, lowConfidence}`. +13 i18n keys ×17. `tests/jds-skill-gap-route.test.mjs` (+4: relay, traversal-reject, 404, fail-soft). +4 → **2485**. Populated buckets verified via headless screenshot (String()/template children — no number-child NodeError this time). Before it v1.185.0 (2026-08-14 — **Funnel & velocity #/stats tab (stats)**: new `GET /api/stats/funnel` zero-token read-only relay of funnel-velocity.mjs (fail-soft `{available:false}`), + a 6th `#/stats` tab `renderFunnel` rendering calibration (response/interview rate vs benchmark bands + caveats), the in-flight waiting list, and per-hop median/p75 velocity. +18 i18n keys ×17 + snapshot; help-hint test 5→6 tabs. `tests/stats-funnel-route.test.mjs` (+2). +2 → **2481**. NB: the velocity table passed raw numbers as UI.el children → `appendChild` NodeError; caught only by a headless-Chrome screenshot of the populated tab (route test was green), fixed with String(). Before it v1.184.0 (2026-08-14 — **Dashboard quick-action grid alignment (UI)**: `.qa-grid` switched from `auto-fill` to a fixed `repeat(4, minmax(0,1fr))` (stepping 3/2/1 at breakpoints), so a 3-tile action group no longer renders wider than a 4-tile group and every section shares a flush right edge. CSS-only; verified via headless-Chrome screenshots against a synthetic fixture. `tests/dashboard-grid-align.test.mjs` (+2). +2 → **2479**. Before it v1.183.0 (2026-08-14 — **canonical URL dedup (scanner)**: new pure `server/lib/url-key.mjs::normalizeUrl` (forces https, lowercases host, drops fragment + a single trailing slash, removes a narrow tracking-param denylist [utm_/gh_src/fbclid/gclid/…], sorts the rest; non-http → '' — "no key is not a key"). Wired into both scanners' `seen` dedup (seed + check, en + ru) AND `parsers.mjs::addPipelineUrl`'s dedup. Fixes the hole where the same posting re-listed with `?utm_…`/`http↔https`/trailing-slash counted as NEW → dup scan-history row + dup pipeline line + a wasted eval. UNDER-strips on purpose (never merges two distinct postings; keeps functional ids like gh_jid). `tests/url-key.test.mjs` (+5) + `tests/parsers.test.mjs` (+1). +6 → **2477**. Before it v1.182.0 (2026-08-14 — **locale-neutral salary ranges (i18n fix, scanner)**: one-sided salary strings across six sources (`getro`, `remotli`, `manfred`, `agenticjobs`, `justjoin`, `jobicy`) used English `from`/`up to` words that leaked untranslated into non-English scan/tracker rows — now `≥`/`≤` symbols (`≥ 120000 EUR`, `≤ 90000`). Two-sided ranges (`100000–150000 USD`) were already neutral. Display only; the client salary filter parses the numbers regardless of prefix. Source-suite test expectations updated; suite **2471**. Follows an AI-review finding on the v1.181.0 Getro salary feature. Before it v1.181.0 (2026-08-14 — **Getro parity port (LOW, scanner) — parent #2640**: ported the safe half of the parent's `providers/getro.mjs` extension into `server/lib/sources/getro.mjs` — a Getro job now carries a **salary** display string (from `compensation_amount_min/max_cents` + currency, annual-only, mirroring the remotli/lever string convention the client `Skills.parseSalaryRange` re-parses), its **location** joins ALL `locations` (was only `[0]`), and `work_mode: 'remote'` is honoured as a remote signal. Pure data-mapping from the API response web-ui already fetches — no new host, no SSRF change. Deliberately NOT ported: the parent's `collection_id` auto-resolve (fetches an arbitrary `careers_url` → separate change through `safeGet`) and the raised 1500-page cap (web-ui keeps 200/4000 for browser-triggered scans). The other 11 commits in the sync have no web-ui mirror (agent-inbox, invite-match, jd-similarity/skill-gap, reserve-report-num, verify-cv-facts, classify-tier's tier-skip). `tests/sources-getro.test.mjs` (+5). +5 → **2470**. Before it v1.180.0 (2026-08-14 — **#/reports table + score rescue (MEDIUM, reports)**: the `#/reports` list became a responsive TABLE (Report · Date · Legitimacy · Score). A long "Score not detected" chip used to squeeze the title column to ~0 and the card title's `overflow-wrap:anywhere` then broke the report name one character per line ("вёрстка поехала"); now each field has its own column, the name cell wraps at word boundaries, and a new `.reports-scroll` container scrolls horizontally on narrow viewports (new i18n key `rep.colReport` ×17). Also **FIND-A**: `parseReportHeader` now rescues a body `**score** X / 5` value form when a `## Machine Summary` placeholder (`score: —` / out-of-range) had filled the parsed score slot and blocked the bold-value-form fallback (new step 4.5) — a report with a real body score no longer shows "Score not detected". Client + parser only; no route/CSP/SSRF/parent-write. `tests/reports-table.test.mjs` (+5) + `tests/report-header-locale.test.mjs` (+2). +7 → **2465**. Before it v1.179.0 (2026-08-13 — **decoder consolidation (LOW, scanner) — closes the parent-sync worklist**: 20 scraping sources each carried their own `decodeEntities`/`decodeXmlEntities` (+`fromCodePoint`) — drift-prone copies (3 could throw RangeError, fixed v1.172.0; others admitted NUL/C0 or mis-parsed `&#1a2;`). All now route through the single `server/lib/html-entities.mjs`; **−237 lines**. The 8 RSS-style sources gained `&nbsp;`; cryptocurrencyjobs double-decode preserved via a `const decodeXmlEntities = decodeEntities` alias; jobvite fully deduped; `hh` exempt (keeps `&mdash;`/`&ndash;`). New `tests/decoder-consolidation.test.mjs` guard (+2) fails if a source re-grows a local decoder. Behaviour-preserving (full suite green, no source-test change). +2 → **2458**. No route/CSP/SSRF/parent-write. **The PARENT-SYNC worklist is now fully closed (GAP #1–#5 + decoder consolidation).** Before it v1.178.0 (2026-08-13 — **parent-parity constant refresh (LOW) — PARENT-SYNC GAP #4 + #5**: (#4) `http-json.mjs::BROWSER_LIKE_USER_AGENT` Chrome 131 → **151** (WAF/bot-gate parity with parent user-agent.mjs; guarded by a `Chrome major ≥ 151` test); (#5) `states.mjs` last-resort `FALLBACK` gained the parent Turkish status aliases (#2615: değerlendirildi/başvuruldu/mülakat/teklif/reddedildi/işe alındı/…) — production already read them from the live templates/states.yml, this closes the fresh-clone/CI fallback gap. `tests/http-json.test.mjs` +1 + `tests/states.test.mjs` +1. +2 → **2456**. Two constants only; no route/CSP/SSRF/parent-write. This closes the actionable PARENT-SYNC worklist (only the optional ~20-decoder consolidation remains). Before it v1.177.0 (2026-08-13 — **csod cookie replay (MEDIUM, scanner) — parent #2769 / PARENT-SYNC GAP #1**: some Cornerstone tenants gate the search API behind session cookies set on the bootstrap page and 401 without them → 0 jobs. `sources/csod.mjs` now reads the bootstrap via a new additive `http-json.mjs::fetchResponse` (returns `{status,headers,text()}` so `getSetCookie()` survives), builds a `Cookie` header (`cookieHeaderFrom` — name=value/jar), and replays it on the search POST; same-origin only (host-pin + `redirect:'error'`), no-cookie tenants unchanged. `tests/sources-parity-v1118a.test.mjs` +1. +1 → **2454**. Before it v1.176.0 (2026-08-13 — **reports FIND-5 (MEDIUM) — score under an unlisted bold label**: two RU reports wrote the score as `**Итоговый балл:** 1.8 / 5` / `**Скор:** 1.8 / 5` — labels `REPORT_LABELS.ru` doesn't enumerate (only «Оценка»/«Балл»), so they stayed «Score not detected». Instead of growing the synonym list, `parseReportHeader` gains a `boldScoreValueForm` fallback (step 2.6): a `/5`-rubric fraction under ANY bold label — language-independent, immune to a heading (no `**`/`/5`), and rejects a date `5/5/2026` via a negative lookahead on the denominator. `tests/report-header-locale.test.mjs` +2 (the two RU labels + a date guard). +2 → **2453**. Server-parser only; no route/CSP/SSRF/parent-write. Before it v1.175.0 (2026-08-13 — **hardening (LOW) — AI-review follow-up**: v1.174.0 FIND-3 replaced the hard-coded `~55` in each site `meta.desc` with a `{adapters}` placeholder but shipped **no test** — the AI reviewer flagged it as a drift risk. Added `tests/site-meta-desc-parity.test.mjs` (+3): all 17 `site/src/i18n/*.json` carry `{adapters}`, none re-hard-codes a `~NN` count, and `Landing.astro` interpolates it into all three description metas. Also made `stripEmphasis` nullish-safe (`''` not the string `undefined`). +3 → **2451**. Before it v1.174.0 (2026-08-13 — **reports parsing + SEO fixes (HIGH) — 4-finding audit**: (FIND-1) a non-EN report whose H1 contains the score-label word (`# Оценка вакансии: <title>`) had the title mistaken for the score → `parseReportHeader` now anchors on the localized BOLD label (`**Оценка:** 1.5/5`) [new `boldLabelValue`], skips `#` heading lines, and requires the label colon-adjacent (rewritten `proseLabelValue`); (FIND-2) `stripEmphasis` removes stray `**` from the legitimacy chip; (overflow) `compactScore` collapses a score line with trailing status text to just the number + `.score-pill` gets a no-wrap/overflow cap + reports.js left column `minWidth:0`; (FIND-3) cvstart.org meta/OG/Twitter descriptions ×17 were hard-coded `~55` while the body counts the live registry — `meta.desc` now uses a `{adapters}` placeholder substituted in Landing.astro (→`~75`). `tests/report-header-locale.test.mjs` +4. +4 → **2448**. FIND-4 (wiki stale v1.171.0/2437) handled in the release fan-out. Before it v1.173.0 (2026-08-13 — **Hermes joins cli-detect (LOW, config) — parent-sync GAP #3**: the `#/config` “AI CLI tools” tab now probes for Hermes (Nous Research, binary `hermes`), the parent’s newly supported agent runtime; `server/lib/routes/cli-detect.mjs` fixed allowlist 10→11 tools, still a read-only PATH scan (never executes a binary). Test count unchanged (`tests/cli-detect-routes.test.mjs` count assertion 10→11 + a hermes-presence check). No i18n/route/CSP/SSRF/parent-write change. Suite **2444**. Before it v1.172.0 (2026-08-13 — **entity-decoder crash fix (MEDIUM, scanner) — parent #2150 parity**: `oraclecloud`/`gem`/`dassault` decoded numeric HTML entities with a bare `Number.isFinite` guard before `String.fromCodePoint`; a ref above `0x10FFFF` (e.g. `&#99999999;` from a malformed/adversarial feed) threw an uncaught `RangeError` and aborted that source’s whole parse. New shared `server/lib/html-entities.mjs` (mirrors parent `_html-entities.mjs`) bounds numeric refs to the XML 1.0 §2.2 Char set (fromCodePoint cannot throw) and matches hex/decimal separately (`&#1a2;` no longer mis-parses); the three sources import it. Broader ~20-copy consolidation tracked in `qa/PARENT-SYNC-WORKLIST-v1.26.0.md`. Server-lib only — no i18n/route/CSP/SSRF/parent-write. `tests/html-entities.test.mjs` +7. +7 → **2444**. Before it v1.171.0 (2026-08-13 — **D-4 (LOW, design-system): type-scale + z-index tokens, first step** — audit SHIP 9/D-4): sizes and stacking were literal per component. Introduced `--font-size-*` (xs 11/sm 12/md 13/base 15/lg 18/xl 22/2xl 28, base=Inter 15px) and named `--z-*` layers (topbar 5→…→skiplink 10000). EVERY z-index literal migrated to a layer (backdrop/onboarding via `calc(var(--z-…) - 1)`); values preserved → stacking byte-identical; a canary (`tests/design-tokens-scale.test.mjs`) forbids new bare z-index magic numbers. Core font sizes (11/12/13/15/18/22/28) migrated to ramp tokens (value-preserving, zero pixel change). Off-ramp one-offs (14/16/20/24…) + weight/line-height tokens remain incremental backlog. CSS-token only — no behaviour/JS/i18n/route/CSP/SSRF/parent-write; dark-mode guard green. `tests/design-tokens-scale.test.mjs` +3. +3 → **2437**. This is the last design-export item — the backlog is now fully addressed (D-4 as a first step + documented incremental follow-up). Before it v1.170.0 (2026-08-13 — **P4-ETA (LOW): generation ETA hints** — audit SHIP 9/P4-ETA): heavy AI generations showed a bare "Generating…" with no duration. A muted `.eta-hint` `⏱ ~Ns` span now sits next to the generate button on `#/career-plan` (~40s), `#/orientation` (~30s), `#/stats` market (~30s), `#/networking` (~30s), `#/two-pager` AI-fill (~20s), mirroring the `#/auto` `.auto-eta`. Two generic i18n keys carry it (`common.eta` `~{n}s` substituted per page, `common.etaTitle`); `.eta-hint` joins the existing `.auto-eta,.advisor-eta` rule. Client-only — no route/CSP/SSRF/parent-write. +2 keys ×17 (snapshot 1219→1221). `tests/generation-eta-hint.test.mjs` +2. +2 → **2434**. This closes the design-export backlog except D-4 (type-scale/z-index tokens — documented P4 backlog, intentionally not implemented). Before it v1.169.0 (2026-08-13 — **D-5 (LOW): inline PDF preview** — audit SHIP 9/D-5): `GET /api/output/pdfs/:name` forced `Content-Disposition: attachment`, so even the `#/cv` "Open" link downloaded PDFs instead of previewing. `?inline=1` (or `?disposition=inline`) now serves the SAME `sanitizePathName`-validated file with `Content-Disposition: inline` (browser renders in a new tab); the default stays attachment. `#/cv` PDF list first button → 👁 Preview (`?inline=1`), Download unchanged; `cv.openPdf` reworded Open→Preview ×17. No CSP/SSRF change (same path guard; inline only affects the disposition header on the user own application/pdf). `tests/output-pdfs.test.mjs` +3 (inline header, default-attachment, path guards under inline). +3 → **2432**. Before it v1.168.0 (2026-08-13 — **D-2 (LOW, a11y): checkbox target size** — audit SHIP 9/D-2): checkbox/radio-wrapping labels on `#/scan`/`#/config`/`#/evaluate`/`#/cv-studio` sat in a ~22px band, under the WCAG 2.5.8 24×24 floor. A scoped `label:has(> input[type=checkbox]/[type=radio]) { min-height:24px; align-items:center }` rule guarantees a ≥24px band — min-height only (the labels are already flex, nothing shifts); `.apply-checklist` (32px) was already compliant. CSS-only — no behaviour/JS/i18n/route/CSP/SSRF/parent-write. `tests/checkbox-target-size.test.mjs` +1. +1 → **2429**. Before it v1.167.0 (2026-08-13 — **FIX-9/D-3 (LOW): elevation token** — audit SHIP 9): `--panel-2`/`--surface-elev1` resolved to `--slate`, the same value as the `--line`/`--border` hairlines, so an elevated panel/chip had no separation from a bordered card. A dedicated theme-aware `--elev` token now backs the raised surfaces (`#eef1f6` light / `#1e232e` dark, distinct from `--slate` in both themes); hairlines stay on `--slate`. `--elev` is declared in the light `:root` + both dark blocks. The other design-export items are restated as tracked backlog in `docs/UX-ROADMAP.md`: D-2 (checkbox target, per-view), D-4 (type-scale/z-index tokens, P4 backlog), D-5 (inline PDF preview) + P4-ETA (generation ETA) are behaviour changes → separate future releases. CSS-token only — no behaviour/JS/i18n/route/CSP/SSRF/parent-write change; dark-mode contrast guard green. `tests/elevation-token.test.mjs` +2. +2 → **2428**. Before it v1.166.0 (2026-08-13 — **FIX-8 (LOW): docs-aligned rubric terminology** — audit SHIP 8): career-ops.org/docs says "five scoring dimensions plus a holistic global score" but the web-ui/site/wiki all said "six-dimension rubric" (5+1=6 reconciles, vocabulary did not). Adopted the docs phrasing across README ×17, cvstart.org site copy ×17 (`method.*`/`how.3`/`features.2`/`meta`), help guide ×17, `docs/career-ops-canonical.md`, wiki (Home ×17 + Features). The SPA UI never used the phrase (no i18n-dict key). Docs/marketing only — no code/route/CSP/SSRF/parent-write. `tests/rubric-terminology.test.mjs` +2 guards the EN source surfaces. +2 → **2426**. Before it v1.165.0 (2026-08-13 — **FIX-7 (LOW): consistent "Two-pager" term** — audit SHIP 7): in `ar` the sidebar showed the Latin "Two-pager" while the page `<h1>` (`twoPager.title`) was localized (`الصفحتان الخاصتان بك`) — the only Latin word in an otherwise mirrored RTL nav. Enforced decision: per locale, `nav.twoPager` and `twoPager.title` agree on the term (both Latin OR both localized); only `ar` was split → its nav is now `الصفحتان`. All 16 other locales were already consistent. `tests/two-pager-term-consistency.test.mjs` gates it. i18n-only — no route/CSP/SSRF/parent-write; 1 value changed (ar), no new keys (snapshot 1219). +2 → **2424**. Before it v1.164.0 (2026-08-13 — **FIX-6 (LOW): search placeholder fits** — audit SHIP 6): the top-bar global-search placeholder "Find a company, role or URL…" overflowed (nowrap, scrollWidth > clientWidth) when the searchbar flex-shrinks, hiding the "…or URL" affordance. `top.search` (×17) is now the short "Search or paste a URL" (≤24 chars/locale), fitting a narrow bar while keeping the URL hint; the hardcoded `index.html` placeholder fallback matches; the input `aria-label` still carries the full detail. Copy-only — no route/CSP/SSRF/parent-write; 1 key reworded ×17, no new keys (snapshot 1219). `tests/search-placeholder-fit.test.mjs` +2. +2 → **2422**. Before it v1.163.0 (2026-08-13 — **FIX-5 (LOW): help covers report → PDF** — audit SHIP 5): the in-app "Ask the docs" assistant answered that the guide did not cover exporting a report to PDF, though `#/reports/:slug` has a 📄 Generate PDF control. Added an "Export a report to PDF" H3 under §10 Reports in all 17 `docs/help/*.md` bundles (button → `generate-pdf.mjs` → `output/*.pdf`, needs Playwright, review-before-send); the docs-assistant retrieval (`splitSections`/`topSections`) now surfaces Reports for a PDF question. Docs/help-only — no code/route/CSP/SSRF/parent-write/i18n change. Help gate 31 H2 / 112 → **113 H3** (updated in `help-ru-config-section` + `locales-de-it-tr`). `tests/help-reports-pdf-section.test.mjs` +2. +2 → **2420**. Before it v1.162.0 (2026-08-13 — **FIX-4 (MEDIUM): "?" target size ≥24×24** — audit SHIP 4): the `.help-hint` "?" in every page `<h1>` was 18×18 px with `padding:0`, under the WCAG 2.5.8 24×24 minimum. The element box is now 24×24 (the measurable pointer target) while the visible ring stays 18px, drawn by a centered `.help-hint::before` (border/bg moved there so the box can grow without enlarging the glyph); hover/aria-expanded/focus-visible target `::before`; margin trimmed 6→3px so the title gap is unchanged. Page-title lines are taller than 24px → no baseline shift. CSS-only — no JS/i18n/route/CSP/SSRF/parent-write; the v1.158.0 document.title clone-strip is unaffected. `tests/help-hint-target-size.test.mjs` +2. +2 → **2418**. Before it v1.161.0 (2026-08-13 — **FIX-3 (MEDIUM): "score not detected" chip** — audit SHIP 3): after the v1.159.0 locale-aware parser, a report that still had no parseable score rendered a blank metadata area (indistinguishable from a failure) because `reports.js` used `rep.scoreNum != null && pill`. Now the score cell branches — parsed → tone pill; null → a muted `.score-muted` chip `rep.scoreUnparsed` ("Score not detected", ×17) + `rep.scoreUnparsedHint` tooltip; the card stays a `role=link` that opens the report, date still renders. Reuses the existing neutral token (no new colour). Client-only — no route/CSP/SSRF/parent-write. +2 i18n keys ×17 (snapshot 1217→1219). `tests/reports-unparsed-chip.test.mjs` +3. +3 → **2416**. Before it v1.160.0 (2026-08-13 — **FIX-2 (HIGH): honest provider copy** — audit SHIP 2): `#/config` said the live eval "uses your Anthropic or Gemini API key" and that OpenAI is "not used by the web UI itself", and `#/dashboard`'s Evaluate card said "Anthropic-first scoring" — all false since the 7-provider cascade (v1.157.0), self-contradicted by the `Active: OpenRouter` chip. `config.providerModelNote` (×17) now names all seven providers (Anthropic · Gemini · OpenAI · Qwen · OpenRouter · GitHub Models · Hermes) with auto-order + fallback and drops the false OpenAI sentence; `dash.quick.evaluateSub` (×17) + its `dashboard.js` fallback → vendor-neutral "0–5 fit scoring"; `config.js` `Keys: N/5` → `N/7` (7 `SECRET_KEYS`). Copy-only — no route/CSP/SSRF/parent-write; 2 keys reworded ×17, no new keys (snapshot 1217). `tests/provider-copy-honesty.test.mjs` +3. +3 → **2413**. Before it v1.159.0 (2026-08-13 — **FIX-1 (HIGH): locale-aware report metadata** — audit `FIX-PROMPT-post-v1.158.0.md` SHIP 1): non-English reports rendered a blank metadata strip on `#/reports` (no score pill/date/legitimacy) because `parseReportHeader` matched only English `**Score:**`-style bold labels. Now it resolves by precedence — English bold labels (EN byte-identical) → the language-invariant `## Machine Summary` YAML block (`score:`/`legitimacy:`/`date:`, the same source `auto-pipeline` reads) → locale-aware prose labels (`REPORT_LABELS`, 17 locales). `scoreStringToNum` tolerates `1.5/5`, `1,5/5`, `1.5 из 5`, `4.5 out of 5`; `date` falls back to file mtime (threaded from `store.mjs::safeListReports` + `routes/reports.mjs`). Read/parse only — no route/CSP/SSRF/parent-write change, no i18n key. `tests/report-header-locale.test.mjs` +8 + a `GET /api/reports` RU-fixture integration case. +8 → **2410**. Before it v1.158.0 (2026-08-12 — **Cosmetic display fixes** — user-reported NITs): (1) the HelpHint `?` affordance leaked into `document.title` — `router.js::focusNewView` now deep-clones the view `<h1>`, strips `.help-hint` from the clone, then reads `textContent`, so the tab reads "Vacancy search" not "Vacancy search?" (the live heading's visible `?` is untouched); (2) the cvstart.org landing showed "17 AI providers" — `Features.astro`'s `sub()` helper greedily rewrote every `{n}` to `facts.locales` (17) before the per-card override, so `{n}` is now resolved per-card (providers → `facts.providers` = 7, languages → 17). Display-only; no server/route/CSP/SSRF/i18n-key change; `facts.json` shape unchanged. `tests/document-title-per-route.test.mjs` +1 (source-static guard for the `.help-hint`-stripped clone). +1 → **2402**; Playwright 62/62. Before it v1.157.0 (2026-08-12 — **Fix: live evals run on ANY configured provider, not just Anthropic/Gemini** — user-reported): a user with only `OPENROUTER_API_KEY` was forced into manual mode ("set ANTHROPIC/GEMINI…"). **Root cause:** `init` with Claude Code writes `LLM_PROVIDER=claude`; if the user later adds only a non-Anthropic key, the forced-claude routing found no key and dead-ended at manual — even though OpenRouter was configured + supported. **Fix:** a forced provider whose key isn't set now **falls back to the auto order among the configured providers** (a pin WITH its key stays forced), applied in `env-config.mjs::selectActiveProvider` **and both** dispatch cascades (`routes/llm.mjs::_provGate` + `llm-dispatch.mjs::gate` — each gains a `_hasKeyFor` check), so `/api/status/providers` and the actual run always agree. **Client:** `#/deep` + the mode-page views (`#/contacto`/`#/interview-prep`/`#/project`/…) gated "Run live vs manual" on a stale `/api/health` `ANTHROPIC/GEMINI_API_KEY` probe → now use the new `public/js/lib/provider-status.js` (`window.ProviderStatus.live()` → `/api/status/providers`, all 7 + label map); `#/dashboard` shows one honest **Live evals · ready/manual** badge (from all 7 provider rows) instead of Anthropic/Gemini-only. `deep.tipManual`/`deep.needKey`/`eval.manualMode` reworded ×17 + 3 new `dash.system.*` keys + `config.llmProviderHint` (pin-fallback wording) ×17 (snapshot 1214→1217). No route/CSP/SSRF change — the fallback only picks among keys the operator already set. `tests/live-provider-gating.test.mjs` +4 + a `selectActiveProvider` fallback case (the old "pin no key → null" updated). +5 → **2401**; Playwright 62/62. Before it v1.156.0 (2026-08-12 — **Refactor: split `scan.js` under the 800-line limit (P-16)** + a CodeQL fix): `public/js/views/scan.js` was **906** lines (over the hard limit). Extracted two behavior-preserving factories → **648**: `public/js/views/scan/runner.js` (`window.createScanRunner(ctx)` → `{ runScanAll, stopScan }` — the scan-execution engine: run-state/Scan-Stop, progress bar, error banner + Retry, SSE `streamTo`, per-source runners; `activeES`/`lastRunFn` runner-internal) and `public/js/views/scan/filters.js` (`window.createScanFilters(refs, deps)` → `{ applyFilters, resetFilters, getFilterState, setFilterState }`). The live-poll teardown timers (`__activeScanPollHandle`/`__activeScanDoneTimeout`/`__cancelActiveScanPoll` + the `hashchange` listener) stay at `scan.js` top level — both files are classic `<script>` tags sharing the global lexical scope, so the runner references them directly (same pattern as `lib/scan-results.js`). Both new files load before `scan.js` in `index.html`. The `loadScanSrc()` helper now concatenates scan.js + the 2 new files + scan-results.js, auto-repointing the helper-based tests; `scan-sse-a11y`/`auto-eta-stop`/`ux-v1781` switched their `SCAN` const to it. **Both oversized views now under 800 — P-15 (config.js 783) + P-16 (scan.js 648) complete.** Also fixed CodeQL `js/useless-assignment-to-local` (#428) in `config/tab-controller.js` (`let n = i;` → `let n;`). Pure refactor: no route/server/i18n/CSS change; count unchanged **2396** (repointed, not added); Playwright scan+smoke 26/26 in-browser. Before it v1.155.0 (2026-08-12 — **Refactor: split `config.js` under the 800-line limit (P-15)**: `public/js/views/config.js` was **1030** lines (over the hard limit). Extracted two behavior-preserving modules → **783**: `public/js/views/config/field-specs.js` (`window.ConfigFieldSpecs` — the curated per-provider model lists + the read-only `FIELDS` descriptor table) and `public/js/views/config/tab-controller.js` (`window.createConfigTabController(c, panelHost)` → `{ tabBtn, activate }`, the ARIA tablist + keyboard-nav). Both loaded via `<script src>` before `config.js` in `index.html`. Six source-reading tests repointed to the new files (config-tabs-aria, provider-selector, openrouter/openai-model-selector, gemini-default-model, config-validation-detail) — assertions otherwise identical. **`scan.js` (906) intentionally NOT force-split** — its results-rendering was already extracted to `lib/scan-results.js` (v1.132.0); the remaining core is an event-wiring closure bound to ~18 filter/DOM refs where a factory extraction would need an 18-arg signature and worsen coupling (a proper split needs a filter-state-model refactor, deferred). Pure refactor: no route/server/i18n/CSS change; count unchanged **2396** (tests repointed, not added). Before it v1.154.0 (2026-08-12 — **New guide "Running the whole stack in the cloud"** (docs-only): career-ops has no cloud/server story of its own, so this adds one — in-app **Help §31** ×17 covering the parent `career-ops` pipeline + this `career-ops-ui` viewer + the AI **engine** (a **Claude subscription** via the Claude Code CLI, a local **Hermes** gateway, or provider API keys) on a small always-on server: the three moving parts, provision + install (VPS, Node ≥ 18), pick-your-engine (mixable), and expose-it-safely (HTTPS reverse proxy + auth + the CSP/SSRF/XSS/no-secrets invariants that survive the move off `127.0.0.1`). Grounded in `docs/integrations/HERMES.md` §2. Help bundle grows to **31 H2 / 112 H3** (was 30/108) — the four gate tests (`canonical-docs-coverage`, `help-ui`, `help-ru-config-section`, `locales-de-it-tr`) move to that contract. Also a README `## Run the whole stack in the cloud` section ×17 (+ the release-badge drift fixed v1.137→v1.154 across all 17) and a wiki Cloud-Deployment page. No route/server/client change, no new i18n key. Count unchanged **2396**. Before it v1.153.0 (2026-08-12 — **Jobvite scanner migrated to the public XML feed (parent-sync)**: the parent retired Jobvite's JSON API — `https://jobs.jobvite.com/api/company/{slug}/jobs` now 302s to zero jobs — and web-ui's `sources/jobvite.mjs` used that same dead endpoint, so any tracked Jobvite company silently scanned empty. Ported parent `#2623`: the source now reads the public per-tenant XML feed `https://app.jobvite.com/CompanyJobs/Xml.aspx?c={companyEId}` (indexOf-cursor XML parse — dodges CodeQL polynomial-redos — CDATA + entity decode, `detail-url` over `apply-url`, `http:`→`https:` on display-only per-job URLs). `companyEId` resolves from `company_eid:` config → `c=` param of an explicit `api:` → board-page discovery (scrape `companyEId`). **Security:** `assertJobviteUrl` pins **two** hosts (`jobs.jobvite.com` discovery + `app.jobvite.com` feed), https-only, strict allowlist, **no redirect followed** (`redirect:'error'` discovery / `redirect:'manual'` feed — 3xx read for empty-board classification, never chased); eId is only ever a `?c=` value, URLs rebuilt from resolved slug/eId. `http-json.mjs::fetchText` gained read-only `.location`/`.retryAfter` on non-ok (backward-compatible). Adapter `matches`/`buildEndpoint` rewritten; en-scanner already threads `company` so `company_eid` reaches both for free. Registry source count unchanged (`meta.value='jobvite'`). **CI:** `deploy-pages.yml` concurrency → `cancel-in-progress:false` (a superseded Pages build was reding the status badge). Parent-sync: the only web-ui-relevant change in the 17 parent commits since 1.26.0. +4 → **2396**. Before it v1.152.0 (2026-08-12 — **Hermes provider completion + docs actualization**: a detailed code review of v1.151.0 surfaced two real gaps + four completeness items, all fixed here. The `#/config` `LLM_PROVIDER` dropdown gains `hermes` as the 8th option (was unforceable from the UI) guarded by a new dropdown-vs-`LLM_PROVIDERS` parity test; `hasHermesKey` relaxes `isUsableKey`'s 20-char floor to 8 via a new `minLen` param (a self-hosted `API_SERVER_KEY` may be short — the Hermes docs' own `change-me-local-dev` is 19 chars); `hermesChatUrl` completes a bare host (`http://127.0.0.1:8642` → `…/v1/chat/completions`); `routes/llm.mjs` manual-fallback copy names Hermes; `health.mjs` gains a `HERMES_API_KEY` row (was omitted in v1.151.0). The six-provider chain/force-list strings were normalized to the full seven (Anthropic → Gemini → OpenAI → Qwen → OpenRouter → GitHub Models → Hermes) across README ×17, help ×17, `config.llmProviderHint` ×17, and `docs/sdd`; CONVENTIONS "all 16 non-EN locales"; snapshot regenerated (1214). No new route / SSRF / CSP change. +2 → **2392** (the `isUsableKey` minLen guard + the dropdown-parity guard). Before it v1.151.0 (2026-08-12 — **Hermes wired as an LLM provider (roadmap Phase 5, Shape A)**: the scoping spike (WebFetch of the Hermes docs) confirmed `hermes gateway` exposes an **OpenAI-compatible API Server** (`POST /v1/chat/completions` at `http://127.0.0.1:8642/v1`, Bearer `API_SERVER_KEY`, streaming, `GET /v1/models`) — so Shape A, not the agent-runtime Shape B. Wiring mirrors the Qwen/GitHub-Models pattern: `runHermes` on the shared `runOpenAICompatible` (`server/lib/openai.mjs`, with `hermesChatUrl` normalizing a `/v1` base), gated in **both** cascades (`llm-dispatch.mjs` + `routes/llm.mjs`), auto provider-order tail + `LLM_PROVIDER=hermes` pin (`env-config.mjs`: KNOWN_KEYS/LLM_PROVIDERS/providerOrder/KEY_GROUPS/SECRET_KEYS), `/api/status/providers` (`health.mjs`), `llm-pricing.mjs`. `#/config` gains `HERMES_API_KEY` (secret) + `HERMES_BASE_URL` (default `http://127.0.0.1:8642/v1`) + `HERMES_MODEL` (`hermes-agent`); 6 new i18n keys ×17 (snapshot 1208→1214). **Security:** the provider fetch is a CONFIGURED endpoint (like OpenRouter/Qwen), NOT a user job URL, so it does NOT — and must not — pass through the `isValidJobUrl` SSRF guard (which is for scanned postings); no CSP/sanitizer change. End-to-end verified: `/api/status/providers` selects `hermes` when its key is set + `LLM_PROVIDER=hermes`. `tests/hermes-provider.test.mjs` +5 (stubbed transport) + the v1.146.0 "no Hermes branch" canary INVERTED to assert it IS wired + provider-surface tests updated to 7 providers. Docs moved planned→wired: HERMES.md (Shape A shipped), help §30 ×17, README teaser ×14, `hermes-bridge` skill, roadmap Phase 5. Shape B was not needed. +5 → **2390**. Before it v1.150.0 (2026-08-12 — **Consistent empty states (roadmap Phase 4 polish)**: `#/activity`, `#/cv-studio`, `#/stats`, `#/usage` dropped a redundant inline `style: { padding: '40px', textAlign: 'center', color: 'var(--foggy)' }` on their empty-state panels — all three props are already owned by the shared `.empty` class (`--space-7`=48px, centered, `--foggy`, 2px dashed border), so the four now render pixel-identical to the ~25 other `.empty` panels instead of drifting on a magic 40px. **Client CSS-usage cleanup only** — the `.empty` RULE is unchanged; no route/server/i18n/CSS-rule change (snapshot 1208). Legit per-view overrides (`#/dashboard` width:100%, `#/pipeline` border:none) untouched. Browser-verified (`#/usage` empty panel computes 48px padding + dashed border, no inline style, 0 console errors). `tests/empty-state-consistency.test.mjs` +2 (`.empty` is the single source of truth; no view re-states its props). This is the concrete slice of Phase 4's "overall visual polish"; broader taste-driven refinement stays open to direction; Phase 5 (Hermes provider) stays blocked on the API-contract spike. +2 → **2385**. Before it v1.149.0 (2026-08-12 — **Portals → Settings (roadmap Phase 4)**: the `#/portals` sidebar nav item moved from the *Sourcing* group into the *Setup* (settings) group next to *App settings* in `public/index.html` — since v1.144.0 it's a settings surface (enable/disable tracked companies + ATS health probe), not a sourcing action. **Nav markup only** — the `#/portals` route, view, and `nav.portals` label are unchanged (no i18n/route/server change). Browser-verified (the item renders under the *Setup* header, 0 console errors). `tests/portals-nav-placement.test.mjs` +2 (under Setup + not in Sourcing). +2 → **2383**. Before it v1.148.0 (2026-08-12 — **Cleaner scan filters (roadmap Phase 4)**: the `#/scan` result-filter panel (`.scan-filters` in `components.css`) moved from a ragged `flex-wrap` of rigid 160–240px boxes to a responsive `display: grid` (`repeat(auto-fill, minmax(180px, 1fr))`, even gutters) so the 11 labelled filters align into tidy columns at every width; Apply/Reset actions now span the full grid on their own hairline-separated, right-aligned row (`.scan-filters__actions`), and `scan.js` dropped the old hidden-label alignment hack + inner flex wrapper. **CSS + one small DOM cleanup only** — every `#scan-filter-*` id + `#scan-apply` + `SR.render()` wiring preserved (Playwright scan-filter flow untouched), NO new i18n keys. Browser-verified (grid 4 cols @1100px, separated right-aligned actions, 0 console errors). `tests/scan-filters-grid.test.mjs` +3. Remaining Phase 4 — the whole-app "overall visual polish" pass + the `#/portals`→settings-nav move — left for explicit user direction (subjective). +3 → **2381**. Before it v1.147.0 (2026-08-12 — **Hermes & Telegram — in-app help §30 (Phase 5b, part 2, FINAL)** (roadmap Phase 5b): the second/final slice of the Hermes docs work — a new in-app help-guide section `## 30. Hermes & Telegram` (what Hermes is + the two integration shapes; running on a cloud server; Telegram via Hermes + the "what NOT to expose" rule) fanned out to **all 17 locales** via the single-writer JSON-applier pattern, reachable from `#/help`; the `docs-assistant`/`DocsFab` grounding picks it up automatically (both read `docs/help/<lang>.md` — no wiring). Help-bundle gate lifted **29→30 H2 / 105→108 H3** across `canonical-docs-coverage`/`help-ui`/`help-ru-config-section`. cvstart.org gets a footer deep-link to the guide (`footer.hermes` ×17); the /help pages auto-render §30 ×17 at build. Still docs-only — the Hermes LLM-provider stays **planned/not-wired** (Phase 5). New canary `tests/help-hermes-section.test.mjs` +2 (every locale carries §30 with its language-independent anchors: HERMES.md/hermes-bridge/#/help/127.0.0.1/Telegram). This CLOSES the Phase 5b docs+skill deliverable (the provider integration remains a separate, blocked Phase 5 item). +2 → **2378**. Before it v1.146.0 (2026-08-12 — **Hermes agent + Telegram (Phase 5b, part 1)** (roadmap Phase 5b): a docs + skill deliverable — `docs/integrations/HERMES.md` deep-dive (two integration shapes [OpenAI-compatible endpoint vs. agent runtime], cloud-server deploy [reverse proxy + HTTPS + systemd, read-only parent contract on a headless box], Telegram-via-Hermes, threat-model "what NOT to expose" list), a `## Hermes agent + Telegram` README teaser (EN + the 13 fully-translated locale READMEs), a `hermes-bridge` skill (`.claude/skills/hermes-bridge/` — scoping-gate + prereq checks, never writes secrets to disk/logs, refuses to invent an endpoint or claim the provider is wired), and an OVERVIEW **Integrations** link. Hermes as an LLM **PROVIDER** stays **planned / not-yet-wired** — blocked on the Phase 5 API-contract spike; docs-ahead-of-code by design. `tests/hermes-docs.test.mjs` +4 (asserts the honesty markers + that `llm-dispatch.mjs` has no Hermes/Nous branch). The in-app help "Hermes & Telegram" H2 ×17 + the cvstart.org marketing surface are **deferred to v1.147.0** (Phase 5b, part 2). +4 → **2376**. Before it v1.145.0 (2026-08-12 — **Insightful stats (cont.): a rebuildable chart** (roadmap Phase 3): the `#/stats` **Target-role trend** tab (`renderTrend` — NOTE: v1.140.0/v1.141.0 changelogs mislabeled this scan-derived salary/role section as "My pipeline"; it was always renderTrend) gains a **Build a chart** widget — a `customChart()` with metric (vacancies / median salary / average salary) × dimension (by country / by role) selects that re-render live; salary metrics honor the existing currency + per-year/month controls, vacancies are a plain count; built from the same `RoleStats.aggregate` already computed for the tab (no new data/endpoint). 8 new i18n keys × 17 (`stats.customChart`/`metric`/`dimension`/`metric{Vacancies,Median,Avg}`/`dim{Country,Role}`); snapshot 1200→1208. Browser-verified (selects render + chart re-renders on change, 0 console errors). `tests/stats-custom-chart.test.mjs` +2. +2 → **2372**. Before it v1.144.0 (2026-08-12 — **Settings & filters (Phase 4, part 1): enable/disable tracked portals** (roadmap Phase 4): new `POST /api/portals/toggle` (server/lib/routes/portals.mjs) — an explicit user write that surgically flips a watched company `enabled:` flag in the parent `portals.yml`, keyed by careers_url. The edit is a comment/order-preserving TEXT edit (setEnabledInRaw: insert or flip just the one line, matching store.mjs append-pattern) + parse-validated (yaml.load must succeed or no write); withFileLock; same write-through contract as POST /api/tracker. `#/portals` gains an Enable/Disable button per company. The SCANNER needed ZERO change — en-scanner.mjs already does `companies.filter((c) => c.enabled !== false)`. 5 new i18n keys × 17 (portals.disable/enable/enabledToast/disabledToast/toggleFailed); snapshot 1195→1200. Browser-verified (Disable/Enable render per state, POST payload correct). DEFERRED (subjective/cosmetic, want user input): the scan-filter visual redesign + moving #/portals fully into a settings nav section. `tests/portals-routes.test.mjs` +3. +3 → **2370**. Before it v1.143.0 (2026-08-12 — **Understandable (cont.): `?` hints on the core workflow views** (roadmap Phase 2b, wave 2): the `?` help affordance (`window.HelpHint.title`, shipped v1.139.0) now covers 9 more view titles — `#/scan`, `#/evaluate`, `#/cv-studio`, `#/tracker`, `#/config`, `#/deep`, `#/batch`, `#/auto`, `#/apply`. 9 new i18n keys × 17 (`help.hint.scan`/…/`apply`) via single-writer agent-translate + deterministic applier (0 dups, audit clean); snapshot 1186→1195. Browser-verified EN (scan/evaluate/config/tracker/batch render the ? + popover, 0 console errors); config + batch each have TWO page-title h1s (main + catch/error state) — both wired (a wire-only-first-occurrence trap). Remaining lighter views (dashboard/cv/reports/usage/pipeline/portals/activity/docs-assistant) are a later wave. `tests/help-hint.test.mjs` +1 (9-view wiring guard). +1 → **2365**. Before it v1.142.0 (2026-08-12 — **Correctness: no more "Unknown" career archetype** (roadmap Phase 3): the `#/orientation` AI prompt (`server/lib/routes/orientation.mjs::buildOrientationPrompt`) now MUST rank the top-3 best-fit from **exactly** the 8 named career vectors (Functionalist/Administrator/Communicator/Specialist/Analyst/Innovator/Manager/Entrepreneur) and may NEVER answer "Unknown"/"N/A"/"insufficient data" or invent a label — a thin CV still gets the 3 closest at lower confidence with the missing evidence named, instead of a nonsensical "double down on Unknown". Server-only prompt change; NO i18n/schema change (output stays localized via the directive). `tests/orientation-routes.test.mjs` +1 (prompt names the 8 vectors + forbids "Unknown"). +1 → **2364**. Before it v1.141.0 (2026-08-12 — **Insightful stats (cont.): funded-company enrichment** (roadmap Phase 3): `#/funded` flat table → **enriched card grid** — each recently-funded company is a card with a **logo** (`window.CompanyLogo.badge('', name)` → derived-from-name domain via `/api/logo`, letter-avatar fallback), **round** + **amount** chips, the parent relay's **discovery_score** and **suggested_action** (previously discarded), and the funding-news source link + date; plus a **funding-amount horizontal bar chart** of the top companies by disclosed amount (new pure `parseAmount` — "$120M"/"€1.5B"/"500K" → magnitude, currency-agnostic). Browser-verified with a route-mock (cards + chart + score chips render, 0 console errors). 3 new i18n keys × 17 (`funded.byAmount`/`funded.score`/`funded.action`); snapshot 1183→1186. `tests/funded-view.test.mjs` +2 (vm-extracted `parseAmount` + source-static card wiring); the v1.133.1 regression guard in `parity-routes-v1133.test.mjs` updated table→cards. **Description + salary range are NOT in the funding feed** — out of scope for this source. +2 → **2363**. Before it v1.140.0 (2026-08-12 — **Insightful stats: richer salary figures** (roadmap Phase 3, first slice): `public/js/lib/role-stats.js::salaryStats` now returns **`avgUsd`** (mean) alongside `minUsd`/`medianUsd`/`maxUsd` — median resists outliers, mean exposes right-skew. `#/stats` "My pipeline" salary section gains a **per-year ⇄ per-month** toggle (÷12) and a **min·avg·median·max table per country** (`salaryTable` helper), both driven by the existing currency + new period selectors. 8 new i18n keys × 17 (`stats.period`/`perYear`/`perMonth`/`colCountry`/`colMin`/`colAvg`/`colMedian`/`colMax`); snapshot 1175→1183. `tests/role-stats.test.mjs` +1 (an average-right-skew case: 100k/100k/400k → median 100k, avg 200k). Remaining Phase 3 — interactive/rebuildable charts, the "Unknown" archetype fix (orientation, LLM output not a code literal), funded-company enrichment — is later releases. +1 → **2361**. Before it v1.139.0 (2026-08-12 — **Understandable: `?` help hints**: a reusable, CSP-safe `?` popover primitive (`public/js/lib/help-hint.js` → `window.HelpHint`; body rendered via `UI.md()` escape-first boundary, `role="tooltip"` + `aria-expanded`, Escape/outside-click close + focus restore, theme-aware, RTL-mirrored, viewport-clamped, singleton) wired to the **5 `#/stats` tabs** (active-tab caption "Rejection patterns (?)") and **8 AI/analytics view titles** via `HelpHint.title` (career-plan / orientation / two-pager / networking / mock-interview / memory / funded / interview-digest). **14** new i18n keys × 17 locales (`help.hint.*` + `stats.hint.*`) via single-writer deterministic fan-out (0 dups, audit clean); assembled-dict snapshot regenerated 1161→1175. All 30 views already had a `page-subtitle`, so the `?` adds the deeper on-demand explanation and makes empty states (career-plan / weekly digest / funded) self-explanatory. Browser-verified EN + RU. Guarded by `tests/help-hint.test.mjs` (+4 — CSP-safe, wired, EN-present). +4 → **2360**. Before it v1.138.0 (2026-08-12 — **generation-in-UI-language + review hardening**: the output-language directive now threads through **all 8** AI-generation endpoints (career-plan / orientation / market / mock-interview / networking / docs-assistant / **memory-suggest** / **two-pager**) and the client sends the active `lang` on all 8 generate POSTs, so a Russian/Spanish/… UI gets Russian/Spanish/… output (code + two-pager YAML **keys** stay English; only prose is localized); +2 canaries (`memory-routes`/`two-pager-routes`). **Review hardening** (both AI reviews): source-static **CSS colour-role guard** `tests/css-role-tokens.test.mjs` (+3 — text-role tokens `--fg`/`--danger`/`--ok`/… never a `background`, surface-role `--card`/`--panel`/`--line`/… never a text `color`, aliases resolve), a `UI.md()` **XSS-loader self-probe** (a future brace-mis-slice of `api.js` fails loudly, not green), and a `#/career-plan` `scrollIntoView` **isConnected** guard. `docs/UX-ROADMAP.md` renumbered (?-hints/descriptions/empty-states → **v1.139.0**, Insightful stats → v1.140.0, Settings/filters → v1.141.0) + **Phase 5/5b** Nous Research/Hermes provider + cloud/Telegram deploy guide + Hermes skill. +5 → **2356**. Before it v1.137.0 (2026-08-11 — **user-reported readability & rendering pass** (no parent-sync): dark-mode **white-on-white / black-on-black** across `#/pipeline`, `#/stats` tabs, `#/config`, `#/two-pager`, `#/mock-interview`, `✓ set`/error text fixed at the ROOT — 15 CSS custom properties (`--fg`/`--panel`/`--panel-2`/`--line`/`--surface-elev1`/`--ok`/`--go`/`--err`/`--error`/`--danger`/`--warn`/`--muted`/`--ink`/`--card`/`--border`) were referenced by views but never declared, so they fell back to hardcoded light/black literals; now aliased on `:root` to theme-aware tokens (`--hof`/`--paper`/`--slate`/`--kazan-text`/`--rausch-text`/…) so they follow the theme. `.tab-btn.is-active` solid-pink-pill+white → tinted-badge pattern. **0 WCAG-AA failures across 29 views** verified by an alpha-composited contrast auditor; regression guard `tests/dark-theme-tokens.test.mjs`. Also: `#/stats` SVG bar-chart labels **ellipsize + hover `<title>`** (were hard-cut mid-word), and `#/career-plan` **auto-renders the generated plan as formatted HTML** (was raw Markdown). Feature backlog captured in `docs/UX-ROADMAP.md` (Phase 2 Understandable = ?-hints ×17 + page descriptions + empty states + generation-language; Phase 3 Insightful stats = min/max/avg + monthly/yearly + interactive charts + Unknown-archetype fix + funded enrichment; Phase 4 = portals→settings + filter redesign). +2 → **2351**. Before it v1.136.0 (2026-08-11 — **parent career-ops v1.26.x parity, quality wave**: one new zero-auth source **`eightfold`** (Eightfold AI `*.eightfold.ai/api/apply/v2/jobs`, host-pinned) → registry **79 sources = 74 EN + 5 RU**, `ALL_ADAPTERS` 74; plus a wave of quality/robustness ports to code web-ui MIRRORS: (1) shared **`server/lib/text-key.mjs::normalizeTextKey`** (NFKC + keep `\p{L}\p{M}\p{N}`, #2569/#2587/#2667) now keys `detect-reposts` company grouping (width/punctuation variants cluster; distinct non-Latin employers never collapse) and folds `role-matcher` titles (full-width ≡ half-width; non-Latin tokens survive instead of erasing to []); (2) **`fetchJsonWithRetry` no longer retries a refused redirect** (#2657 — a `redirect:'error'` 3xx is a deterministic no-status `TypeError`, distinguished by undici's `err.cause.message`); (3) **`title_filter.positive` ` + ` AND-groups** (#2552, `compilePositiveKeyword` in location-filter.mjs); (4) **oraclecloud numbered apexes** `oraclecloud1-99.com` (#2683, bounded family); (5) **workable** retry/headers/serialization hardening (#2675); (6) **personio** XML→HTML-scrape fallback when the feed is disabled; (7) **states FALLBACK aliases** resynced (#2615). Wired eightfold into `portals/registry.mjs` + the 5 gate lists. NOT ported: reply-matcher #2672 / jd-similarity #2661 / jd-skill-gap #2686 (no web-ui surface), scan env-paths #2568 / --flag=value #2589 (in-process), CLI-only cover/CV/doctor/ollama/pdf. +37 → **2343**. Before it v1.135.0 (2026-08-11 — **parent career-ops v1.26.0 parity**: **5 new zero-auth scan sources** — `join` (join.com `__NEXT_DATA__`), `getro` (api.getro.com VC portfolio POST API), `consider` (getconsider.com board POST, structural-SSRF host guard), `joinup` (joinup.ch SSR scrape), `remotli` (remotli.ch JSON, CHF salaries) — each a `sources/<slug>.mjs` + `portals/adapters/<slug>.mjs` + CI-isolated `tests/sources-<slug>.test.mjs`, → registry **78 sources = 73 EN + 5 RU**, `ALL_ADAPTERS` 73; wired into `portals/registry.mjs`, the two gate tests, `FALLBACK_SOURCES` (scan-results.js) + `SOURCE_URLS` (site Sources.astro). Provider fixes to boards web-ui already carries: **a16z Speedrun** transient-retry (new shared `fetchJsonWithRetry` in `http-json.mjs` — bounded retries on 429/5xx/timeout only, never a 4xx; page budget 3→6 / cap 120→1000, parent #2506/#36d0c44); **arbeitsagentur** → v6 Jobsuche API (`/pc/v6/jobs`, response-shape rename, #2494); **thehub** → v2 `jobsandfeatured` API (#6b33fc4, no posted date → exempt from age filter); **hackernews** → `tags=story,author_whoishiring` account lookup (#3aa5e15). NOT ported: the Unicode role-dedup/company-key #2569/#2587/#2429 (web-ui's `detect-reposts` keys company on plain lowercase — already non-Latin-safe; `trust-validator`'s ASCII heuristic degrades to no-flag, never a silent merge; role-token collapse matches the parent's deliberate out-of-scope call); followup rejection-latency #2014 + `company-funded` touch-ups (relay-absorbed, fail-soft); scan env-paths #2568 / `--flag=value` #2589 (web-ui runs scanners in-process, no CLI); the UA-consolidation refactor #2536 (web-ui already centralizes `BROWSER_LIKE_USER_AGENT`). +119 → **2306**. Before it v1.134.1 (2026-08-05 — **validation-hardening** from a full-project audit (all 17 locales browser-swept + code defect-hunt): (1) `server/lib/sources/successfactors.mjs` mid-scan failure now keeps partials — the v1.134.0 dead-board-throw port left its RMK pagination loop uncaught, so a page-2+ failure discarded scraped jobs and a page-N 404 could quarantine a live tenant as dead; now mirrors phenom/radancy (`succeededOnce` guard, +1 regression test). (2) `#/scan` facet filter chips (`public/js/lib/scan-results.js`) made keyboard-operable — were `<span onClick>` with no tabindex/role (WCAG 2.1.1 fail); now `role="button"` + `tabindex="0"` + `aria-pressed` + Enter/Space. (3) Three hardcoded English strings localized (`#/scan` trust tooltip + Reloc header, `#/dashboard` Score header) — invisible to the i18n parity gate because they were never keys; now `scan.trustTip` + `scan.col.reloc` (2 new keys ×17) + `track.col.score` reuse, guarded by `tests/scan-chip-a11y.test.mjs`. +3 → **2187**. Security: bumped `fast-uri` 3.1.4→3.1.5 in `site/package-lock.json` (Dependabot GHSA high — host confusion via backslash authority; transitive dev-only dep of the Astro toolchain; `npm audit` 0 vulns, site build green). Before it v1.134.0 (2026-08-05 — **parent career-ops v1.25.0 parity**: new scan source **getManfred** (`server/lib/sources/manfred.mjs` + adapter + `tests/sources-manfred.test.mjs`; board-wide Spanish/EU tech feed with published salaries from `www.getmanfred.com/api/v2/public/offers`, zero-auth, host-pinned) → registry **73 sources = 68 EN + 5 RU**, `ALL_ADAPTERS` 68; wired into `portals/registry.mjs`, the two gate tests, `FALLBACK_SOURCES` (scan-results.js), and `SOURCE_URLS` (site Sources.astro). Plus provider fixes: a16z Speedrun 50-job truncation (`PER_PAGE` 100→50, parent #2404); the **dead-board-throw** contract on cryptocurrencyjobs/phenom/radancy/successfactors (parent #2379 — a total outage with no successful request THROWS so `#/portals` health records a failure, instead of swallowing to `[]`; mid-scan failures keep partials); and **workable** switched to the public widget API (`apply.workable.com/api/v1/widget/accounts/<slug>`, parent #5ab8425) so large accounts scan fully. +40 → **2184**. NOT ported: detect-reposts #2389 title-bucketing (perf-only over web-ui's small in-process history), the Unicode company-key fixes (company-history/fingerprint/tracker-utils — not mirrored; web-ui tracker already non-Latin-safe), scan `--since`, cv-facts, CV template/PDF audit, doctor, modes untrusted-content. Before it v1.133.1 (2026-08-02 — **`#/funded` bugfix** (user-reported "данные не грузятся"): two bugs made the Funded-companies table always empty even though the relay worked. (1) `public/js/views/funded.js` read the discovery results under `res.candidates`, but the parent `company-funded.mjs` emits them under **`companies`** (each `{company, amount, round, funding:{sources:[{source,url,observed_date}]}}`) — the view now reads the right key + maps `funding.sources[0]` for the evidence link/source/date. (2) The results table passed cells to `UI.el('tr', {}, …)` as **varargs**, but `UI.el(tag, attrs, children)` takes `children` as a single node OR an array (`[].concat`), so only the first column rendered — cells now passed as an array (matching `reports.js`). Browser-verified: 11 companies × 4 columns, working links, 0 console errors; empty pass now shows per-source diagnostics. The CI fake in `tests/parity-routes-v1133.test.mjs` mirrored the WRONG `candidates` shape (which is why the bug shipped green) — now emits the real `companies` shape + source-static canaries (reads `companies` not `candidates`, rows use array children); +1 → **2144**. Before it v1.133.0 (2026-08-01 — **two read-only parent-parity relays** (parent career-ops advanced past v1.24.0): (1) **funded-company discovery** — new route module `server/lib/routes/funded.mjs` (`GET /api/company-funded`) relays the parent's `company-funded.mjs` via `--json --dry-run` (JSON to stdout, no writes; review-first list of recently funded companies from public host-pinned RSS feeds — TechCrunch/PR Newswire/The Guardian/Hacker News; user input never threaded into `--sources`), surfaced as the `#/funded` view under Sourcing; (2) **weekly interview digest** — `GET /api/interview/weekly-digest` (added to the existing `interview` route module) relays the parent's zero-LLM `weekly-digest.mjs` (roll-up of `interview-prep/sessions/*.md`; optional `?from=&to=` threaded only when BOTH are valid `YYYY-MM-DD`; empty range is a valid `available:true` digest), surfaced as `#/interview-digest` under Analytics. Both follow the fail-soft `{available:false}` relay contract (like `/api/stats/lifetime`). New views `public/js/views/{funded,interview-digest}.js`, 26 new i18n keys ×17, CI-isolated `tests/parity-routes-v1133.test.mjs` (+5 → **2143**). NOT ported: the parent's Next.js web/ Follow-up Tracker page (#1422) + backend PDF render (#2182) — web-ui already has its own follow-up relay + PDF runners, and the `followup-cadence.mjs` hardening arrives free via the shell-out. Before it v1.132.0 (2026-07-31 — **`#/scan` results-rendering subsystem extraction (file-size-contract, pass 1)**: the results subsystem (`renderResults`, `buildChipRow`, `getRows`, row/facet builders, seniority/country option painters, `FALLBACK_SOURCES` mirror) moved verbatim out of `public/js/views/scan.js` (~1254→~906 LOC) into `public/js/lib/scan-results.js` — a `window.ScanResults.create(ctx)` factory closing over a view-supplied context (filter els, active-facet Sets, paginator, two-pager data, `lastResults` getter); the view now calls `SR.render()`/`SR.getRows()`, and `index.html` loads the new lib before `scan.js`. **No behavior change** (functions moved verbatim, closure vars rewired to `ctx.*`); source-static scan tests repointed to `tests/helpers/scan-src.mjs::loadScanSrc()`, verified in a real browser by the new `tests/playwright-scan-filters.mjs` (seeds `data/last-scan.json`, drives every filter, asserts exact counts). Still >800 LOC — extraction pass 2 owed. Housekeeping: README "Latest release" banner slimmed to a one-line summary + CHANGELOG link ×17 (the long multi-version narrative wall retired); a CodeQL pass removed dead `readFileSync`/`resolve`/`APP_CSS` imports from 4 CSS source-guard tests (closes 8 `js/unused-local-variable` alerts). No net `node --test` delta → **2138**. Before it v1.131.2 (2026-07-31 — **`app.css` split (file-size-contract debt)**: the ~1990-LOC single stylesheet became three ordered `<link>`s — `app.css` (~672: a11y/tokens/theme/sidebar/main/buttons/content-shell), `components.css` (~595: cards/grids/paginator/badges/tables/forms/log/markdown/lang-switcher/chips/connection-banner), `overlays.css` (~737: toast/notif-drawer/modal/misc/`[dir=rtl]`/docs-fab/usage-hud), each within the 800-LOC hard limit. Contiguous cut → cascade byte-for-byte identical; CSS-asserting tests now read the concatenation via `tests/helpers/css.mjs::loadAppCss()`; split locked by `tests/css-modularization.test.mjs` (+3 → **2138**). No behavior/markup/i18n change. `scan.js`/`config.js` remain the outstanding size debts. Before it v1.131.1 (2026-07-31 — **adapter host-pinning hardening** (code-review follow-ups on the v1.130.0 sources, defense-in-depth, no behavior change for valid inputs): the `a16z-speedrun-talent` **adapter** re-validates the `api:`/`a16z-speedrun-talent:` override at `buildEndpoint` (HTTPS + exact host via a new exported `SPEEDRUN_TALENT_HOST_RE`, shared with `assertSpeedrunUrl`) and falls back to `FEED_URL` on failure — parity with the `cryptocurrencyjobs` adapter, so an off-host value never reaches the fetch slot; `cryptocurrencyjobs` `cleanUrl` now uses the exact-match `CRYPTOCURRENCYJOBS_HOST_RE` (was `endsWith`, accepted subdomains) so the parser is never looser than the SSRF guard; +2 tests → **2135**. Before it v1.131.0 (2026-07-31 — **#/tracker CRM stage-tab board** (parent web/ `/pipeline` port): the funnel-chip bar + status `<select>` become a **stage-tab strip** — All + one tab per canonical status (Evaluated/Applied/Responded/Interview/Offer/Rejected/Discarded/SKIP/Hired), live whole-history counts INCLUDING zero-count stages (full-funnel CRM look), active tab drives the filter (click active → All); new read-only `GET /api/tracker/stages` returns the canonical labels + alias-fold map from `server/lib/states.mjs` (templates/states.yml + fallback) so the client never hardcodes the whitelist (legacy no-param `/api/tracker` still `{rows}` only); new pure lib `public/js/lib/tracker-stages.js` (`window.TrackerStages` foldStatus/stageCounts, unit-tested); rows gain toggle-gated brand logos; tabs a11y (role=tablist/tab, aria-selected, ≥44px, counts in accessible names, focus restored on switch); NO new i18n keys (stage labels verbatim like badges); +10 tests → **2133**. Before it v1.130.0 (2026-07-31 — parent career-ops v1.24.0 parity: **2 new scan sources** — a16z Speedrun (`a16z-speedrun-talent`, board-wide talent-network JSON feed, #2231) + Cryptocurrency Jobs (`cryptocurrencyjobs`, Web3 board via public RSS) → registry **72** = 67 EN + 5 RU, ALL_ADAPTERS 67; MIRROR echojobs #2258 (hybrid stays distinct from remote, never collapsed to Remote) + radancy legacy TalentBrew markup + JSON results-fragment transport; role-matcher #2209 already covered by v1.127.0; parent CLI/compliance/jurisdiction/contacts/interview/ledger/outcome/triage modes + scan.mjs-orchestration items (Interamt Playwright, iCIMS reverse-sweep, country-eligibility, DNS pacing, StepStone rltr, scan-history normalized-company) NOT ported — web-ui runs the EN/RU scanners in-process and doesn't shell into scan.mjs; +50 tests → **2123**. Before it v1.129.1 (2026-07-29 — AI-review follow-ups (all advisory) on the v1.128/v1.129 web-ports: **job-facets seniority precedence** (explicit staff/senior/junior/intern modifier now beats a management word — "Senior Engineering Manager"→senior, was lead), **states.mjs** returns the FALLBACK uncached (transient parent unavailability at boot no longer pins it) + warns on a malformed present file, **score-tone** null/blank→muted not red, **domainFromName** skips non-ASCII slugs before /api/logo, +a states.test isolation-sanity guard; +4 → **2073**. Before it v1.129.0 (2026-07-29 — wired the v1.128.0 `job-facets.js` lib into `#/scan`: a **Seniority** filter facet (JobFacets.seniorityFromTitle, auto-populated like Country; null bucket passes), a **Seniority** badge column, and a zero-token **Age**/freshness column (daysSince → today/Nd); 12 new i18n keys ×17, +3 tests → **2069**. Before it v1.128.0 (2026-07-29 — four solutions ported from the parent's own Next.js web app (`../web/`), vanilla JS/ESM, no new deps: (1) `server/lib/states.mjs` reads `templates/states.yml` LIVE for the tracker status vocabulary + alias canonicalization (kills the manual whitelist re-sync every parent release; POST folds aliases, GET funnel buckets by canonical status); (2) `company-logo.js` `domainFromName()` — ~90 brand→domain overrides + slug fallback so ATS-hosted rows show a brand logo; (3) `score-tone.js` 4-tier score tone (≥4.2/3.8/3.0 + letter fallback, new `.score-muted`); (4) `job-facets.js` seniority/source/days facets. +21 tests → **2066**. No new sources (registry 70), no i18n/help changes. Before it v1.127.0 (2026-07-29 — parent career-ops v1.23.0 parity: **3 new scan sources** — Flowxtra (board-wide aggregator), VDAB (Flanders vindeenjob keyword API), iCIMS (`careers-<tenant>.icims.com` hosted portal, distinct from jibeapply) → registry **70** = 65 EN + 5 RU, ALL_ADAPTERS 65; agenticjobs HTML→REST rewrite (#2167), Greenhouse office-city /offices enrichment (#2104), role-matcher parity (#1933/#2164/#2009: MTS prefix, product baseline, accent-fold, sub-baseline disagreement); **Cursor re-added** to cli-detect (10 tools) + the roster ×17 (parent #2115 restored it as a first-class host); +76 tests → **2045**. Before it v1.126.1 (2026-07-25 — docs-drift patch: two CLI-roster spots the v1.126.0 sweep missed — the `#/config` API-keys tab intro `config.providerModelNote` (i18n ×17) gained Antigravity + Grok Build, and a stale `Cursor / Gemini CLI` help-table row ×17 (+ CI-built site help) now carries the full 8-CLI roster; i18n snapshot regenerated, no test delta → 1969). Before it v1.126.0 (2026-07-25 — docs & CLI-roster resync with career-ops.org/docs (all 31 pages read): `cli-detect.mjs` now detects all 8 first-class CLIs — added Grok Build (`grok`) + Kimi (`kimi`), Antigravity probes `agy` first → 9 tools reported; help ×17 + README ×17 canonical AI-assistant lists (intro / comparison table / provider-setup / AI-CLI-tools tab / CLI→provider mapping) resynced to the 8-first-class + Gemini-legacy roster; `canonical-docs-coverage` CANON widened, `cli-detect-routes` length 7→9, wiki Features.md CLI-detect line updated; help bundles keep 29 H2 / 105 H3; no net test-count change → 1969). Before it v1.125.4 (2026-07-23 — maintenance sync: dependabot site bumps (sharp 0.35.3 / svgo 4.0.2 / fast-uri 3.1.4, #151–#153) + parent parity sweep `37d17ec..254764a` (post-v1.22.0 #2108–#2168 — set-status wrong-row guard, localized-mode Risk Summary, updater manifest check: all CLI-side, nothing ported; parent VERSION stays 1.22.0). Before it v1.125.3 (2026-07-23 — **da/hi prompt-locale fix**, user-reported: `LOCALE_NAMES` + all five `SCAFFOLD_STRINGS` bags in `server/lib/prompts.mjs` were never extended for Danish/Hindi, so `resolveLocale()` normalized them to `en` and every AI prompt lost its `# Output language` directive; both locales now first-class, `tests/locale-scaffold.test.mjs` sweeps the canonical 17-locale `I18N_LANGS` list + a scaffold-vs-en structural parity gate, +12 tests → **1969**). Before it v1.125.2 (2026-07-22 — first external-contributor pack, @Alien10140: #145 headless deep-prompt fix (`buildDeepPrompt`/`bundleProjectContext` `{headless}` flag — live `/api/deep` no longer 502s on Gemini `MALFORMED_FUNCTION_CALL`; manual copy-paste prompt keeps Claude Code tool names) + #144 Gemini defaults `gemini-2.0-flash`→`gemini-3.6-flash` (dropdown/server fallback/OpenRouter chain/hints ×17/help ×17), pinned by the new `tests/gemini-default-model.test.mjs` drift gate. Before it v1.125.1 (2026-07-21 — SuccessFactors multi-brand RMK brand-path fix, parent #2099; before it v1.125.0 (2026-07-21 — cvstart.org "Job sources" landing section: all 67 sources as linked chips, registry-synced via facts.sources + tests/site-sources.test.mjs drift gate, header Sources anchor, 4 site i18n keys ×17. Same day v1.124.0, parent career-ops v1.22.0 parity — five sources (wttj/agenticjobs/jobvite/gem/alibaba, registry **67** = 62 EN + 5 RU), Arbeitsagentur VOLLSTAENDIG remote verification, SmartRecruiters public-URL fix. Before it v1.123.0 (2026-07-17, parent v1.21.0 parity — Oracle Recruiting Cloud source+adapter (registry **62** = 57 EN + 5 RU), role-matcher #1922 specialized-suffix fix; tests/sources-oraclecloud.test.mjs. Before it v1.122.0 (2026-07-16 — **Hindi (hi) added as the 17th locale**: full dict + help bundle + README.hi.md + CHANGELOG.hi.md (starts v1.122.0) + cvstart.org pages + wiki Home-(हिन्दी); all parity gates now ×17. Same day v1.121.0 — cvstart.org gained localized **/methodology/ /license/ /changelog/** pages ×16 (sync-assets now syncs CHANGELOG ×16 + LICENSE at build; header Methodology entry; footer links; `tests/site-pages.test.mjs`), plus career-ops.org/methodology links across README/help/wiki. v1.120.0 same day: parent v1.20.0 parity — the CareerOps Manifesto surfaced: SPA sidebar-footer link + `footer.manifesto` ×16, Help §29 ×16 (29 H2 / 105 H3), README section ×16, cvstart.org footer link, `tests/manifesto-link.test.mjs`). **2396** `node --test` cases, **79 scan sources** (74 EN + 5 RU, incl. the v1.136.0 eightfold source), 32 route modules, 17 UI locales, cvstart.org landing (`site/`, CI-built). History below is retained from earlier snapshots:
> Playwright (smoke + full-cycle + forms + locale-sweep ×12 + theme-toggle). **v1.70.0 (I18N-EXPAND)** added 3 locales — Polish (pl), Ukrainian (uk), Arabic (ar, full RTL via `<html dir>` + `[dir="rtl"]` in app.css) — for **12 UI locales**, plus a flag `<select>` language switcher; **v1.71.0** ported the parent's cover-letter mode to `#/cover` with a Generate-PDF button; **v1.71.1** translated the in-app help guide into all 13 locales (`docs/help/{pl,uk,ar}.md`). Parent tracked at **career-ops v1.11.0**. **v1.69.0 (P-14)** made `server/lib/sources/registry.mjs`
> auto-discover scanner adapters at boot (drop a `*.mjs` with an `export const meta` block — no registry edit).
> **v1.60.0 (I18N-SPLIT)** split the 8-language
> translation megafile into one file per locale under `public/js/lib/locales/`; `i18n-dict.js` is now an
> assembler, `t()` and all call-sites unchanged (lossless, 678 keys). v1.55.1→v1.56.4 consolidated UX fix-prompt
> complete; **v1.57.0** adds OpenRouter as a 5th headless live-eval
> provider (one key → 300+ models, live `#/config` model dropdown via
> `GET /api/openrouter/models`) and fixes the `/#/config`
> "validation failed" bug (keys pasted with whitespace/newline now
> trim + save for every provider). Sole open item: G-005 (cross-repo,
> blocked on the parent oferta.md commit).

---

## What this repo is

`career-ops-ui` is an Express + vanilla-JS SPA that puts a polished
web interface on top of
[`Fighter90/career-ops`](https://github.com/Fighter90/career-ops) —
a Claude-Code-driven AI job-search pipeline. It sits inside the
parent project as `career-ops/web-ui/` and reads/writes the same
data files (`cv.md`, `data/applications.md`, `reports/`,
`portals.yml`).

**Single-tenant, loopback-by-default, no telemetry, no cloud accounts.**

---

## Stack snapshot

| Layer | Tech | Where |
|---|---|---|
| Server | Node ≥ 18, Express 4, js-yaml, multer | `server/index.mjs` (~130-LOC orchestrator) + `server/lib/routes/*.mjs` (15 modules) |
| Helpers (v1.21+) | ESM, no transpiler | `server/lib/{paths,parsers,runner,security,prompts,store,anthropic,env-config,activity-log,dotenv,safe-fetch,file-lock,rate-limit,en-scanner,ru-scanner,gemini,detect-reposts,role-matcher}.mjs` + `server/lib/sources/{greenhouse,ashby,lever,rss,smartrecruiters,workable,workday,geekjob,getmatch,habr,hh,trudvsem}.mjs` (46 adapters as of v1.97.0, auto-discovered via `registry.mjs` since v1.69.0/P-14) |
| SPA | Vanilla JS, hash-router | `public/index.html`, `public/js/{app,router,api}.js`, `public/js/views/*.js`, `public/js/lib/{i18n,i18n-dict,skills,auto-pipeline,pdf-generate,report-export}.js`, `public/js/lib/locales/i18n-dict.<lang>.js` (16 + aliases) |
| Styling | Hand-written CSS + design tokens | `public/css/{app,components,overlays}.css` (v1.131.2 split, 3 ordered `<link>`s) |
| Tests | `node --test` (TAP) + Playwright | `tests/*.test.mjs`, `tests/playwright-smoke.mjs`, `tests/e2e*.mjs` |
| Build | None | Files served as-is from `public/` |
| CI | GitHub Actions, Node 18/20/22 | `.github/workflows/{ci,release,publish-package,ai-review,dashboard-screenshots}.yml` |

**Test baseline (v1.71.0):** 1100/1100 unit · Playwright (smoke + full-cycle + forms + locale-sweep ×12 + theme-toggle) · 20/20 smoke E2E · 23/23 comprehensive E2E · coverage ~93% line / ~83% branch.

---

## Hard rules (full text in `CLAUDE.md`)

1. **Never edit anything outside `web-ui/`.** Parent career-ops is read-only from this repo.
2. **Never load real user data into context.** `.aiignore` exists.
3. **Never weaken security headers.** CSP excludes `'unsafe-inline'`.
4. **Never bypass `isValidJobUrl`** for URL-fetching endpoints. v1.21+ also requires routing through `server/lib/safe-fetch.mjs::safeGet` for the actual fetch (DNS-rebind defense).
5. **Never sanitize CV markdown outside `stripDangerousMarkdown`.** Entity-aware as of v1.22.0.
6. **Never commit `.env`.** Use `.env.example` placeholders.
7. **Never use `--no-verify` / `--force` / `git reset --hard`** without explicit user approval.
8. **Tests must be CI-isolated.** No parent dependency, no live network, no port collision (`server.listen(0)`).

---

## v1.21–v1.22 security envelope (added since last big update)

When auditing or extending these surfaces, route through the helpers below — don't reinvent.

| Surface | Helper | Lives in |
|---|---|---|
| URL validation (input gate) | `isValidJobUrl(s)` | `server/lib/security.mjs` |
| Outbound GET (DNS-pinned, redirect-revalidated) | `safeGet(url, opts)` | `server/lib/safe-fetch.mjs` |
| `:name` / `:slug` route-param sanitization | `sanitizePathName(s)` | `server/lib/security.mjs` |
| CV markdown XSS strip (entity-aware) | `stripDangerousMarkdown(s)` | `server/lib/security.mjs` |
| LLM endpoint throttle (LAN deploys only) | `llmRateLimit` middleware | `server/lib/rate-limit.mjs` |
| Concurrent-write mutex on parent files | `withFileLock(path, fn)` | `server/lib/file-lock.mjs` |
| i18n key coverage (CI canary) | `tests/i18n-coverage.test.mjs` | (test-time) |
| a11y form-wire validity (CI canary) | `tests/a11y-form-wires.test.mjs` | (test-time) |

---

## SPA invariants

- **No build step.** Add a script via `<script src>` in `public/index.html`; the file ships as-is.
- **No inline event handlers.** CSP blocks them. Use `addEventListener` via the `on*` branch of `UI.el()`.
- **All markdown rendering** goes through `UI.md(text)` (in `public/js/api.js`). It escape-firsts. Don't bypass with raw `innerHTML`.
- **All i18n** goes through `t('key', 'EN fallback')`. The fallback is for development; missing-from-DICT keys fail the `i18n-coverage` test in CI.
- **a11y form wires.** Every `<input>` / `<textarea>` / `<select>` needs either a `<label htmlFor="…">` or an `aria-label`. Every `aria-describedby` must point at an existing `id`. The `a11y-form-wires` test enforces both.
- **`UI.el()`** accepts `htmlFor` as a React-style alias for the `for` attribute (v1.20.0+, null-guarded v1.22.0+).

---

## Project tree (top three levels)

```
career-ops-ui/
├─ AGENTS.md, CLAUDE.md, GEMINI.md            # agent shims (CLAUDE.md is canonical)
├─ CHANGELOG.{,es,pt-BR,ko-KR,ja,ru,zh-CN,zh-TW,fr,pl,uk,ar}.md
├─ README.{,es,pt-BR,ko-KR,ja,ru,zh-CN,zh-TW,fr,pl,uk,ar}.md
├─ LICENSE, package.json, package-lock.json
├─ bin/
│  ├─ start.sh                                  # one-shot launcher
│  ├─ setup.sh                                  # bootstrap (clones career-ops + web-ui)
│  └─ run_all.sh                                # every test surface in one go
├─ server/
│  ├─ index.mjs                                 # orchestrator: middleware + 14 register*Routes(app)
│  └─ lib/
│     ├─ paths.mjs, parsers.mjs, runner.mjs
│     ├─ security.mjs                          # isValidJobUrl, sanitizePathName, stripDangerousMarkdown, …
│     ├─ safe-fetch.mjs                        # v1.21.0 (B-1) DNS-pinned safeGet
│     ├─ file-lock.mjs                         # v1.21.0 (H-6) withFileLock
│     ├─ rate-limit.mjs                        # v1.21.0 (H-5) llmRateLimit
│     ├─ prompts.mjs, store.mjs, anthropic.mjs
│     ├─ env-config.mjs, activity-log.mjs, dotenv.mjs
│     ├─ en-scanner.mjs, ru-scanner.mjs
│     ├─ sources/                              # 46 adapter clients (v1.97.0) + registry.mjs (auto-discovery, P-14 v1.69.0)
│     ├─ portals/                              # adapter registry + resolveAdapter()
│     └─ routes/                               # 24 route modules — one per topic (incl. openrouter, market, career-plan, orientation)
├─ public/
│  ├─ index.html                               # CSP-locked shell
│  ├─ css/{app,components,overlays}.css        # design tokens + WCAG 2.2 AA (v1.131.2: 3 ordered files)
│  └─ js/{app,router,api}.js + js/lib/* + js/views/*
├─ tests/
│  ├─ *.test.mjs                               # 900 unit + integration
│  ├─ playwright-{smoke,full-cycle,forms}.mjs  # 58 browser flows
│  ├─ e2e{,-comprehensive}.mjs                 # 20 + 23 E2E
│  └─ fixtures/                                # CI-isolated test data
├─ docs/
│  ├─ PROJECT.md, ROADMAP.md, PRODUCTION-READINESS.md, portals-examples.md
│  ├─ sdd/{SDD-GUIDE,CONVENTIONS}.md
│  ├─ architecture/{OVERVIEW,SERVER,FRONTEND,API,DATA-FLOWS}.md
│  ├─ reviews/REVIEW-*.md                      # per-release code reviews
│  ├─ specs/V1.20.1-BACKLOG.md                 # last sprint backlog (closed)
│  ├─ help/{en,es,pt-BR,ko-KR,ja,ru,zh-CN,zh-TW}.md   # 16-section help bundles
│  └─ adr/                                     # architecture decision records
├─ .claude/
│  ├─ settings.json
│  ├─ PROJECT-CONTEXT.md                       # this file
│  ├─ agents/                                  # 3 project-specific subagents
│  └─ commands/                                # slash-command stubs
├─ .github/
│  ├─ workflows/                               # 5 CI workflows
│  └─ copilot-instructions.md                  # GitHub Copilot canonical entry point
├─ qa/
│  └─ claude-cowork-browser-test-prompt.md     # E2E manual + automated prompt
└─ .planning/                                   # GSD scratch (gitignored)
```

---

## How to navigate

| Question | Read this |
|---|---|
| High-level project goal | [`../docs/PROJECT.md`](../docs/PROJECT.md) |
| Server architecture | [`../docs/architecture/SERVER.md`](../docs/architecture/SERVER.md) |
| Every endpoint + payload | [`../docs/architecture/API.md`](../docs/architecture/API.md) |
| Data flows in/out of parent files | [`../docs/architecture/DATA-FLOWS.md`](../docs/architecture/DATA-FLOWS.md) |
| Last shipped release | [`../docs/reviews/REVIEW-2026-05-14-v1.21.0.md`](../docs/reviews/REVIEW-2026-05-14-v1.21.0.md) |
| Deferred work | [`../docs/PRODUCTION-READINESS.md`](../docs/PRODUCTION-READINESS.md) |
| Spec-driven workflow | [`../docs/sdd/SDD-GUIDE.md`](../docs/sdd/SDD-GUIDE.md) |
| Coding conventions | [`../docs/sdd/CONVENTIONS.md`](../docs/sdd/CONVENTIONS.md) |

---

## Standard agent loop (for non-trivial work)

```
1. gsd-explore           → research what exists / what's needed
2. gsd-plan-phase        → docs/specs/V<X.Y.Z>-BACKLOG.md or .planning/ artifact
3. gsd-execute-phase     → small atomic commits with passing tests
4. gsd-verify-work       → goal-backward check against the spec
5. gsd-code-review       → produce docs/reviews/REVIEW-*.md
```

For trivial work (single file, single concern, < 30 min): just edit, run tests, commit.

---

## Common pitfalls (read once, avoid forever)

- **Hardcoded `../cv.md`** — use `PATHS.cv` from `server/lib/paths.mjs`.
- **Hardcoded port 4317** — tests bind to `:0` and read `server.address().port`.
- **Inline event handlers** (`<button onclick="…">`) — CSP rejects. Use `addEventListener` via `UI.el()`.
- **`innerHTML` without `UI.md`** — bypass of XSS strip. Route all markdown through `UI.md(text)`.
- **`globalThis.fetch` for outbound HTTP** — bypasses DNS-rebind defense. Use `safeGet` from `server/lib/safe-fetch.mjs`.
- **Read-modify-write on `applications.md` / `pipeline.md`** without `withFileLock(path, fn)` — race condition will silently drop rows.
- **New i18n key without DICT entry** — `tests/i18n-coverage.test.mjs` (+ `tests/i18n-locale-files.test.mjs` parity) fails. Add the key to **all 16 per-locale files** `public/js/lib/locales/i18n-dict.<lang>.js` (I18N-SPLIT v1.60.0; I18N-EXPAND v1.70.0/v1.85.0) — NOT to `i18n.js` (logic-only) or `i18n-dict.js` (assembler).
- **`aria-describedby` without matching `id`** — `tests/a11y-form-wires.test.mjs` fails.
- **New runtime dep** — current production deps are `express`, `js-yaml`, `multer`. Adding more needs a spec.
- **Real LLM calls in tests** — mock the SDK adapter; never hit Anthropic / Gemini from a unit test.

## Upstream mirror inventory (parent-sync map)

Some `server/lib` modules are **hand-mirrors of parent `career-ops` modules** and must be re-checked whenever the parent changes them. The in-code "ported from"/"parity" comments were removed for product cleanliness, so this is the authoritative map (keep it here, not in the source):

| web-ui file | tracks parent |
|---|---|
| `server/lib/role-matcher.mjs` | `role-matcher.mjs` (web-ui is AHEAD — Unicode `\p{L}` strip) |
| `server/lib/detect-reposts.mjs` | `detect-reposts.mjs` |
| `server/lib/classify-tier.mjs` | `classify-tier.mjs` |
| `server/lib/url-key.mjs` | `url-key.mjs` |
| `server/lib/html-entities.mjs` | `providers/_html-entities.mjs` |
| `server/lib/http-json.mjs` | `providers/_http.mjs` |
| `server/lib/trust-validator.mjs` | `providers/_trust-validator.mjs` |
| `server/lib/states.mjs` | states / `templates/states.yml` (+ web/ states) |
| `server/lib/location-filter.mjs`, `scan-sanitize.mjs`, `cooldown.mjs`, `text-key.mjs` | corresponding `scan.mjs` filter/sanitize logic |
| `server/lib/sources/*.mjs` | `providers/*.mjs` (each job board) |
| `server/lib/portals/adapters/*.mjs` | provider detection in `providers/*.mjs` |
| `public/js/lib/{logbuf,bug-report,job-facets,score-tone,company-logo,tracker-stages}.js` | parent `web/src/lib/*` |

When porting a parent change here, frame CHANGELOG/README as a **new feature** (never "ported/parity"); provenance stays only in the commit body + this file. See the memory note *changelog-readme-new-feature-framing* (centralized here on purpose — the map is engineering context, kept out of product/CHANGELOG surfaces, not concealed).

## Realizations / hard-won notes (v1.57–v1.97)

- **I18N-SPLIT (v1.60.0) — per-locale dictionary.** Translations live ONLY in `public/js/lib/locales/i18n-dict.<lang>.js` (12 files) + `i18n-dict.aliases.js`; `i18n-dict.js` is an assembler that rebuilds `window.__I18N_DICT` — it stores nothing. Three traps: (1) **vm-realm deepEqual** — objects assembled inside `node:vm` have a foreign `Object.prototype`, so `deepStrictEqual` vs a JSON snapshot throws "same structure but not reference-equal"; round-trip `JSON.parse(JSON.stringify(x))` first. (2) **Load order is load-bearing** — all 13 locale `<script>`s must precede `i18n-dict.js`, which must precede `i18n.js` (locked by `tests/i18n-locale-files.test.mjs`). (3) **A "passing" CI step can be a no-op** — the `ci.yml` inline i18n check validated an *empty* dict for ~37 releases after the v1.23 split (it loaded only `i18n.js`); now it loads the full chain + a `keys < 600` floor. Node tests load the dict via `tests/helpers/i18n-vm.mjs` (`loadAssembledDict` / `loadI18n` / `legacyDictText` / `allLocaleSource`).
- **`PATHS` resolves ONCE per process** (`server/lib/paths.mjs`, at module load). Set `CAREER_OPS_ROOT` *before* the first `server/*` import; you cannot switch parent roots mid-process. `node --test` isolates per *file* (child process) — multi-root within one file is infeasible. Path/IO-coupled helpers (`checkProfileCustomized`) → guard **statically** (assert the source contract), not via cache-bust dynamic imports. (v1.58.0 cache-bust test passed locally, failed CI on all Node versions → v1.58.1 static-guard fix.)
- **Pre-commit AI review is advisory; `ci.yml` is the hard gate.** A green pre-commit + red CI is possible (v1.58.0). Always re-confirm CI/Publish conclusions after a tag push.
- **`publish-package.yml` runs the test suite** before publishing and is **manual `workflow_dispatch`** — the `GITHUB_TOKEN`-created Release does NOT trigger it. A failing test reds both CI *and* Publish.
- **`api.js` is parsed as binary by `grep`** (stray byte) — use `grep -a` or `sed`.
- **The SPA's `api.js` auto-injects `lang` into every JSON POST body.** Non-LLM routes that strictly reject unknown keys must `delete body.lang` before validating (this was the real `/#/config` "validation failed" root cause, v1.57.2).
- **Server diagnostics stay English by policy; the SPA localizes its own chrome.** Don't add one-off i18n to a server error string — it'd be inconsistent with every other server error. Localize client-owned strings only (`api.netError`/`api.netHint`).
- **Live smoke = GET only.** Write-side endpoints on the deployed server write the real parent `.env`/files. Verify writes via CI-isolated tests with `CAREER_OPS_ROOT=mktemp`.
- **`cleanLlmMarkdown` (`server/lib/llm-output.mjs`)** strips echoed tool/agent scaffolding from model prose; apply at every provider boundary + on serving saved briefs. It is NOT an HTML sanitizer — `UI.md()` remains the XSS boundary.
- **Repro before patching (v1.58.5, NEW-3).** The v1.58.3 MASTER regression observed two POSTs to `/api/mode/followup` after a single Run-live click — but source inspection of `public/js/views/mode-page.js::submit()` showed no structural double-bind (single `onClick` per button, no parent `<form>`, no `addEventListener('submit')`), and `UI.withSpinner` (FIX-L1) already disables the button while the request is in flight. Following the fix-prompt's "repro first" doctrine: NEW-3 was triaged **not-reproducible**, and a Playwright test in `tests/playwright-smoke.mjs` was written to *prove* exactly-one-POST under the exact regression recipe (date left blank, manual button shares `submit()` with Run live, 3 s window). When in doubt about a flaky-symptom report, write the regression-locking test first; if it goes green the QA observation is recipe-only, not a shipped code bug. Two practical Playwright lessons reinforced here: (i) cross-test localStorage pollution (a prior language-switcher test leaves `career-ops-ui:lang=ru` in the shared context) — fix with `page.addInitScript(() => localStorage.setItem('career-ops-ui:lang','en'))` so the SPA reads EN at module init *before* any view renders; (ii) use a locale-stable selector (e.g. the `▶` glyph is identical across all 8 locales) instead of localized button text.
- **CSP is unconditional (v1.58.4, NEW-1).** Before v1.58.4 the `Content-Security-Policy` header was layered on only when `isPubliclyExposed()` was true (HOST bound beyond loopback). The v1.58.3 MASTER regression (§5) flagged that `/` and `/api/health` returned **no** CSP on `127.0.0.1`, leaving `UI.md()`'s escape-first contract as the sole XSS defence — defence-in-depth must not depend on the bind address. CSP is now always emitted with the identical directive set. The directive set itself is unchanged (Google Fonts allowlist preserved for Inter; `script-src 'self'` only, never `'unsafe-inline'`/`'unsafe-eval'`). When you add a new asset/source, update both the policy in `server/index.mjs` *and* the route-walk in `tests/playwright-smoke.mjs` — the latter monkeys console errors for `Refused to … because it violates the following Content Security Policy directive` across en/ru/ja/zh-TW × 7 routes.
