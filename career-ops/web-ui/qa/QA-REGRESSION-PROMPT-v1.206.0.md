# QA REGRESSION PROMPT — career-ops-ui **v1.206.0** (Docs: in-app help ×17 covers the 5 newest features)

**Documentation release.** The in-app help guide — and the "Ask the docs" assistant that answers from it — now documents the five most-recently-shipped features in **all 17 languages**: **Setup doctor** (`#/config`), **Discover ATS boards** (`#/portals`), the **"still live?"** check (`#/tracker`), the **"reuse a past CV?"** hint (`#/cv-studio`), and the **Skills log** (`#/assessments`). No server or SPA code changed — this ships help content + reference-doc actualization.

- **Under test:** `package.json` **1.206.0**. **Server:** `npm start` → `http://127.0.0.1:4317`.

## §0 — Gates

```bash
npm test                                     # 2610, exit 0 (capture $? directly, never | grep)
node scripts/check-changelog-parity.mjs      # 16 non-EN at v1.206.0
node --test tests/canonical-docs-coverage.test.mjs tests/help-ui.test.mjs \
            tests/help-ru-config-section.test.mjs tests/locales-de-it-tr.test.mjs
```

## §1 — What changed (and what did NOT)

- **`docs/help/<lang>.md` ×17** — five new H3 subsections, one per feature, slotted under the right H2 (Setup doctor → §config; Discover → §portals; Liveness → §tracker; Reuse → §CV Studio; Skills log → §stats). Structure is now **H2 31 / H3 118** (was 113), parity-gated across every locale. RTL (ar) + CJK dividers verified; **no English text leaked** into a translated file.
- **Gate tests bumped** — `help-ru-config-section.test.mjs` and `locales-de-it-tr.test.mjs` H3 assertion 113 → 118 (the other two gate files count H2/H3 dynamically).
- **Reference docs actualized (no behaviour):** `docs/architecture/API.md` gains the five routes behind these features (`GET /api/liveness`, `POST /api/portals/discover` + `/track`, `GET /api/cv-sync-check`, `GET /api/assessments` + `POST`, `GET /api/jds/:name/reuse` + `/skill-gap`); `CLAUDE.md` + `docs/sdd/CONVENTIONS.md` route-module count **32 → 36**; version anchor → 1.205.0 baseline.
- **Build artifacts self-heal:** `site/src/content/help/` and `site/src/generated/facts.json` are **gitignored**, regenerated by `site/scripts/sync-assets.mjs` in the Pages prebuild — no manual commit; they refresh to v1.206.0 / 2610 / 79 adapters on deploy.
- **CI only:** AI-review model switched to `qwen/qwen3.8-27b` (`OPENROUTER_REVIEW_MODEL` repo var + `ai-review-call.sh` fallback default). Not user-facing.

## §2 — Manual smoke (deployed)

`GET /api/help/en` (and a non-Latin locale, e.g. `/api/help/ru`) → 200, `{ lang, markdown }`; the markdown contains the five new section headings. In the app: open the help view + the "Ask the docs" FAB and confirm a question about e.g. "still live" returns an answer grounded in the new section. Read-only; no writes, no user data touched.

## §3 — Sign-off

Suite **2610** green · CHANGELOG parity ×17 at v1.206.0 · README badge+banner ×17 · help H2 31 / H3 118 across 17 locales · zero server/SPA code delta · no new dependency, no parent edits.
